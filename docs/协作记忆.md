# 协作记忆（跨会话）

> 作用：新会话通过 CLAUDE.md 自动读到本文件，快速恢复「用户是谁、怎么协作、现在在做什么」。
> 维护规则：每个会话结束前由 AI 主动更新（追加/修订/删除过时条目）；用户口头修正永远优先于本文件内容；本文件只记事实，不记猜测。
> 首次建立：2026-08-13。

---

## 一、用户的协作习惯

| # | 习惯 | 说明 |
|---|------|------|
| 1 | 中文交流 | 所有对话、文档、注释用中文 |
| 2 | 先讨论再动手 | 范围广的改动先确认再执行；用户明确指派的小任务可直接做到底（含构建+真机验证） |
| 3 | 绝不 push GitHub | 只允许本地 git 提交；发布到 github 仅在用户明确要求时做 |
| 4 | 不删构建缓存 | android/sdk、.gradle、build、dist 等一律保留（删了构建全量重跑） |
| 5 | 模型分工 | 默认挂 flash 执行；规划/架构决策/系统级错误排查，AI 在回复中标记「建议切 pro」，用户手动切换后同会话继续 |
| 6 | 真机验证 | 用户会提供 adb 手机链接（设备 704c2441，装有本地版+服务器版两个 APP）；安卓改动应装真机验证 |
| 7 | 服务器可运维 | 可 SSH 连服务器 101.200.14.126（root + C:\Users\FANGL\.ssh\id_rsa）；生产环境改前先备份 user_data；已有每日 3 点备份 cron（/opt/backups）+ attack_watch 每分钟 cron；8765 只本机、公网走 8787 反代 |
| 7 | 图片兜底 | 会话模型无图像输入时，用 Windows 内置 OCR（WinRT PowerShell）识别截图；uiautomator dump 读页面文字；**识图用 vision 技能（mimo-v2.5）**——已接入会话工具并固化到个人预设（见下方「进行中/最新」） |
| 8 | 沙箱按需升级 | 被沙箱拒绝时可按规则升级重试；用户曾直接「去掉沙箱」授权全权限 |
| 9 | 文档规范 | 树状分文件放 docs/，单文件 ~200 行内；重要结论在聊天里给摘要，全文进文档 |
| 10 | 服务器版隔离 | server/ 代码不入 git；前端以 app/static 为唯一源，server/frontend 与 android assets 经 tools/sync_frontends.py 同步 |
| 11 | 发布前校验 | 发版必须跑 tools/check_version.py；5 处版本号同步 |
| 12 | 研究目录隔离（硬约束） | `FireflyVoiceResearch/` 是**进行中的研究**，与日常开发分开只为避免互相干扰——**永不移动、永不清理、永不入库、任何清理批次都不许动它**（已 gitignore；rg/git 默认跳过，不影响开发链路）。同理 `论文/` 属研究侧。<br>**例外（2026-09-12）**：用户明确指示清理时按指示办。该次清掉了实验音频产物、可再生的解码缓存、已被取代的一次性脚本（共释放 ~356 MB）；**保留**源素材、正在用的参考、交付物、量化/导出工具、未结案问题的试验台。⚠️ 该目录**不入 git，删除不可恢复**——清理前必须先列出清单确认 |
| 13 | **绝对禁止子代理（硬约束）** | **永不调用** `subagent`/`subagent_fork`/`workflow`/`ralph` 或任何派生执行体。2026-09-11 事故：派 2 个子代理，它们自行派生到 3 层共 24 个，**不到 5 分钟 18 元**（当日 token 1.317 亿）。调研用 `web_search`/`web_fetch`，审计用 `grep`/`read`，长任务用后台 job——**主会话自己做** |
| 14 | **先控制变量再下结论** | 2026-09-12 语音参考排查教训：先认定"我的素材不行"，再做 6 轮"证伪+换下一个"，全错——真因是**参考音频时长**（拿长参考比短参考，比较本身无效）。多可疑因素时先设计**能分离变量**的对照（2×2 起）。另：**长时间任务产物复用前必须校验完整性**（"文件存在"≠"文件正确"），**阈值必须由实测分布标定**（凭直觉定 3.0，实测应为 2.0，会误杀 24/28）。详见 `docs/错误总结.md` #8 |
| 15 | **改动验收与验证手段（2026-09-13 新增）** | ①每张任务卡收尾必跑：全量 `tests/test_*.py` + 四门禁（check_version / sync_frontends --check / build_frontend_bundle --check / check_backend_sync --check）；②`app/` 下任何改动都会让**安卓后端镜像**漂移 → 跑 `python tools/check_backend_sync.py --sync`；③**前端行为改动优先用真浏览器验证**（Playwright + 沙箱 `FIREFLY_ANDROID=1 FIREFLY_DATA_DIR=<临时目录>`，脚本按 `tests/manual_*.py` 惯例入库、不进自动化套件）；④**别用 PowerShell 的 `Get-Content -Raw` / `-replace` / `Set-Content` 改 UTF-8 文本文件**——默认按 GBK 读入，实测把脚本改成乱码还吃掉换行；改文本一律用 Edit/Write 工具；⑤`git commit -m "中文含双引号"` 会被 PowerShell 拆成多个参数（报 `pathspec ... did not match`）→ 提交信息写进文件用 `git commit -F`；⑥**冒烟/验证脚本一律自建沙箱数据根**，端口被占（8765 可能有用户实例，server.py 会主动 shutdown/强杀占用者）直接退出，绝不误杀真实进程 |

## 二、用户关注的方向

- **当前主线**：双线版本推进——本地版「场景真实化」；服务器版「部署验证与推广」。
- **近期关注点**：产品体验与设计感（如服务器版 APP 顶部 ActionBar 修复）；**小白使用者的易用性**（新用户是否会被门槛劝退）。
- **历史待办清单**：原 `docs/三版本APP缺陷分析执行文档.md` 已随 0.8.0 完成归档至 `封装（没指定就不读）/2026-08-16_归档/docs/`。
- **技术偏好**：依赖最小化（能自己实现不引库）；架构上显式记录取舍（YAGNI）；改动前先讨论。

## 三、当前工程状态快照

| 项 | 状态 |
|----|------|
| 版本 | **0.8.1**（`core.config.APP_VERSION` / versionCode 806）——2026-09-18 订正，原记 0.8.0/805 |
| 服务器 version.json | 仓库源 **0.8.1**（含 exe/apk https 直链）；线上是否同步见下行部署状态 |
| 服务器代码 | **2026-09-18 实测更正**：08-25 安全修复**已部署**（09-13 带入，token 全哈希/schema v3 可证）；09-15 两批修复**仅缺 2 个文件**——`server/download_page.py`（A4，线上仍是旧版）与 `app/shared_http.py`（A1/A2，对服务器模式无影响）。详见 [审计-服务器与开发版-2026-09-18](审计-服务器与开发版-2026-09-18.md) 第一部分 |
| 服务器版/本地版 APK | 均已重新构建 + 真机验证（导出下载链路实测通过） |
| 本地版 PC exe | console=False 无黑窗口版已构建验证 |
| 前端三副本 | 已同步（app/static、server/frontend、android/app/src/main/assets；login.html 已消除历史漂移） |
| check_version.py | 已扩展覆盖 8 处，全 PASS |
| 已知未修 | 微信输入法键盘 bug、PC 图标白方块；数据导入、HTTPS/域名、主动性自然语言文案待做 |

## 五、进行中 / 最新

- **架构重构 阶段 2 + 阶段 3 全部完成（2026-09-13，本地提交至 `7734d87`，未 push、版本号未动、服务器未部署）**：仍在主会话直接执行、**零子代理**。**阶段 2（7 张卡，纯移动）**：`app_config.py`(1043) → `core/{paths,userctx,config,presets,migrations}.py`；`routes_data.py`(974) → `api/{data_export,data_sync}.py` + `infra/sync/restore.py`；`routes.py`(795) → `api/{chat,debug,router}.py`（chat() 拆三函数）；`server_app.py`(720) → `quota/admin/auth_endpoints/app`（server/ 不入库，tar 备份在 `_harden/`）；`panels.js`(842) → 外壳 + `panels/{packs,data,debug}.js`（bundle 门禁改 rglob 支持子目录）；import 期副作用清零（`mode_root` 改纯计算 + `ensure_mode_root`）；`sticker_picker` 归位 `domain/stickers/picker.py`（旧路径留兼容层）。**关键经验（已写进测试守卫）**：兼容层必须做**写转发**（`_ShimModule.__setattr__` 落回所有持有该名字的 core 模块）——否则 tests 的 `cfg.USER_DIR = tmp` / `sp._REGISTRY_FILE = tmp` 这类补丁全部失效，会写进真实 user_data；"顺带 import" 的名字也是对外契约（routes 88 个）；纯移动对用 `__file__` 相对深度的模块**不成立**（sticker_picker 归位后指错目录）。**阶段 3（8 张卡，行为变化）**：3.1 REGISTRY 加 owner；3.2 `user_data/packs.json` 成为包存在性唯一权威（目录双向自愈，内置包不入清单）；3.3 卡定义槽位三态 baseline/inherited/customized（**记录优先**：有记录就信记录，修掉"内置更新后未编辑副本被误报已修改"；内置包槽位另存顶层 `bundled_slots`）；3.4 快照 `_manifest.json`（打包/核对共用一套哈希口径；旧 zip 无清单照旧可恢复）；3.5 **删除两级化**（`/pack-archive` + `/pack-restore`；`delete_pack` 只允许抹除 archived 包且**抹除前强制全量快照落 `backups/pack-erase-*.zip`（失败即中止）**；贴纸 `pack` 弱引用降级为全局共享；前端主按钮改「归档」+ 首页归档区）；3.6 记忆游标自愈（`verify_index` 在 `wake()/rest()` 兜底）；3.7 server db v3 迁移（两个索引，`ensure_auth_columns` 定性为历史兼容路径）；3.8 快照/同步白名单切 `backup_pack_ids()`（= MODES ∪ 清单含 archived；**归档 ≠ 丢保险**）——3.8 与 3.5 是同一件事的两半（前者让归档包进得了备份，后者才敢以"必有备份"为抹除前提）。**新方法（建议复用）**：凡是"模式 id → 目录"的定位，一律用 `cfg.pack_root(id)` 而**不是** `mode_root(id)` —— 后者对不在 MODES 的 id 会静默回退默认包，归档包会被读写到 story 的目录（本阶段在同步链路与快照恢复两处都踩过）。**出口**：58 个 `test_*.py` 全绿、四门禁 PASS、每卡 PyInstaller 冻结冒烟；**真实 `user_data/` 副本升级演练**（`.tmp_test/drill_p3.ps1`，跑完即删，生产目录全程只读）四段全过：老安装升级、清单损坏/删除自愈、两级删除走查、数据完整性对比（唯一"丢失"的 3 个文件是既有 R-04 清理删掉的"与内置逐字相同（仅 CRLF/LF 差）"副本，零信息损失）。**待用户表态（本轮汇报重点）**：① 归档/恢复/抹除的**真实浏览器**点击走查是否现在做；② 阶段 2 出口遗留的"无文件 >500 行"还差 8 个（views.js 现 799 行最突出）——补拆还是并入阶段 6；③ 真实孤儿目录 `user_data/custom_f447527f/`（只有空 `character/`）是否清理；④ 1.7 托管链路冷却取舍；⑤ `server/` 四批改动（1.1/1.2/2.4/3.7）待人工部署；⑥ 阶段 4（业务深化 + 语音插件，语音按用户指示只留接口）是否开工。

- **架构重构 阶段 0 + 阶段 1 全部完成（2026-09-13，本地提交至 `1ec443c`，共 14 个 commit，未 push、版本号未动、服务器未部署）**：依据 `docs/架构重构计划/`（规划者 Kimi K3 出的指导文件集）+ `项目架构图/architecture-refactor.html` 执行，**全程主会话直接做、零子代理**。**阶段 0 保护网**：①路由 oracle 测试钉死 57 POST + 28 GET 端点（阶段 2 拆分验收基础）；②快照三回归（R-01 未注册自建包完整落地 / R-02 恢复中途失败旧数据原样 + 换入回滚 / E-1 pre-restore 失败必逐包回落）；③门禁修复 D-1/D-3（check_version 缺失文件记 SKIP 而非 FAIL；bundle ORDER 与 assets 清单改**集合双向断言**——注意 `js/pc_nav.js` 是故意不进 bundle 的、`app/assets` 根目录有 4 个无引用遗留文件，计划书的"目录全量=期望集合"与代码不符，按"以代码为准"处理）；④四门禁写入发版清单。**阶段 1（12 张卡全绿）**：1.1 托管授权账号化（P0 安全敞口：原先任何登录账号带 `X-API-Mode: proxy` 就能烧运营者 Key）——**发现 `quota_api_proxy` 列只有读、无任何写入入口，上报后用户拍板"加最小管理台入口"**，遂补 `POST /admin/api/users/quota` + 用户表额度列；1.2 `FIREFLY_SERVER` 硬置（空串环境导致服务器版静默按本地版跑）；1.3 自动备份统一裁剪；1.4 快照上传校验（PK+zip+解压炸弹）；1.5 clear-history 状态复位（键是 `(mode,scope)` 元组，按字符串 pop 恒 no-op）；1.6 REGISTRY 通配项匹配（零行为变化，已证）；1.7 API 冷却加用户维度；1.8 `/set-config` 加锁（**关键补充：锁内必须重读盘取基准**，只加锁仍丢更新，已用打桩回旧实现证测试有鉴别力）；1.9 当前包持久化（**真浏览器 5/5**）；1.10 孤儿包可清理 + 建包失败回滚；1.11 `knowledge_dirs` 越界校验（**实测 Windows 下 `ROOT / "/etc/passwd"` => `F:\etc\passwd` 真能逃出仓库**，旧代码靠 `strip("/")` 侥幸挡住）；1.12 包详情页代际保护（**真浏览器 3/3**：A 页保存写回 A 而非切换后的 B）。**顺带**：修掉 `test_fault_tolerance` J 段的随机 jitter flaky（根因是断言依赖随机 sleep 恰好耗尽预算，改为确定性注入；顺带让 J2 真正守住"截断"语义）。**状态**：全量 43 个 `test_*.py` 全绿；四门禁全 PASS；架构图视图⑫ 这 12 项已标 fixed。**下一步**：阶段 2 模块拆分（app_config→core/ 五文件、routes_data→三文件、routes.py→api/、server_app→四文件，纯移动）→ 阶段 3 数据所有权与包管理（**阶段 3 完成后停下汇报**，那是行为变化分界）。**待用户表态**：真实孤儿目录 `user_data/custom_f447527f/` 是否清理（能力已具备）；1.7 托管链路的冷却取舍；`server/` 不入库 → 1.1/1.2 的服务器改动需人工部署。**语音**：按用户指示"暂时不实装，只留功能接口与处理逻辑"，阶段 4 的语音插件待研究完成后再开工。
- **角色卡深化六项全部完成（2026-09-08，本地提交至 `62b22be`，未 push、版本号未动、服务器未部署）**：在角色预设化（七阶段）基础上的第二轮。边界拍板：**角色卡=基础设定（core/identity/sms_samples/prompts/封面/头像/知识库/开场/表情包），用户数据=互动产生（对话/记忆/手账/收藏）**。①核心设定放权：core/identity/sms_samples 开放编辑（保存即 clear_cache 生效）；`routes_pack.py` 拆分（包管理端点集中）；`pack-files` 的 customized 语义=与 bundled 内容不同才算（首启拷贝不误标）。②主动消息按包：配置存 `user_data/{mode}/data/proactive.json`（`pack_cfg/set_pack_cfg`，读取 包级→全局→默认）；设置页主动消息模块整段撤除，详情页新增「主动消息」区（防抖自动保存）；**时间锚定规则**（analyzer+memory_manager：相对时间词按说话当天日期锚定为绝对日期——修「昨天说后天今天还是后天」）。③表情包进详情页：网格管理本包可用（全局共享+本包专属），菜单改「全部表情包」总览带归属标注；边界确认**角色卡含表情包**。④服务器数据快照：`routes_snapshot.py`，全包打包（含 stickers 用户表情包+_config 剥Key不自动恢复），存 `{user_root}/snapshots/` 每账号限 5 份滚动；本地版已登录自动转发认证服务器（换机可恢复）；删本地备份组（导出 zip 并入快照）。⑤全包化：导出默认全包快照、导入识别快照格式按包分发、sync_now 遍历所有包（`_sync_one_mode` 抽主体）；snapshot_restore 复用 `_restore_full_snapshot`。⑥AI 搜索建卡向导：`web_search.py`（DuckDuckGo IA/HTML + Bing 国内 + 萌娘百科 MediaWiki 四源兜底，无 Key，国内不可达自动降级，实体解码）+ `pack_forge.py`（内存会话状态机：搜索→core→identity→sms_samples→polisher 分步草案可编辑确认→落包）；前端卡片列表页「✨ 让 AI 帮我搭」向导区。**踩坑记录**：heredoc 写大段代码第二次转义污染（sync_now 重构混入废稿）——**复杂重构一律用 Edit 分步，不用 python heredoc 写文件**；Edit 两次误删相邻行（函数头/标签行被 old_string 包含吃掉）——Edit 的 old_string 必须最小化。LLM 生成链路（pack-forge）未实测（不主动消耗 API 额度，待用户验）。**剩余**：服务器未部署（快照/全包化/服务器版适配待部署时验证）；PC exe/安卓 APK 未重建；语音研究由用户在其他 AI 处推进。
- **角色预设化（全角色扩展）七阶段全部完成（2026-09-05，本地提交至 `ca98839`，未 push、版本号未动、未部署服务器）**：方案 `docs/设计/角色预设化方案.md` + 盘点清单（8 项不确定点用户拍板按倾向执行）。核心：「模式」解耦为角色×剧本×形态，**一包 = 一个角色 × 一个剧本**（story=流萤·主线、haruno=流萤·春日手信各成内置包）。①注册表：MODES 从写死改扫描 `assets/character/*/preset.json`（含 `knowledge_dirs` 声明），本地版追加扫用户自建区 `user_data/{id}/character/preset.json`（服务器版跳过用户区），`reload_presets()` 支持运行时刷新。②角色名/用户称呼全后端变量化（`char_name()/user_name()` 访问器；渲染基线逐字对比 13/13 一致）。③提示词槽位化：人设段/分析器核查段/组织器双提示词/主动情境文案全部入包 `prompts/*.md`（`render_pack_prompt` 三段兜底），输出协议段框架锁死；**缓存未破**（逐字一致）。④编排泛化：跳检索改 `has_knowledge()`、环境后缀入包、开场泛化 `preset_opening()`、形态按 `presentation` 分发（sticker/narration/none）、`preload_knowledge` 按包预热。⑤前端泛化：模式卡片/轮播/头像/角色名/签名全部从 `GET /modes` 动态渲染；视觉资产入包 `assets/character/{包}/assets/`（开拓者_穹/星留全局=用户形象不入包）。⑥软件内资产编辑：表情包加 `pack` 字段（空=全局共享，现有数据零迁移；新增按当前包归属；组织器按包过滤）；「角色包」管理页（头像/封面替换与恢复默认、六段提示词折叠编辑、custom 包删除入口）；端点 /pack-files、/pack-asset、/pack-asset/delete、/character-file/delete。⑦新建角色：首页「+ 新建角色」卡（仅本地版）→ 表单 → `custom_xxxxxxxx` 包 → 管理页编辑人设；全链路实测通过。**踩坑记录**：Cygwin shell 的 Bash 通道一度全挂（wrapper 用 /f/ 前缀但实际环境是 /cygdrive/ 前缀，重启无效）——**绕过法：Bash 工具显式传 `cwd: "/cygdrive/f/CodeFile/firefly"`**；analyzer 删常量时行号脚本误删类定义（匹配串在引用行二次命中），恢复后改用 Edit 精确文本锚点——大段删除优先用 Edit 不用行号脚本；test_fault_tolerance J1 第三次 flaky（deadline 0.5s 余量时间敏感断言，高负载偶发，复跑即过，值得哪天顺手加固）。**剩余**：PC exe/安卓 APK 未重建、服务器未部署（部署时注意 knowledge/ 未移动——story 经 knowledge_dirs 声明引用原位，部署清单不变）；语音研究由用户在其他 AI 处推进。
- **技术负债清理三连（2026-09-04 完成，已本地提交）**：用户拍板「按顺序进行」后执行 05 负债表三项——①**setting_fix 删悬空引用**：`CONTEXT_ONLY_FILES`（haruno world/plot.md 从未产物化，运行时恒空路径）+ `load_context_files` + run_alignment/run_proposal 两处消费点全删，test_setting_fix 断言同步；资料卡若日后产物化（素材 `docs/搜索资料结果/春日手信世界构建_完整资料卡.md`）再恢复，见 git 历史。②**降级话术合并**：单一来源 = `polisher.DEGRADED_TEXT`（「嗯…信号不太好，等会儿再试试？」），orchestrator `api:error` 引用常量、前端 chat.js 注释对齐——实际是三处副本统一（orchestrator 旧句「信号好像不太好，你等一下哦」已废）。③**_HARUNO_OPENING 归位**：新建 `app/assets/character/haruno/opening.json`（bundled 角色资产），orchestrator 改经 `resolve_character_file` 读取（用户副本优先同语义，解析失败降级无开场+日志），删 `first_message` 单数键死兼容、修正返回值标注 dict→list。**「不新建文件」约定澄清**：它约束的是设定纠错助手的六文件白名单（AI 提案不许新建文件），不约束开发者归位角色资产——05 负债 #6 原表述的"冲突"系误读。验证：全量 33/33 绿、sync_frontends --check 一致、diff --cached --check 通过；文档 03/05 负债表已同步更新。
- **08-29 两项已验收改动提交（2026-09-04，本地 commit `2f18c2b`，未 push）**：提示词系统梳理+RAG 清理 + 图片链路归并 + 提示词系统 5 册文档入库（26 文件）。提交前全量 33/33 绿（test_fault_tolerance J1 曾偶发失败一次，单测复跑过——deadline 0.5s 余量的时间敏感断言，高负载下 flaky，非回归）。顺手修 llm_retriever.py 尾部空行（diff --check 拦截）。环境备注：本会话 shell 为 Cygwin 路径（/cygdrive/f/...），git log 需 `PAGER=cat` 否则 pager 卡死；输出首行偶发被吞。

- **提示词系统梳理 + 向量 RAG 残留清理（2026-08-29 完成，2026-09-04 已提交 2f18c2b）**：起因——用户对上一份（Kimi 版）梳理提出 5 个质疑，逐条核查后：①**"RAG"确指已抛弃的本地向量库，不是检索器**（app/ server/ 无任何 embedding/vector 实现，build_index.py 已不存在）；死代码是 `analyzer` 的 `knowledge_query`/`memory_query`（模型每轮真输出、全项目零消费者）+ `retrieved_memory`（恒空）+ 提示词「### 4. 还需要什么信息？」整节，**已全部清理**，顺带修正「供组织器直接使用」→「供回复器」、删死变量 `_ANALYZER_SYSTEMS`、改 orchestrator 过时文档串「向量检索→」。测试 analyzer/orchestrator/polisher/organizer/llm_retriever 五套全绿（16/17/14/13/12）。android 副本是 build.gradle.kts 构建期自动拷贝，无需手工同步。②**实测数据推翻两个直觉**（`pipeline.jsonl` 44 轮 story / 15 轮 haruno）：分析器才是唯一大头（中位 12.98s，占 55%，P90 25s），检索器只占 26% 且最稳（缓存命中），组织器 story 仅 0.98s（4%）——"先把组织器后台化"在 story 几乎无收益，但在 haruno 值 26%（旁白是 Think High 创作）。③**撤回一条旧论断**："环境句时间推断 vs 模板时间规则存在张力"逐行核对**不成立**，勿再引用。④**归因验证（同日完成）**：分析器慢**不是**输入长导致——检索摘要长度与分析器耗时相关性 r=+0.10（不显著），控制产出量后偏相关 −0.021；haruno 无检索摘要（长度恒 0）分析器照样 11.67s；最慢/最快 25% 轮的输入几乎相同（1016 vs 973 字符）而思考量差 7.3 倍。四节点合并回归（217 点）得统一规律 `耗时 ≈ 1.84s + 产出字符/192`（R²=0.67，去最慢 2% 后 0.88），而以 system 体量为自变量 R²=0.001。**结论：耗时只由产出量决定，分析器慢是因为它按提示词逐项思考（产出是回复器的 6.6 倍，71% 是思考文本）——用户判断正确，属"仔细思考"的必要代价，不是浪费；"砍分析器输入提速"已被数据排除。**⑤禁令实测 27 条（story 16 + haruno 11），其中 9 条是通用大模型腔病、两份手抄副本**已分叉**（story 有「复读自己」haruno 漏了）。文档落 `docs/设计/提示词系统/`（README + 4 分册）。**用户明确态度：历史事故教训不足以论证设计，一切以用户实际体验为准。**

- **图片链路归并（2026-08-29 完成，2026-09-04 已提交 2f18c2b）**：采纳用户拍板「只保留模型原生识图一条链路」——删除上传时的自动文字描述（`vision.py` 的 `describe_image`/`_DESC_PROMPT`/vision_desc 计数全删，只留 `to_data_url`；`routes_assets.upload_image` 不再生成 desc）；图片理解统一由模型原生识图（发图当轮原图字节注入回复器，`polisher` 动态 user 段加「原生识图说明」：先确认媒介、看不清如实说、禁止猜来历，不破坏 system 缓存）；随后按用户二轮拍板**定稿**：不做任何能力猜测（`orchestrator._caps_vision` 整函数删除——模型名判据不可靠，mimo-v2.5 名字无 vision 却支持识图，默认即 DeepSeek 识图模型）；取消弹窗（前端 `chat_media.js` 删 `window.prompt`，`routes_assets` 不再返回 `need_desc`，3 份前端已 build+sync）；模型真不支持识图时上游 4xx 拒绝 → polisher 降级为「图片好像看不清……」诚实说明（不脑补内容，此为「无法识图返回合理说明」的落点）。**用户明确要求：改动以简化/删减为原则，不堆砌条件**；且**只改核心流程（发送→提交前），前端消息队列逻辑（_chatSend/批次窗口）零改动**——本轮 diff 已证（chat.js/chat_render.js/state.js 等为空）。同批清死代码：asset_or_placeholder、_DIRECT_REPLIES["image:unseen"]、reset_knowledge_cache 三处零调用全删。业务全流程 + 技术负债盘点落 docs/设计/提示词系统/05_业务流程与技术负债.md（本轮已把恒 True 函数连同三元表达式删掉；polisher fallback 压成一行三元；注释压平）。起因：二创图被脑补为「银狼拍的/什么时候拍的」→ OOC，根因是旧描述提示词禁止模型说出媒介。单测全绿（e2e_20turns 需真实本地服务+联网，不在本次范围）。

- **管理台「最近请求」账号归因（2026-08-28 完成，已部署）**：起因——网关（8787）日志 tab 账号列恒空，无法定位"哪个账号的请求"。修复：`download_server.py` 的 requests 表加 `user` 列（`_db_connect` 内 PRAGMA 检测 + ALTER 原地升级），`_record_request` 新增 `_user_for_request()`：Bearer token sha256 查 sessions 联 users 得邮箱（明文遗留行兜底、匿名/失败一律空串、只读 mode=ro 绝不阻塞响应）；`server_app.py:_gateway_recent` SELECT 带 user + 旧表结构兜底。admin.html 零改动（本就渲染 x.user）。验证：本地临时库 4 例全过（哈希/明文/匿名/无效）、33/33 测试绿、生产 schema 已迁移、匿名与无效 token 行实测正常；真实登录用户的归因行待有机流量自然出现。回滚备份：服务器 `*.bak_reqattr` 两文件。主服务 tab 的账号列此前已有（_log_recent 带 email），本次补的是网关 tab。

- **Kimi 项目梳理+加固部署+大重构（2026-08-28 完成，版本号不动 0.8.1）**：①安全加固（08-25 GLM 审查产物）**已提交 8922a56 并已部署服务器**——部署前对生产 md5 全量比对（差异=加固+2 处落后：sticker_picker.py 停在 W5 前、frontend/api.js 停在 8996675 前，均已一并带齐）；备份 `/opt/backups/{user_data,code}_pre_harden_20260828-1605.tar.gz`；重启后验证全绿（db v2 迁移 14 条明文 token 透明哈希化、公网实测假 2GB Content-Length→413、resolve→foruda 直链、CORS 预检正常）；顺手删除生产 `/opt/firefly/app/` 下 4 个 server 文件惰性重复副本（ExecStart 证实跑的是 server/ 副本）。**版本号未动，旧客户端无感**（多次强调勿升版本）。②超大文件拆分（路由 oracle 键集对比+全量 33/33 测试绿为验收）：routes.py 2680→750+8 子模块（routes_common/auth/config/assets/fix/data/update/relay）；proactive.py 1009→337 facade+proactive_gate+proactive_gen（`modules.proactive.<名字>` 全量 re-export，测试打桩点兼容）；前端 panels.js 1218→498、chat.js 1098→574，新增 settings/update/sync/chat_render/chat_media/history 六模块（**CURRENT_VERSION 已移到 update.js，check_version.py 同步改验**，bundle ORDER 更新，函数集合 171==171 不变）。③死代码清理 21 项（逐项 grep 取证）：**安卓 FireflyJs 桥已补注册**（考古结论：该桥从未在 Kotlin 存在，此前服务器模式后台主动通知静默失效；现对齐「仅后台打扰」语义）、删 set_admin/_SCHEMA_VERSION/ASSETS_DIR/VALID_MODELS/server_url 死配置、迁移 bak 保留 5 份上限、GLOSSARY/协作记忆/.gitignore 清 android_server 幽灵引用、本次更改说明.md 归档至 封装/2026-08-28_归档。④新增 `项目架构图/index.html`（交互式架构可视化，单文件离线，数据为拆分后实测口径）。**注意：以上②③只提交本地 git（5 个 commit），服务器跑的还是拆分前代码（行为等价），安卓 APK 未重建（allowBackup/FireflyJs 修复待下次构建生效）**。剩余债：明文 HTTPS 未做（最高优先，待域名决策）、quota_api_proxy 字段未启用、_SESSION_MAX 全局帽。（docs/项目结构报告.md 等过时文档已在 docs 整理中归档，见 dca6a74）

- **安全加固集中修复（2026-08-25 完成，2026-08-28 已提交 8922a56 并部署服务器，版本未动）**：三端安全审查后修复 10 项——①网关 `_proxy` 请求体上限 16MB（原任意 Content-Length 直接 read 可单连接 OOM；411/400/413 三段校验）；②relay SSRF 双层拦截（X-API-Base 入口 + relay_proxy 出口均过 `api_client.is_public_endpoint` 公网校验：DNS 全记录、环回/私网/链路本地/CGNAT 全拒，host 级 300s 缓存）；③**定性修正**：img_id「跨用户穿越」实测不可达（pathlib glob 的 `..` 是字面量匹配，Python 3.12.6 实证），真实问题是绝对路径 pattern 抛未捕获 NotImplementedError——已加 `[A-Za-z0-9_-]{1,64}` 白名单（routes 的 _load_image_data_url 与 get_image 两处统一）；④sessions token 落库改 sha256 哈希（db schema v2 迁移自动改写存量 + 旧明文行命中时就地升级，auth.py 零改动）；⑤verify_password 改 hmac.compare_digest；⑥loginfail 键写路径自愈清理（db.prune_login_fail_keys，防邮件喷洒撑 settings 表）；⑦Android allowBackup→false（**重建 APK 才生效**）；⑧multipart 畸形 Content-Length 安全拒绝；⑨AdminHandler body 64KB 帽。顺带修 test_image_unseen flaky（硬编码 sleep 0.2s 撞窗口注册竞态→轮询等待；stash 验证确认与安全改动无关）。**验证**：新增 tests/test_security_hardening.py 59 项断言全过；全量 33 测试文件退出码全 0；check_version / sync_frontends --check / build_bundle --check 三门禁 PASS。文档：docs/安全加固执行记录-2026-08-25.md（含未实施建议清单——公网 HTTPS 为最高优先遗留项）。**server/ 四文件（download_server/server_app/auth/db）改动前备份在本机 QoderWork 工作区 backup_20260825_security/；部署生效提示见报告第七节（db v2 迁移在服务重启时自动执行，用户无感知）**。另：用户已明确知识库全量注入是有意设计（RAG 无法处理抽象用户语言 + 安卓不可行），勿再作为问题提出。

- **消息引用与收藏（2026-08-17 完成，PC 验证通过，待装机）**：聊天页长按消息（触屏按住 500ms / PC 按住或右键）弹出 QQ 式菜单「📎 引用 / ⭐ 收藏」。引用 = 输入框上方引用条 → 消息带 `quote` 快照落 jsonl（白名单字段 + 2000 截断），LLM 侧 `compose_user_text` 合成 `[引用流萤：「…」]` 前缀（重启回灌 hydrate 同样带），气泡上方渲染 `.quote-preview` 小卡片。收藏 = 快照式存储 `{mode}/data/favorites.json`（`app/modules/favorite_store.py` 新模块，上限 300、按 seq 去重、无 seq 生成 f 前缀 id、随导出/同步 zip 自动打包），☰ 菜单新增「收藏」页签（6 页签网格）。接口 POST /favorite、GET /favorites、POST /favorites/delete（server_app 复用 routes 自动生效）。测试：tests/test_quote_favorite.py 37 项 + Playwright `.tmp_test/verify_longpress.py` 13 项全过（含「重复长按菜单唯一」断言）。**踩坑记录**：① `_showMsgMenu` 漏 `_msgMenu = menu` 赋值导致菜单叠加（用户 PC 复现），已修；② Playwright `mouse.down()` 不派发 pointer 事件，需 dispatchEvent 合成 PointerEvent；③ 首次引导教程会中途 showHome 干扰浏览器测试，需 init_script 预置 guide done 标记；④ **浏览器自动化测试会真实触发流水线消耗用户 Key 额度并写污染进 user_data**——先临时改名 config.json 进无 Key 模式再测，测完恢复；用户数据已截断恢复（seq 119 止）。文档：docs/设计/消息引用与收藏.md。未部署服务器、未发版、未装机（安卓侧用户自行处理）。
- **管理后台一键进入（2026-08-17 完成，链路实测）**：`_harden/`（gitignore）新增 `启动管理后台.cmd` + `admin_launcher.ps1` + `admin_proxy.py`——双击自动 SSH 隧道（8766）→ 本地令牌注入代理（127.0.0.1:8768）→ 打开浏览器直达管理台，免输令牌。**关键根因发现**：生产 AdminHandler 对 admin.html 页面本身也校验 X-Admin-Token（`do_GET` 全体 `_authed()`），浏览器导航不带自定义头 → 页面 403，这就是「本地无法进入管理界面」的直接原因；API 带令牌头是通的。方案：代理对所有转发请求附加 X-Admin-Token 头 + HTML 注入 localStorage 双保险，服务器零改动、直连 8766 仍 fail-closed（实测 403）。全链路实测：无头浏览器经代理加载生产 admin.html → 用户列表/请求面板正常渲染、无 JS 错误；真实令牌（_harden/admin_token.txt）有效。服务器侧若想支持直连免代理，可考虑后续把页面路径（/ /admin /admin.html）从令牌校验中放行（仅要求本机来源）——待用户拍板，未动生产。
- **表情包缩减逻辑 + 服务器安全加固（2026-08-16 深夜）**：
  - **表情包**：`tests/test_sticker_picker.py` 原在 Windows GBK 控制台打印 ✓ 崩溃，且旧测试没覆盖 enabled 逻辑——已修 stdout + 新增“50 项/启用35/停用15”回归与 `_migrate_enabled_defaults()` 迁移测试（107/0）；`test_sticker_upload.py` 14/0。
  - **模型锁**：`server_app.py` 在 import app_config 前设 `FIREFLY_SERVER=1`；`_load_config`/`save_config` 服务器版四模型强制 flash；`set_config` 服务器分支忽略 pro；proxy 模式无论是否注册配额钩子都走 `QuotaClient`（每次 create 强制 `model=deepseek-v4-flash`）。新增 `tests/test_server_model_lock.py` 12/0。
  - **配额展示**：管理台额度卡改为“今日对话额度（约）/每日上限”，另加“今日托管请求/请求上限”卡，并在用户列表注册时间下方加共享池剩余小进度条；当前 = 5000 次请求/天 ÷ 每轮约 4 次 ≈ **1250 轮对话/天**（`FIREFLY_PROXY_REQS_PER_CONV=4` 可调）。按用户要求**不做单账号防刷**（proxy_usage 保留 user_id 记账维度，需要时再启用）。
  - **审计修复**：会话 key 加入用户作用域（同 session_id 不再跨账号串上下文）；服务器版用户表情图片落到 `user_data/{uid}/stickers/` 且 `resolve_asset` 按用户目录解析；zip 导入拒绝 Windows 盘符绝对路径；reset-send 对未注册/发送失败统一返回 ok（防邮箱枚举）；服务器版不再把 env 兜底 Key 写入 config，`/config` 不再返回 key_prefix；`_read_body`/`_read_json` 类型校验；表情包 label/file、更新版本号/URL 等 innerHTML 全部 `escapeHtml`。**遗留风险（待办）**：服务器 `/set-config` 仍写全局共享配置（模型已被 flash 锁，但 proactive 等设置仍全局）；聊天文本进入提示词属角色扮演预期，无跨用户影响；以上服务器代码尚未部署到生产。
- **设定纠错助手 v1.2（2026-08-16 完成并装机）**：按用户反馈补齐/修复——① 顶部新增**状态点+状态文字**（空闲=状态正常/对齐中/已对齐/方案待确认/处理中/出错）；② 模式切换下新增**「最近聊天记录」可折叠面板**（复用 `/history?limit=20&mode=`，展示对应模式最近 20 条，带刷新；真机展开后聊天区仍不溢出）；③ **修复 AI 回复不刷新 bug**：`sendFixMessage` 等操作在 `_fixBusy=true` 时调用 `loadFixStatus()` 被其 `if(_fixBusy)return` 吞掉，改成 `loadFixStatus(force)` 并在消息/开始/应用/放弃/回滚/清空后强制刷新；④ **清空入口去重**：删除头部 ↺，只留修正记录右上「清空当前问题」；⑤ 提案区加「修改清单（尚未生效）/ 待确认」标题；⑥ 详细引导改为 **13 步“实际操作教程”**（不再只靠“下一步”阅读）：每步高亮目标 + 等待用户真的点击对应功能（点设置→点 Key 输入框→点开主动消息/模型/数据→点聊天页 ☰→点表情包页签→点映射表→点添加表单→点纠错入口→展开最近聊天→点输入框→点反馈），操作成功自动进入下一步，气泡有「没反应？下一步」兜底；目标在下半屏时气泡自动移到顶部避免遮挡；纠错步骤打开页面时不再自动弹键盘。基础引导同样改为 4 步实际操作。**补丁1**：从反馈页点教程按钮时，`_guideEnsureHome()` 必须先 `closeFeedback()` 再 `showHome()`，否则反馈面板 z58 仍盖在首页上（真机复现后修复）。**补丁2**：教程只变暗不拦截的问题——新增四块透明 `.guide-block`（top/bottom/left/right）在高亮目标四周盖住其余区域（pointer-events:auto），目标处开洞可点、其余按钮真的点不到；Playwright `elementFromPoint` + 真机误点验证通过。已装机验证布局；Playwright 模拟消息流确认 AI 回复立即出现。
- **表情包启用开关 + 默认集合（2026-08-15 完成，已装真机验证）**：用户通过 `tools/sticker_selector.py`（本地 127.0.0.1:8767 选择器，已用 vision 验证图片正常）选定默认集合并细化描述词，保存进 `app/assets/stickers/registry.json`（备份 registry.bak-20260815-192648.json）。当前 **50 项：默认启用 35 / 停用 15**。代码：`sticker_picker.enabled` + `_migrate_enabled_defaults()`（老安装自动合并 bundled 默认集合/描述词，且补齐新增条目）；组织器与聊天面板只取启用项（`GET /stickers?enabled=1`）；菜单→表情包管理页从列表改为**手机端卡片网格**（大图预览 + 启用中/已停用开关 + 分类 + 详细描述词 + 保存/删除，vision 验收无重叠/裁切；2026-08-15 深夜进一步改为手机单列、≥460px 两列），新增描述词上限 120 字。APK 已重建装机：`/stickers` all=50、enabled=35；停用项（嘴炮/车票等）不会进组织器清单。全量单测 18/18。
- **前端显示漂移修复（2026-08-15 深夜完成，已重装真机 + vision 明暗两主题验收）**：用户反馈菜单按钮漂移、表情包映射表按钮不适配、设置页右侧文字被隐藏。根因与修复：① `.settings-body button{width:100%}` 把「保存 Key」按钮撑满，导致旁边提示文字被挤到右边缘逐字竖排——改 `.set-save-row #key-save{width:auto;flex:0 0 auto}`；② 菜单操作排改为 4 等分网格 + 图标上/文字下（不再被图标挤出按钮）；③ 表情包映射表改为手机单列、≥460px 两列，卡片左缩略图+启用开关、右侧分类/描述/保存·删各占整行（**踩坑追加**：`.menu-content button{width:100%}` 会继承到卡片内保存/删按钮，导致删按钮 stretch 到整行红框、保存被压成 20px 文字溢出——必须给 `.stk-save/.stk-del` 显式 `width:auto`，已用 Playwright 计算样式确认 save=184.65px / del=48px 并装真机 vision 复核）；④ 接口地址下拉选项缩短为「DeepSeek 官方 / OpenCode Go」并把说明移到下方；⑤ `--panel-solid` 改完全不透明，杜绝下层聊天内容透出。已重装 704c2441 并用 vision 逐页验收（亮/暗主题菜单、映射表、设置高级区域均无溢出）。
- **设置页体验优化 P0+P1（2026-08-15 实施，待真机验收）**：用户拍板“主动消息全开但大幅降频 + 设置页分组重构 + 模型两档预设”。已做：① 默认值 `proactive_hard 4→6`、`soft 0.5→0.35`、`prob 0.3→0.10`（隐藏式保持开启，随概率整体降频）；② **修复概率式 30%×10s 轮询≈空闲 1 分钟内必触发**——`prob_gate_open` 增加 10 分钟静默窗（`_PROB_LAST_CHECK`）；③ 设置页改为五组卡片，默认只展开「账号与连接」，主动消息自然语言化（较少/偶尔/经常），模型区「快速/更强」两档 + 自定义折叠，隐藏式仅安卓壳显示，low 思考档从 UI 移除（后端兼容）；④ 非 Key 设置改动 400ms 防抖自动保存，Key 仍显式保存；⑤ 旧 config.json 当前值已按新默认更新。全量单测 18/18（proactive 82 项）通过、check_version/sync --check PASS。方案见 `docs/设计/设置页体验优化方案.md`。
- **设定纠错助手（2026-08-15 实施，2026-08-16 v1.2 已装机验证）**：按用户拍板方向**作废旧 👍/👎 反馈与 harness_rules 路线**，改为“用户描述问题 → AI 多轮对齐（聊天+选择题）→ 点「开始修改」→ AI 生成六文件修改清单（不生效）→ 用户点「应用」才原子写入，可回滚”。设计文档 `docs/设计/设定纠错助手设计.md` v1.2。代码：`app/modules/setting_fix.py`（对齐 Agent + 提案 Agent + 静态校验，核心三文件只 replace，用户设定/memory/手账可 append，haruno world/plot 只作只读上下文）、`setting_fix_store.py`（`{mode}/.setting_fix/` conversation/pending/manifest/audit/backups vN）、`routes.py` `/setting-fix/{status,message,start,apply,dismiss,rollback,reset}`；删除 `prompt_gate.py`/`prompt_registry.py`/`harness_rules.md`/`test_feedback.py`/`test_harness.py` 及 `/feedback` `/chat/reroll` `/prompt-*`；polisher/analyzer 已移除 `{harness_rules}` 注入。前端：首页 `#fix-module` 入口卡片 + **独立全屏页 `#fix-view`**（v1.2：状态点/最近聊天记录面板/去重清空入口/强制刷新）；反馈页只留 GitHub/QQ/邮箱；新增**纯代码引导**：基础 4 步（`firefly_guide_v1_done`）+ 结束时可点「深入了解」进入 8 步详细教程（设置五分组/主动消息/模型/数据/菜单页签/纠错助手/反馈，`firefly_deep_guide_v1_done`），反馈页可随时重开（v1.2 起点按钮必定重开，不再被完成标记挡住）。验证：全量单测 **18/18 通过**、check_version PASS、sync --check 一致、本地 HTTP 冒烟 `/setting-fix/status` 200、旧端点 404。**注意**：已随 v0.8.0 部署服务器并推送 Git；后续服务器改动单独维护（server/ 目录 gitignored）。
- **提示词时间线统一（2026-08-14 深夜完成）**：接续本日早些时候未收尾的「提示词动刀」工作——流萤状态从「重伤医疗舱恢复初期」推进为「**恢复得不错、能重新开机甲活动（依然以机甲为主，不能长时间脱离装甲待在外面）**」。三层已对齐：polisher/analyzer/proactive 模板、bundled 与 user_data 的 core/identity/sms_samples（user_data 副本已同步）、知识库 knowledge/story/firefly.md 当前状态。测试全绿（polisher 14/analyzer 16/proactive 81+68/retriever 12/orchestrator 17/organizer 13/llm_base 全过）；本地 git 提交 **5bda55d**（未 push）。提示词动刀方案与实现设计已于 2026-08-16 归档至 `封装（没指定就不读）/2026-08-16_归档/docs/`。
- **反馈模块定稿 + We-need 思维链（2026-08-15，提交 d965b2d 未 push）**：按用户真机反馈重构——反馈从聊天页移入「✉ 反馈页」独立模块（纯反馈收集供开发者/AI 参考）；列表**按对话轮数分组、最多 10 轮、默认收起（手风琴）、最新轮在最上、轮标签带模式名**、行可展开全文；👍/👎+标签理由按消息 seq 定位，后端幂等去重 + 反馈队列上限 500 条；“换一条”前端入口与“行为改进”区块已移除（后端端点保留，审批入口位置待定）。**思维链前缀**（A/B 实测后定稿）：polisher 两个模板最开头三行 `You are a helpful Firefly assistant.` + `Bootstrap with shell/read...tool catalog...` + `When you think, think in ENGLISH, start with "We need..."`（完整三行结构 + Firefly 身份 3/3 触发英文 We need；单换角色或单加身份句都失效，description 句是关键变量）；analyzer 开头只加 We-need 行（JSON 稳定）。8 轮完整流水线实测评测：用户判定**整体更好**。**遗留观察**：实测对话中“周六看电影”被回成“今天”——已澄清**非 bug**（测试当天 2026-08-15 恰为周六，模型按环境推断“周六=今天”属正确行为；真人当天也不会说“周六”）；“想看哪部”跨轮重复一次，待观察；服务器未部署新代码；本地 git 未 push。
- **harness 实施 P0–P2a（2026-08-14 深夜，提交 4bf07a5 未 push）**：方案 v2（批准制+安全三闸+召回式回滚+用户行为修正，提交 acfc0af）与实现设计文档已定稿。代码：① P0 槽位注入点（polisher/analyzer 模板 `{harness_rules}`，core 之后固定位，空文件零影响，proactive 复用）；② P1 反馈闭环（气泡 👍/👎/换一条 + 标签理由弹层；`POST /feedback` 落 feedback.jsonl 含上下文快照；`POST /chat/reroll` 复用 undo+全流水线重跑并落 preference.jsonl）；③ P2a 安全闸（prompt_gate 静态校验：元指令黑名单/引文出处/事实冲突/预算；prompt_registry pending→apply→rollback→dismiss 版本化；批准 API + 反馈页「✨ 流萤 · 行为改进」入口——入口定稿在反馈页，不放设置面板）。前端三副本已同步。测试：新增 test_feedback 12/12、test_harness 23/23，回归全绿。**待办**：P2b 优化器脚本（归因→候选→双盲 judge，依赖回归样本集）、P3 服务器管理端点/灰度、真机联调；4 个初定参数待用户拍板（批次触发/通过线/召回阈值/样本集）；设置面板减负另行讨论（未动）。
- **上会话清理任务核实（2026-08-14 深夜）**：上个会话报告的清理已确认完成——tools/ 恢复为原有 8 工具、docs/搜索资料结果/ 保留 15 份研究（含 流萤剧情精读研究_确认版.md），14 个垃圾文件与被否定的 2 份文档无残留；被删文件均为未跟踪文件，git 无 D 变更，无需补提交。

- **DSH 桌面壳**（已完成）：`F:\DeepSeek harness\dsh-gui\` —— DeepSeekHarness.exe 单文件壳 + 黑虎鲸图标 + update_dsh.ps1 一键更新 + 桌面快捷方式。壳子动态定位 harness、固定 `node lib/bin.js web` 启动，DSH 升级不用重打包。
- **手机远程访问 DSH**（已完成）：自研安卓壳 `F:\DeepSeek harness\dsh-mobile\`（内置 JSch SSH 隧道 + WebView，点开即连，已装真机验证）。链路：手机 App → SSH 隧道 → 云服务器 127.0.0.1:9090 → 电脑反向隧道（任务计划「DSH Reverse Tunnel」，脚本 dsh-gui/dsh_tunnel.ps1）→ 电脑 DSH 3080。手机密钥 dsh_mobile_rsa（RSA PEM，ed25519 与 JSch 不兼容踩过坑）。注意：官方禁用 `--host 0.0.0.0`（防远程代码执行暴露），勿绕过。
- **识图技能 vision**（已装为 DSH skill）：`C:\Users\FANGL\.agents\skills\vision\`（SKILL.md + vision.py）。命令行直调识图：`python "...\vision\vision.py" "<图片路径>" "<问题>"`，走 opencode go 套餐付费 **mimo-v2.5**（免费版 mimo-v2.5-free 次数少）。Key 内置脚本。这是本会话的"眼睛"（模型不支持图像输入时全靠它）。
- **个人预设 firefly（2026-08-15 创建）**：`C:\Users\FANGL\.dsh\.agent-presets\firefly\`，基于 standard 复制，显示名「流萤·标准模式」。改动：`agent.cordis.yml` 的 skill-filesystem 加了 `customSkillDirs` 指向预设自带 `skills/`，vision 技能随预设走（不依赖用户根）；persona 加了识图提示；`preset.yml` 描述已注明识图。**注意：cordis 预设（创造模式）的 tool-cordis 注册进程全局 Inspect Provider，同一 DSH 进程内 cordis 系预设互斥**——firefly 刻意不含 tool-cordis，与 cordis 共存；需要动态改自己时用 cordis 预设。
- **全局识图指令（2026-08-15 创建）**：`C:\Users\FANGL\.dsh\AGENTS.md` 是 DSH 用户全局指令文件，**自动注入所有预设、所有项目的所有会话**（优先级低于项目 CLAUDE.md）。内容：要求模型涉及图片任务时**主动直调 vision 技能**（`python "...\vision\vision.py" "<图>" "<问题>"`），无需用户提醒。这是「跨项目跨会话自然调用识图」的机制载体，已实测本会话立即注入生效。
- **手机端 DSH UI**（最终方案：只 viewport，不注入 CSS）：dsh-mobile 的 MainActivity 里只加 useWideViewPort/loadWithOverviewMode（解决文字重叠）。**重要教训：任何 CSS 注入（侧边栏折叠/图标统一/按钮省略/16px）都会在"交互时"破坏 DSH 前端布局（用户反馈"使用的时候乱"），已全部去掉。** 盲写 CSS 不可行——DSH 前端是 React+CSS Modules（类名哈希 `pI_x6G_sidebarCol`）+ 疑似 Shadow DOM，无法可靠读 DOM 精确适配；静态截图看着对，交互就崩。若要真正做移动端布局，需要 DSH 前端原生支持或彻底读清 DOM 结构，不要靠盲写 CSS 打补丁。CDP 读手机 WebView DOM 会 403（origin 限制），改用 JS console.log + WebChromeClient 桥到 logcat。
- **安全加固阶段一（已完成，2026-08-14）**：链路「手机→服务器→PC」评估发现手机私钥（APK 内置）原可直接 root 登服务器，已修复——详见 [docs/安全加固执行记录-2026-08-14.md](安全加固执行记录-2026-08-14.md)。要点：① 新建受限账号 `dsh-tunnel`（nologin，key 限制 no-pty/command=/bin/false），手机 USER 已改并重装；root 的 authorized_keys 已删 dsh-mobile，root 仅剩 PC 主 key（C:\Users\FANGL\.ssh\id_rsa）；② 8766 管理后台加令牌双因子（/opt/firefly/.admin_token，本地副本 _harden/admin_token.txt，admin.html 弹窗输入）；③ 明文 root 密码等危险文件移入 /root/harden-backup/；④ X11Forwarding 关闭。**阶段二待议**：9090 DSH 入口仍无鉴权（拿到 dsh-mobile 私钥仍可 -L 进 DSH 指挥开发机），方案 A 令牌代理 / B 手机改 HTTPS 直连；另建议阿里云账号开 MFA（云助手 aliyun-assist 以 root 运行）。运维材料：_harden/ 目录存私钥副本、token、加固脚本（幂等可重跑）。
- **用户日常手机维护（2026-08-14 完成）**：用户日常机 = **OPPO A92s**（PDKM00，Android 12 / ColorOS 12.1，天玑800，8GB，adb 序列号 `7HJ7VSE6WSZ959GA`，与开发测试机 704c2441 不同）。本次：只读体检 → 内存紧张（8GB 已用 5.5GB、zram 4GB 已用一半、微信 PSS 584MB+被换出 298MB 为最大户、开机 10 天碎片 795MB）→ 按用户要求卸载 45 项 OPPO/heytap 增值全家桶（`pm uninstall --user 0` 可恢复；主题商店+软件商店被 ColorOS 系统保护无法卸载/停用）→ 可用内存 1.97GB→3.17GB；用户自行手动重启收尾。**经验**：该机默认 USB 只暴露 MIDI 接口、adb 看不到设备，须先开开发者选项+USB 调试并授权；本机 SDK adb 在 `F:\Android_SDKset\platform-tools\adb.exe`（微信开发者工具自带 adb v39 会杀 v41 服务端，避免混用）；ColorOS 12 的 `pm disable-user` 对受保护应用无效（弹回 default）。

## 六、2026-08-14 服务器版 0.7.2 体检 + 修复（已部署）

- 体检结论：8 处版本号一致；服务器双服务 active；部署代码与仓库 md5 一致；15 个单测脚本全绿。
- **8 个 bug 已全部修复并部署生产**（备份在 /opt/backups/user_data_pre_quota_20260814-1117.tar.gz）：
  ① CORS Allow-Headers 补 X-API-Mode（8787 网关已实测含该头，托管模式 APP 内可用）；② 托管配额强制（proxy_usage 表 + 全站每日池 5000 次 ≈ 15w÷30 + 每用户每日 200 次，admin quota 字段=用户每日上限，admin 页显示今日用量）；③ 无 Key 立即 need_key（不再 4 分钟 relay 超时）；④ relay 错误分类（401/402/429→人话提示）；⑤ 表情包 registry 按用户隔离（旧全局池=只读公共贴纸，新上传只进自己目录）；⑥ 服务器禁用 /set-key、/update-download、/set-config 剥离 api_key；⑦ index.html 双份已同步（粒子场）；⑧ check_version.py GBK 修复。
- 服务器版 APK 已重建（android_server/firefly-server.apk，versionCode=2 未变）；**体积 0.6MB→8.9MB**：StarRailFont.ttf(12MB)+background.jpg 等资产随包打入（符合"资源本地化省流量"方向；下载页"0.6MB"文案待更新）。
- **2026-08-14 追加**：防刷（每用户每日上限）已按用户要求撤销并部署（只保留全站每日池 5000 次；proxy_usage 表保留 user_id 维度，将来恢复防刷不用改表；admin 台 quota 输入框/端点/函数已删）。
- **方向（用户已拍板的偏好）**：通用一套前端；一个安卓 APP 默认完全本地、可开「服务器后端处理」（内含「使用服务器托管额度池」按钮）；砍掉独立本地安卓 APP，三版本 → 两版本（安卓合并版 + Windows 本地版）；60MB 体积可接受；服务器数据与本地数据可同步（切换后重启加载数据）。**实施时机：AI 建议现在做、分阶段（A 数据同步三件套 → B 前端合一+壳改造+删 android_server → C 模式切换与全量回归 0.8.0），等用户最终拍板。**
- 待用户拍板：① 双模式整合开工（阶段 A 可立即做）；② 配额池参数 5000 是否合适；③ 发布：新 APK 是否上传 Gitee/服务器下载通道；下载页 0.6MB 文案；④ 本地版 APK/PC exe 重建时机；⑤ git 本地提交（工作区含此前多会话未提交改动，未代提交）。

## 七、2026-08-14 双模式整合 0.8.0（已实施并验收，等发布拍板）

- **已完成**：① 数据同步三件套（`POST /import-data` zip 覆盖导入+自动备份+zip slip 防御、`POST /sync/upload` 账号云端备份留 3 份、`GET /sync/download` 取回）；② 前端合一（`app/static` 唯一源 + `window.FIREFLY_MODE` 分支；登录/注册/重置表单**内联进首页**——不再跳 login.html；relay/登录模块/API 来源/Key/更新源/导出按模式开关；401 toast 仅对 /chat 提示）；③ 安卓壳合并（SharedPreferences `firefly_mode` 模式开关 + FireflyMode JS 桥（切换后 finishAffinity+killProcess 重启）+ Chaquopy 按需启动 + config.js 拦截注入 + onShowFileChooser 导入 + shouldOverrideUrlLoading file:// 导航 post 处理；KeepAliveService 按模式分流）；`android_server/` 已删除；`tools/sync_frontends.py` 自动同步（app/static → server/frontend + 安卓 assets，`--check` 校验）；④ 版本 0.8.0（7 处一致，versionCode 800）。
- **真机验收（704c2441，vision 识图）**：本地模式首页/设置（模式开关显示、API 来源隐藏、导入导出按钮、v0.8.0）✓；服务器模式首页登录卡片、设置（API 来源+账号备份/恢复按钮）✓；模式切换（select→confirm→壳重启→服务器模式）✓；**内联登录表单真机展开成功**（QQ 邮箱/密码/登录按钮）✓。**待用户手测**：注册/登录/重置提交（需真实 QQ 邮箱）、托管额度链路、导入文件选择器。
- **「崩溃」根因与修复**：用户观察到的「应用内页面内容消失只剩背景 + 请先登录后使用 toast 反复弹出」——服务器日志零错误，根因在客户端：file:// 页面跳转 login.html 被 allowFileAccess=false 拦截/导航竞态破坏渲染帧；401 toast 对后台轮询（10s 一次）无差别弹出刷屏。修复：登录表单内联（单页不跳转）+ toast 仅对 /chat 提示。**已装机验证无复现。**
- **追加修复（2026-08-14 下午）：聊天页「内容消失」的第二根因**——用户报告剧情模式/春日手信进入后同样症状。根因：合并引入的粒子场 `<canvas id="firefly-field">` 在 index.html/style.css 里**没有任何定位规则**（login.html 有 `position:fixed;inset:0` 而 index.html 漏了），canvas 被 JS 撑成全屏后作为**流内元素**占满一屏高度，把流内定位的聊天容器 `#app`（position:relative）推出视口——首页 `#home-view` 是 fixed 不受影响，所以只有聊天页「只剩背景」。修复：style.css 给 `#firefly-field` 加 `position:fixed;inset:0;z-index:0;pointer-events:none`。已重建 APK 装机验证：剧情模式（历史消息+输入框完整渲染）与春日手信（旁白+开场+输入框）均正常；已部署服务器。
- **部署状态**：服务器已再部署（备份 user_data_pre_080_20260814-1430.tar.gz，firefly-server active，新前端 grep 确认在位）；PC exe 已构建（dist/firefly/firefly.exe 3.4MB）；合并版 APK 已构建装机（app-release.apk，versionCode 800）；单测 15/15、sync --check 一致、check_version PASS。
- **待用户拍板**：① 真机手测登录/注册（内联表单）；② 服务器全局 `/set-config` 的 per-user 化（当前模型已锁 flash，proactive 等仍全局）；③ 未跟踪的研究草稿文件（bwiki_* / moegirl / gitbook_content 等）是否保留或清理。

## 八、2026-08-16 发布 v0.8.0（GitHub + Gitee 已上线，服务器已部署）

- 版本 0.8.0 / versionCode 805；`check_version.py`、`sync_frontends.py --check`、全部单元测试通过。
- 资产：GitHub release v0.8.0（firefly.apk 60.4MB + firefly-setup.exe 24.0MB）；Gitee release v0.8.0（release id 799961，附件 id 3050357 / 3050358）。
- 下载小技巧沿用：下载页主按钮 `resolve` Gitee foruda 直链（前端 fetch blob + `a.download` 强制 .apk，规避 Gitee 把 APK 按 zip MIME 下发）；服务器 `/opt/firefly-downloads` 的大文件已移入 `local_bak_20260816`，`/download/firefly.apk` 与 `firefly-setup.exe` 现在 302 到 Gitee foruda，服务器只跑 resolve/zip 处理脚本；`FALLBACK_TAG=v0.8.0`。
- 服务器已部署：`app/`、`knowledge/`、`server/` 全部更新，`firefly-server` 与 `firefly-downloads` active；公网 `version.json` tag=0.8.0；`/download/resolve?file=firefly.apk` 实测返回 foruda.gitee.com 直链。
- git：master 已推 GitHub（origin，SSH 推送）与 Gitee；tag v0.8.0 双端已推。GitHub 提交 8aaa95d。
- 提交审查：恢复被外部内容覆盖的 `readme.md` 与被误删的 `CLAUDE.md`；未提交任何 user_data/配置/备份/研究草稿（`.eval`、`.tmp_test`、bwiki_*、gitbook_content.json、registry.bak 等仍为未跟踪文件）。
- **部署约定已定稿**：`docs/部署与发布约定.md`——提交≠发版（无明确指示不 bump 用户可见版本）；下载 Gitee 优先、服务器兜底；部署后必须汇报 resolve/更新优先级/数据安全/推送结果四关键点。
- **项目整理**：过时计划（0.8.0 检查点、提示词动刀方案/实现设计、三版本缺陷分析执行文档）与无研究草稿已归档到 `封装（没指定就不读）/2026-08-16_归档/`（已 ignore，不进 git）；`docs/协作记忆.md` 引用已更新。

## 八、2026-08-14 发布 v0.7.2（GitHub + Gitee 已上线）

- **版本回归 0.7.2**（双模式整合沿用此版本；versionCode 801 保覆盖升级；check_version 映射规则改为递增校验）。
- **git 历史清洗**（用户明确要求"别把服务器后端代码/本地隐私交过去"）：发现本地与远端历史均含 server/（旧误 push）；已用 git-filter-repo 删除 server/ 与 _trash/ 全部历史痕迹（77 commits 重写），v0.6.0 tag 经 commit-map 重建到清洗版提交，两端 master + 全部 tags 强制更新；验证 GitHub/Gitee 远端树均无 server/。**备份**：`F:\CodeFile\firefly_backup_pre_filter_20260814.bundle`（清洗前全量）。
- **发布**：GitHub release v0.7.2（assets: firefly.apk 60MB + firefly-setup.exe 23.9MB）；Gitee release v0.7.2（id 796794，同名附件）；两端 tag v0.7.2。
- **下载路由**：download_server.py FALLBACK_TAG → v0.7.2、白名单删除 firefly-server.apk（实测 /download/firefly.apk 302 → Gitee v0.7.2 资产、server.apk 404）；version.json（tag 0.7.2、apk 链接 firefly.apk）已部署；双服务 active。
- **本机工具**：Inno Setup 7.1.0 已装（C:\Program Files\Inno Setup 7，经 127.0.0.1:7897 代理下载 GitHub 资产）；downloads/innosetup-*.exe 安装器留档。
- **装机验证**：手机 com.firefly.android = 0.7.2/801，登录态保持（1280728905@qq.com），首页正常。
- 提醒：GitHub/Gitee 历史中的 server/ blob 即使重写历史，GitHub 端旧对象可能延迟 GC（可联系 GitHub 支持彻底清除）；服务端源码无明文凭据（SMTP 走环境变量），逻辑泄露风险有限但已知。
- **2026-08-14 追加发布（ffac186）**：① 按用户要求**移除聊天气泡主题功能**（菜单入口 + 6 套主题 + 选择网格样式全删，消息气泡默认样式保留）；② **提示词防编造/防重复补强** 5 处：memory_manager 整理铁律（只记真实发生的事、不记推断）、polisher story/haruno「引用记忆不越界」（找不到原文不引用）、proactive「引用前先定位」。APK versionCode 803、PC exe+安装器重打、服务器已部署新提示词、两端 release 资产已全部更新。

## 四、本次会话用户明确确认的约定

1. 建立「协作记忆」机制（本文件 + CLAUDE.md 索引 + 会话结束前主动更新）。
2. 模型分工：默认 flash 执行、规划/系统级错误切 pro（AI 提示、用户切换、同会话继续）。
3. 三版本 APP 缺陷分析：以小白视角写执行文档，设计缺陷/工程缺陷/取舍代价三类区分（已完成）。
4. 执行文档中能独立完成的改动已实施（7 项 ✅；文档已归档至 `封装（没指定就不读）/2026-08-16_归档/docs/`）。

## 九、2026-09-15 角色卡完善二期（计划见 docs/设计/角色卡完善二期/）

按用户四条图批执行完毕：① 设置页自绘下拉 `ui_select.js`（安卓 WebView 原生 select 弹窗 CSS 管不到，必须自绘）+ 输入框深色 + 默认模型名改官方名 **deepseek-flash**（官方 /models 实测只有 deepseek-flash / deepseek-v4-pro；`core/config.py` 有 `_MODEL_RENAME` 幂等迁移）；② 首页「指出问题」卡片已删（基础教程 4→3 步、深度教程删纠错三步，纠错本体留 pc_nav 侧栏）；③ 知识库/记忆库规范化：story/knowledge 五域结构（world/factions/story/character(含 arc)/dialogues）+ 范围行注入 25 文件 + index.md 脚本重建（`tools/check_knowledge.py` 门禁）+ `memory/default.md` 出厂记忆（`load_head` 回落链：运行时 memory.md → 包内 default.md）+ haruno 补知识库与出厂记忆 + 自建包模板带骨架 + `has_knowledge` 空骨架不烧检索；④ AI 搭建向导下架（端点摘除、pack_forge.py 归档 _trash/2026-09-15），改为逐文件 ✨ AI 辅助（`modules/pack_assist.py` 规则表 + `api/pack_paths.py` 读写口 + 抽屉 UI，`/pack-assist` 只产提案、`/pack-assist/apply` 校验+备份+原子写）；⑤ 用户形象入包：称呼写包级配置（`/pack-config` 增 user_name），用户头像走包资产槽位 `user_avatar`（回落：包内→内置穹/星）。新增端点：pack-knowledge×5 + pack-memory×2 + pack-assist×2（路由 62 POST + 31 GET）。**踩坑**：`_safe_kb_rel` 文件名正则误杀含句点/括号的合法名（2.0任务对话.md）——白名单只禁危险形态，别用窄字符集；测试里 CONFIG_FILE 要打在 `_paths.CONFIG_FILE`（core/paths），打错模块会读到真实 user_data。验收：全量 59 测试绿、五门禁（+check_knowledge）过、PyInstaller/Gradle 重建、真机复验。全部改动未提交 git。

## 十、2026-09-15 角色卡管理树补漏与嵌入块修复（文件交叉 used_by）

用户提出"核心流程各阶段文件有交叉如何在前端反应"+ 危险区空白。审计两内置模式全量文件 vs `PACK_STRUCTURE` 后修：① **嵌入块销毁 bug**（危险区空白根因，且用户形象/主动消息/表情包同病）：`pack_tree.js _render` 每次 `innerHTML=""`，被 `_moveInto` 搬进树的既有区块随清空销毁、事件接线全死——修法「车位」：清空前 `_parkEmbeds()` 把四块（`#pv-identity`/`.pv-proactive`/`.pv-stickers`/`#pv-danger`）泊入 display:none 容器，渲染后再搬回（`_EMBED_GETTERS` 登记表）。② **file 链路 bug**：`_loadFileContent` 硬编码 `/pack-memory`，opening.json 读写全落在出厂记忆上；「恢复默认」向 `/pack-memory/update` 发空串被后端拒，永远失败——新增 `/pack-file` GET + `/pack-file/update` + `/pack-file/delete`（白名单 = `PACK_STRUCTURE` 里 type==file 的 path，规范驱动；json 后缀校验 JSON 合法；写入走 `api.pack_paths.write_pack_text` 带备份），oracle 同步 64 POST/33 GET。③ **补漏**：story 私有 `_时态标记规范.md` 入人设域（type=file）；haruno 缺 `用户设定.md` 补空文件（story 同款空槽位），自建包模板 `pack_lifecycle.create_pack` 同步补写；preset.json 属注册信息不入树（称呼经 /pack-config user_name 已有安全写口）；运行时记忆/手账属用户数据层（/user-memory、/journal 有专属面板）不重复入树。④ **used_by 文件交叉**：`PACK_STRUCTURE` 每个 slot/file 声明 `used_by`（core/identity/用户设定 = 分析器+回复器共用）与 `feeds`（知识库/出厂记忆 → 分析器），`build_tree` 注入 `assistable`（非白名单文件隐藏 ✨）；前端多阶段读取挂「共用」金标、flow 挂「→ 分析器」、分析器域加 `type=ref` 只读跳转行「人设输入 → 见 人设与口吻」（点击展开目标域，不放重复编辑器）；包未附带文件（story 无 opening.json / haruno 无时态规范）灰态「本包未附带」不提供编辑入口。验收：沙箱 Playwright 14 项（二次渲染嵌入块存活/灰态不可点/ref 跳转/haruno opening.json 读出 JSON 非记忆）、59 测试绿、五门禁、真机 704c2441 截图（危险区恢复链接+归档按钮、→ 分析器标签、灰态行）。全部改动未提交 git。

## 十一、2026-09-15 全量审计第一批修复（A1/A2 + B 簇 + C1/C3，未部署）

HY4 全量审计（`docs/审计-全量代码-2026-09-15.md`，40 项）经主会话抽验 27 项（事实层 A、定性层 B）后开修第一批，报告见 `docs/审计修复执行记录-2026-09-15.md`。核心修复：① `/shutdown` 与全部写端点加浏览器来源校验（`shared_http.local_origin_ok`：Origin/Sec-Fetch-Site/Referer 三标记，全缺=非浏览器放行；本地版 CORS 改本机同源白名单回显——A1+A2 收口）；② 原子写五件套（config.json/会话撤回/手账×2/memory.md 返回值检查+`.memory_index`）+ 损坏 config 备份留证 + preset.json 非对象守卫（B1-B5/B8）；③ C1 `_tm` 作用域 + C3 test_analyzer 补退出码。**关键决策**：/shutdown 保持 GET（改 POST 会把新实例对 ≤0.8.1 旧实例的优雅交接打成强杀丢数据）；Origin 白名单不含 null（安卓本地模式实证同源 http://127.0.0.1:8765）；`FIREFLY_SERVER` 分叉保服务器版行为零变化（服务器版本无 /shutdown 端点）。**门禁**：59 测试全绿 + check_version/sync_frontends/build_frontend_bundle 三查一致 + `local_origin_ok` 11 例攻击矩阵冒烟全过。**一处测试调整（透明记录）**：test_sticker_domain E5 放行清单补「审计」前缀——既有失败，触发源是审计报告自身引用旧工具名，与本批代码无关。改动 9 文件，备份在 QoderWork 工作区 backup_20260915_fixbatch1/。**遗留**：A4 更新通道（GitHub/Gitee 固定直链+sha256，零证书可修）> 08-25 修复与本批的部署债 > B6/B7 > C2/C4（旁白 Think High 需拍板尊配置还是删配置）> C5-C7/C12/C14 > C8/C9（pack_registry fp 改 user_ctx 一行堵雷）。全部改动未提交 git、未部署、未动版本号。

## 十二、2026-09-15 审计第二批修复（A4 + A5/A6 + B6/B7 + B9 + C8，未部署）

报告见 `docs/审计修复执行记录-2026-09-15-第二批.md`，备份 backup_20260915_fixbatch2/（9 文件）。核心修复：① A4 下载页高速通道加客户端直链信任校验（`trustedDl`：https+Gitee/GitHub 域白名单）——**定性修正**：审计所说"客户端直接下载安装 version.json 的 exe/apk"与实际不符（本地链路 `_validate_download_url` 早有防护、服务器版只读 tag 不消费 exe/apk），真实暴露面=下载页 resolve 响应可被改写；sha256 无独立信任根不做（避免死字段）；② A5/A6 前端 XSS 五处转义（settings.js 供应商两字段 + debug.js 四字段，bundle 重建+三端同步）；③ B6 阶段 1 换入前先备份（**隐藏坑**：不能直接 `mode_root(pid)`——未注册 pid 静默回退默认包，须显式 root=pack_dir）；④ B7 `recover_swap_leftovers()` 挂启动链（X.restore_old 存在且 X 不存在→回滚；其余留置绝不自动删）；⑤ B9 返回 None+rest() 跳过（**定性修正**：审计建议"返回 0"是错的，0 同样全量切片）；⑥ C8 pack_registry fp/cache 键统一 `_user_ctx_dir() or USER_DIR`（冒烟实证用户 A 注册的包不串入 B/全局）。门禁 59 测试全绿+三查一致+冒烟 19 例全过。**部署债提醒**：08-25+第一批+第二批+当天两会话改动全部未提交未部署，含 server/（download_page.py/version.json）与 APK 重建面。C4 仍等用户拍板。全部未提交 git、未部署、未动版本号。

## 十三、2026-09-18 语音插件真机验收 + 记忆机制 P0/P1/P2 + 菜单 chip 与清除历史范围

用户真机测试提了三条，逐条查证后实施（**全程主会话直接做、零子代理**）。

**① 菜单页「标签 chip 容器」问题**（用户原话："收藏、表情包、设定文件、请求记录、流程日志这几个各自的容器有问题"）。
真机截图逐像素量出来的事实：`.menu-tabs` 是 `repeat(5, minmax(0,1fr))` 固定 5 列，360 CSS px 屏上每个 chip 恒 61px；
而 4 字标签（设定文件/请求记录/流程日志）实需 图标13+gap5+4×11.84 ≈ 69px → **内容宽 = 容器宽（184 device，左 0 右 0）
= 硬裁**：放大截图里 `#i-list` 图标被切掉一半、`志` 字右半边没了。
另有一个 CSS 常识坑：`text-overflow:ellipsis` 在 **flex 容器**上对匿名文本项无效，只会硬切 → 文字另包 `<span>` 兜底。
改法：`.menu-tabs` 改 flex + `.menu-group-title{flex:0 0 100%}`（强制换行）+ `.menu-tab{flex:1 1 0}` →
组内 3 个各 1/3、2 个各 1/2，既放得下也不留空格（第二组原来右边空 3 格）。桌面断点同步改 `flex:0 1 130px`。
真机复验：row1 三 chip 各 319 device、row2 两 chip 各 488，左右留白 66-150，**零裁切**。

**②「清除历史」不清设定文件**。用户点明"现在设定文件属于用户对话产生的私人数据，情况变了"。
实测取证：`story` 的 memory.md 856 字、手账 422 字都是聊天产物，而 `用户设定.md` **实测 0 字**
（核心设定确实已归位到角色卡 `memory/default.md` 出厂记忆）→ 老代码"不清"的理由已失效。
改法：清 `memory.md` + `手账.md` + `.memory_index` + `data/archive/`，**出厂记忆不动**；
只清一半的副作用（不只是脏数据）：游标归零但旧头部还在 → 下次休息把"从第 0 轮重新切片的新对话"并进一段本该消失的记忆。
`.memory_index` **连文件一起删**而非写 0 —— `get_wake_status` 用「index 在 + memory.md 不在」判"上次休息被中断"，
只写 0 会让下次进聊天误报；实测清完 `wake-status = {interrupted: False, has_memory: False}` ✓。
「用户设定」编辑器从设定文件页移出（角色卡管理 → 人设与口吻 本来就有，`PACK_STRUCTURE` persona slot）。
真机端到端验证（在**空的 haruno** 上种探针再清，不动 story 的真实数据）：清前 24/12 字 → 清后 0/0，出厂记忆 370 字不变。

**③ 历史上下文长度与记忆机制**。查清现状：回灌 40 轮、分析器 20 / 回复器 15 / 组织器 5 / 检索器锚点 10，
且 `ContextManager(token_capacity=300000)` **只存不读（死字段）**——上下文只有轮数约束、没有 token 约束。
当时的方案（项目归档文档原话）：memory.md 的真实职能是「窗口外旧对话的压缩存档」，唯一补偿是**用户手动点「让流萤休息」**。
**顺带挖出真 bug**：`chat_ops.rest` 喂的是 `session["context"].get_full()`（被 hydrate 40 轮截断）与 `.turn_count`，
而游标是**磁盘全量**轮号 → 长对话重启后游标写 40、`verify_index` 看 40<=100 判正常不修 → 切片为空 →
**每次点休息都"没有新内容"，记忆永不更新且零报错**。已记入 `docs/错误总结.md` **#10**。
P0：rest 改走 `load_all(mode)` + `count_user_turns(mode)`（盘上口径）；`get_recent(n_turns, max_tokens)` 接上预算（整轮裁剪、至少留 3 轮）。
P1：新增 `modules/auto_rest.py`（**睡眠期计算**，默认 10 轮触发、后台线程、失败只记日志、不新增每轮调用）；
`_compose_memory_text` 去重 + 分节标题只写一次 + 已完成条目上限 40。
P2：新增 `modules/memory_archive.py`（按月只增不改的历史归档，同 Zep 的 episode+fact 轻量合体），检索器把它当知识源。
**成本知情**：自动整理每次触发 2 次 LLM 调用（记忆 + 手账，`cfg.MODEL`），由 `config.json` 的 `auto_rest_turns` 控制（0=关）。
参考案例（联网读过原文）：Generative Agents（recency/importance/relevance 打分 + 重要性阈值触发 reflection）、
Letta/MemGPT（core/recall/archival 三层 + sleep-time compute）、Zep/Graphiti（双时间轴 + 有效性区间）、
Anthropic/Claude Code（自动 compaction）、SillyTavern World Info（关键词注入 + token 预算）。
新增回归 `tests/test_memory_auto.py`（36 项，沙箱 USER_DIR）；**全量 60/60 绿**；四门禁 PASS；APK 重建 + 真机 704c2441 复验。
**遗留**：`auto_rest_turns` 无 UI 开关（P2-10）、服务器版无全局策略（P2-11）。全部改动未提交 git、未推送、未动版本号。

## 十四、2026-09-18 记忆机制二次设计（活跃窗口 30–100 + 原文存档 + 极简提醒）

用户否掉了上一轮的形态，给出新口径（**这是现在的唯一权威口径**）：

> 历史记录自动整理的限制可以较大放宽，避免频繁消耗用户 token。轮次或上下文余量快耗尽时
> 再在聊天页顶部显示状态/提醒。**活跃窗口需要随时保持**（除刚开始外应保持 30–100 轮，
> 用户点「休息」压缩后也仍加载近 30 轮完整对话进分析器和回复器）。**整理后的历史记录只保留
> 近 20–30 轮**（仍旧加载进分析器和回复器），多出来的都在**检索器阶段**加载。
> 休息时整理的部分记忆在菜单页设定文件副页下，那么**整理后的记忆原文也应该放在设定文件下**
> （保存完整的历史对话记录，允许用户调用 AI 压缩）。这部分是用户对话内容产生的记忆，
> 属于**用户的私有数据，放在角色包（不进入角色卡）**，使用时加入检索器。
> 核心流程为：**检索-分析-回复-调用/环境**。

**据此落地（改掉了上一轮的 P2 形态）**：

| 层 | 内容 | 谁读 | 更新 |
|----|------|------|------|
| L0 出厂记忆 `{pack}/memory/default.md` | 角色开局记忆 | 分析器 + 回复器 | 角色卡管理手改（**不动**） |
| L1 用户记忆 `{mode}/data/memory.md` | **压缩摘要** | 回复器（memory_head） | 整理时自动 / 用户点「AI 压缩」 |
| L2 历史对话存档 `{mode}/data/archive/YYYY-MM.md` | **完整对话原文**（按月分片、只增不改） | **只进检索器** | 整理时把滚出窗口的原文追加 |
| L3 活跃窗口（conversation.jsonl 里"自上次整理以来"的部分） | 对话原文 | 分析器 + 回复器 | 每轮；整理后**保留最近 30 轮** |
| L4 手账 | 第一人称记录 | 回复器 system | 整理时 |

- **阈值**：`memory_window_turns=100` 触发 / `memory_keep_turns=30` 保底 / `memory_warn_turns=80` 提醒。
  三项已接入 `/set-config`（含 0..1000 钳制；`window=0` = 关自动整理只留手动），**Settings 面板滑条仍缺（P2-10）**。
- **rest 语义改了**：不再"把未整理的**全部**压缩一遍"，而是切 `(last_integrated, 总轮数−keep]`——
  搬走的那段**原文进 L2**、同一段文本交给 LLM 压进 L1、游标推进到 `总轮数−keep`。
  所以"用户点了休息，分析器/回复器照样看得到最近 30 轮完整对话"。
- **`ContextManager` 的 token 预算改成纯兜底**（8000 → 30000）：这是**缓存命中率的红线**。
  分析器/回复器把「## 最近对话」放在 user 消息最前面，DeepSeek 按前缀打折；
  窗口 30→100 的**增长期**里历史块只往后追加 → 老部分是稳定前缀、每轮只有新一轮没命中。
  **一旦从窗口头部裁剪，窗口就从"增长"变"滑动"，历史块缓存永远命中不了**——等于把省的 token 又赔回去。
- **提醒形态被用户否了一版**：第一版做成整行横幅（边框 + 按钮），用户反馈"太影响观感"。
  现改为**名字行尾一小截灰字** `· 记忆 92/100`（`#chat-mem`），不加行/不加边框/不加按钮——
  手动入口本来就在菜单「让流萤休息」，到上限后下一条回复也会自动整理，不需要按钮。
- **顺手修掉当天自己埋的形状 bug**：把 `rest` 入参从 `ctx.get_full()` 换成 `load_all()` 时口径对了、
  **形状不对**（jsonl 行是 `who`，rest 读 `role`）→ 切片为空 → 又变成"无新对话，跳过"。
  新增 `load_all_context()`（逐条 1:1、不合并连发，与 `count_user_turns` 同源）；
  **当时的测试只断言 `success` 没断言产物**，所以 60 个脚本全绿也没抓住——已记入 `docs/错误总结.md` #10 尾部。
- 新增端点 3 个（**67 POST / 38 GET**）：`GET /memory-status`、`GET /archive`、`POST /memory-action`
  （白名单式 `save_archive` / `compress` 派发，与 `/voice/plugin` 同哲学，尽量少开口子）。
- 设定文件副页新增「历史对话存档 · 整理后的原文」：月份自绘下拉 + 原文 textarea（可编辑保存，
  改完检索器看到的就是改后的）+「AI 压缩」（压缩进用户记忆，原文保留可反复压）。
- 验收：`tests/test_memory_auto.py` 扩到 **72 项**（新增 B4-B6 形状断言 / E 窗口语义 / F compress /
  J 端点层）；**全量 60/60 绿**；四门禁 PASS；APK 重建装机 + 真机 704c2441 实测
  （状态提示字形、整理真实跑通：归档 `第 1–3 轮`原文 599 字、游标推进、手账同步更新、设定文件页可读可改）。
- **测试踩坑（已修）**：`compress` 的"无 Key"分支用 `patch core.config.get_client` 打桩**无效**——
  `app_config` 里 `from core.config import get_client` 是**名字拷贝**；结果那次真拿着 config 里的 Key
  发了请求（沙箱数据，只有 API 调用出网）。改为覆盖 `modules.app_config.__dict__["get_client"]`。

## 十五、2026-09-18 攻击哨兵「一次封禁弹 4-5 次」修复（用户报障）

**用户报障**：「服务器频繁提示我遭到攻击，是真机测试导致的吗？」
**结论：不是。** 真机测试全程只碰手机**本机**的 `127.0.0.1:8765` 与 App 自己的登录/同步 HTTPS，
一次都没碰 SSH；而告警的触发字段是 `fail2ban_bans`（**从公网进来的** SSH 密码尝试），方向也反。

**真实证据**（只读诊断，服务器未改任何东西）：
- `2026-09-18 22:02:03~22:02:05` fail2ban `Found 47.104.232.1` ×8 → `22:02:05 Ban 47.104.232.1`
- 日报里 `22:03/04/06/07` 四条，全是**同一个** IP；8-28 那次同样 `16:01-16:05` 连续 5 条同一 IP
- 其余计数全 0（主服务 429=0、网关 429=0、近 30 分钟 SSH 失败=0、服务全 active）

**根因（两头叠加，一次封禁被放大成 4-5 条弹窗）**：
1. `server/attack_watch.sh` 统计「**最近 5 分钟** fail2ban 日志里的 `Ban` 行数」且阈值 `>=1`
   → 一次封禁在 5 分钟窗口里**每分钟都命中** → cron 每分钟重写标记文件，`time` 每次都变；
2. `tools/attack_sentinel.py` 的去重键正是 `flag["time"]` → **去重完全失效**，
   每分钟当成新事件：弹窗 + 调模型 + 写日报各来一遍。

**修的四件**：
- **服务器侧**（`server/attack_watch.sh`，已单文件部署 + 备份，`bash -n` 通过）：
  改「新封禁」语义——状态文件 `/opt/firefly/.attack_seen` 记住上次已上报的 Ban 时间戳，
  只统计更新的；标记里的 `time` 用**封禁时间本身**（同一封禁重复执行也不变 → 上游去重有效）；
  标记保留 `GRACE=10` 分钟再撤（≥2× 哨兵 60s 轮询，保证一定被读到）；顺带把被封 IP 写进 `banned`。
- **本地侧**（`tools/attack_sentinel.py`）：去重键换**内容签名**（封禁 IP + 各计数，与 time 无关）
  并加 30 分钟冷却窗口（双保险）；**单次 SSH 封禁按噪声档**处理 → 只写日报、不弹窗、也不调模型
  （顺带省掉一次 API 调用）；模型输出为空/异常时走**本地兜底**，`need_user` 不再恒为 True。
- **回归测试**：新增 `tests/test_attack_sentinel.py`（22 项，纯本地不连服务器），
  把「同封禁不同时间戳必须同签名」「噪声档边界」「空分析兜底」钉死。
- **生效**：本地常驻哨兵已重启（旧 PID 22044 → 新 23268；`--once` 自检 1.4s 静默退出）。

**教训（值得记）**：告警的**去重键选了每次都变的字段**（时间戳）＝ 没有去重；
「检测到什么」与「报几次」是两件事，阈值宽不代表不会刷屏。

## 十六、2026-09-18 聊天 UI 对齐官方短信页 + 角色卡化补债

用户拿**官方游戏内发短信页面**的截图来对比，指出「**气泡和字体大小和位置**」不对，随后追加
「第二行头像又下去了」「为什么没像官方一样把名字加到消息上」「三角标可以去了」「发表情包是什么样
的」，最后一条关键提醒：**「既然是角色卡化了那就应该是通用的样式（别瞎搞）」**。

### 一、量出来的事实（官方截图 591 宽，一律按**占视口宽比例**折算到 360px 屏）

| 项 | 官方 | 改前 | 改后 |
|----|------|------|------|
| 头像直径 | 11.8%（43px） | 8.5%（30.6px） | 10.5%（37.8px） |
| 头像左边距 | 8.8%（32px） | 4.0%（14.4px） | 7.0%（25.2px） |
| 头像↔气泡 | 3.6%（13px） | 2.0%（7.2px） | 3.0%（10.8px） |
| 气泡左缘 | 24.2%（87px） | 14.5%（52px） | 20.5%（74px） |
| **单行气泡高** | 26.8–28.6px | **42.6px** | 28.1px |
| 字形高 | 11.0–11.6px | 13.1px | 12.1px |
| 左右内边距 | 12.8px | 12.96px（本来就对 ✅） | 12.8px |
| 名字行 | 有（字形高≈气泡文字 0.79 倍；左缘与气泡左缘差 **0px**） | 无 | 有 |
| 头像垂直 | **顶对齐名字行** | 底对齐（多行气泡时下坠） | 顶对齐 |
| 气泡圆角 | 左上 ≈2px@591（近直角）／左下 ≈15／右侧 ≈11 | 4/16/16/16 | **3/10/10/10** |
| 气泡尾巴 | **无**（左缘从顶到底是一条直线） | 有 rotate(45deg) 三角 | **已删** |

**关键诊断**：气泡"胖"的根因**不是左右内边距**（本来就与官方一致），而是
**上下内边距 10px + 行高 1.6** 一起把它撑高了 50%。
**一个反直觉的实测**：我原以为"左下角也该是直角"，量完官方发现**只有左上角近直角，底角是圆的**（甚至比右侧更圆）——所以圆角方向没改错，只调了量级。

### 二、角色卡化补债（用户点名"别瞎搞"）

我第一版把用户称呼回落写成了字面量 `"开拓者"` —— 这是**框架里的角色名字面量**，自建卡会显示别人的名字。
改正：
- 新增 `views.js` 统一出口 **`charName()` / `userName()`**：只从当前角色卡取，**取不到返回空串**
  （调用方自行降级，名字行直接不渲染），**不回落到任何具体角色名**。
- 清掉 8 处角色名字面量：`chat.js`（转语音 toast）/ `fix.js`（纠错页 who）/ `panels/data.js`（收藏 who）/
  `panels/debug.js`（user_name 回落）/ `proactive.js`（安卓通知标题）/ `main.js`（起床遮罩）/
  `views.js`（离线兜底预设的 char_name）/ `chat_render.js`（引用卡片 who，原来写死「流萤」）。
- **后端**同类两处：`core/presets.char_name()` 的兜底 `or "流萤"` → `or p.get("name") or "角色"`；
  `routes_common._notify_reply_if_background` 的通知标题写死「流萤 · AI」→ 改成按 mode 取角色名
  （顺带给它补了缺失的 `mode` 参数——原来根本没传，取角色名会 NameError 被 except 吞掉）。
- **HTML 动态文案**走 `data-tpl` 模板（`{c}`/`{u}`），由 `applyTextTemplates()` 在换卡时重填：
  菜单「休息」按钮、手账标签与说明、用户记忆说明、存档占位符、用户形象弹窗标题；
  休息/起床遮罩默认文案也去掉了角色名。HTML 保留**中性默认文案**（无 JS 时不露角色名）。
- 新增守卫 `tests/test_frontend_generic_names.py`（17 项）：**框架里出现角色名字面量即 FAIL**。
  仅保留两处**有正当理由**的白名单，并写明理由：
  `settings.js` 的「确定退出流萤吗？」＝**应用名**（退出的是 App 不是角色）；
  `panels.js` 的 `开拓者_穹.png/开拓者_星.png`＝**随包资产文件名**（改名会断引用）。

### 三、其它
- 新增 `tests/test_chat_ui_geometry.py`（24 项）：钉同源不变量（语音条缩进 ≡ 头像宽+间距；
  气泡与引用容器 max-width 相同）、官方验收线（单行气泡高/字形高/内边距/气泡左缘区间）、
  圆角方向、**无尾巴**、以及"多行气泡头像不下坠"（`align-items: flex-start`）。
  踩坑记录：CSS 注释里写了「气泡被 max-width:100% 覆盖」，不剥注释就解析会先命中注释里的 100%。
- `_mkCol` 外壳化后，原来 `row.replaceChild(span, img)` 的降级路径会抛 NotFoundError
  → 加 `_replaceNode()` 受控替换。
- 验收：真机 704c2441 截图（名字行/顶对齐头像/无尾巴气泡/菜单显示「让流萤休息」）；
  **全量 63/63 测试绿**；四门禁 PASS。

### 四、语音条贴紧自己的消息（同日追加，用户真机反馈）

用户：「**语音应该再贴紧点（现在语音的前端样式和文本分太远了，消息之间反而太近了）**」。

实测（写了个逐行内容块分割的量尺 `.tmp_test/measure_chat_spacing.py`）证实了这条感受，
而且**两者正好反了**：

| | 改前 | 改后 |
|---|---|---|
| 气泡 ↔ 自己的语音条 | **17.0px** | **4.3px** |
| 相邻消息之间 | **10.3px** | **16.3px** |

**根因**：可见间距 = `.msg-row` 的 `row-gap`（`clamp(8px,3vw,12px)`→10.8）**加上**
`.voice-bar` 自己的 `margin-top: 6px` = 16.8px —— 两处叠加，谁也没错，合起来就过了。
**修法**：`.msg-row.has-voice { row-gap: 0 }`（换行行的间距归零，完全交给条自己的 margin-top
`6px→4px` 决定）；`#messages` 的 gap `clamp(8px,2.8vw,12px)` → `clamp(12px,4.5vw,18px)`（→16.2px）。
→ 层级变成 16 vs 4，语音条明显属于它上面那条消息。

新增 H 组 4 条守卫（`test_chat_ui_geometry.py` 升到 28 项），核心是
**断言「气泡↔语音条」严格小于「消息间距」** —— 这条不变量比具体数值重要，
以后谁调间距都不会把层级关系调反。

## 十七、2026-09-18 语音模型上魔搭（已上线）+ 下载器加 sha256 对账

用户问「语音模型插件放到魔搭社区上（能放上去吗？）」，随后拍板"先写脚本、等我给令牌"，并指示
**"不用，我直接发给你（只要你不泄露就行）"** + **"你可以写到我本机的本地环境里，并在项目文档里说明位置"**。

### 一、能不能放 —— 能，且是**实测**不是推测

从本机安装的 `modelscope` SDK **常量**读到平台硬限制（比任何网上说法都权威）：

| 项 | 平台限制 | 本模型 |
|----|---------|--------|
| 单文件 | `UPLOAD_MAX_FILE_SIZE` = **100 GB** | 最大 351 MB |
| LFS 阈值 | `UPLOAD_SIZE_THRESHOLD_TO_ENFORCE_LFS` = 1 MB | 8 个文件全走 LFS |
| **非 LFS 小文件合计** | `UPLOAD_NORMAL_FILE_SIZE_TOTAL_LIMIT` = 500 MB | 不适用 |
| 文件数 | 100000 | 8 |

> **辟谣**：网上（含一篇 AIGC 生成的博客）流传"魔搭单仓库上限 500MB"，那是把
> **非 LFS 小文件合计上限**误读成了仓库总容量。实证：官方 `Qwen/Qwen2.5-7B-Instruct`
> 仓库里就是 4 个 ~3.9 GB（共 15.2 GB）走 LFS 的权重。官方开发者群答复同样是"容量没限制"。

### 二、已上线

| 项 | 值 |
|----|----|
| 仓库 | **`cpt0721/firefly-voice-v4-onnx`**（公开）→ https://www.modelscope.cn/models/cpt0721/firefly-voice-v4-onnx |
| 内容 | 8 个模型（871.9 MB，全 LFS）+ `README.md` + `LICENSE`；**fp16 BERT 571 MB 故意不传** |
| 上传 | 321 s（上行 ~1.4 MB/s）；传完 **8 个文件逐个 sha256 与远端 API 对账全部一致** |
| 零配置 | 地址已写进 `app/voice/plugin.py` 的 `DEFAULT_REPO_URL`；真机插件页 `repo_configured=true`、地址输入框自动隐藏、直接显示「下载模型」 |
| 工具 | `tools/upload_voice_models.py`（审查 → 硬链接暂存 → 建仓 → 上传 → sha256 对账 → 打印地址模板）；`--dry-run` / `--check` 都有 |

**README/LICENSE 措辞**：不出现任何商标词，只写"中文语音合成 ONNX 模型（GPT-SoVITS V4 导出）"，
声明"仅供个人学习、禁止商用、权利归原权利人、收到通知即下架"。**`license` 字段填 `other`** ——
平台预置的 8 个标准许可没有一个适合"转分发他人权重"，填标准许可等于宣称无权授予的权利。

### 三、凭据位置（用户要求写进文档）

**令牌存在本机用户级环境变量 `MODELSCOPE_API_TOKEN`**（注册表 `HKCU\Environment\MODELSCOPE_API_TOKEN`）：
SDK 的 `HubApi.login()` 和我的 `resolve_token()` 都读它 → **日常上传不用再传令牌**。

★ **实测踩坑**：`~/.modelscope/credentials` 那个文件在本机**读不出来**（Python `PermissionError`、
PowerShell「访问被拒绝」，ACL 异常），而且 `HubApi.__init__` 并**不**自动读它
（`self.token = token` 默认 None）——所以**不要依赖那个文件**，用环境变量。
详见 `docs/工具/tts.md` §12.2。

### 四、另外两个坑（都记进 tts.md）

1. **本机 git 全局代理把上传挡死了**：`http.proxy = http://127.0.0.1:7897` 而代理没在跑 →
   SDK 建仓时 `git clone` 报 `Failed to connect to 127.0.0.1 port 7897`。
   解法：给命令加 `$env:NO_PROXY="www.modelscope.cn,modelscope.cn"`（只影响该进程，
   **不动用户的 git 配置**）。Windows 上没法用空环境变量覆盖 git 配置（空值会被删掉）。
2. **下载器加 sha256 对账**（用户批准）：ModelScope 清单接口免费返回每个文件的 `Sha256`，
   下载完逐个比对；坏文件**在 `os.replace` 之前**丢弃、半截 `.part` 一起删（不留下次误当续传起点）；
   已就绪且校验通过的文件直接跳过。清单取不到时**降级跳过校验**（只记日志，绝不阻断下载）。
   新增 `tests/test_voice_download_verify.py`（22 项，起本地假仓库跑真实链路，全离线）——
   它当场抓出我一个真 bug：清单 URL 原来用写死的 `modelscope.cn` 做 base，
   用户配**自建镜像**时会跑去魔搭取清单 → sha 对不上 → 下载被误判失败。已改成从模板自身 origin 推导。

### 五、真机端到端验收（用户要求"手动卸载了再操控点击下载，模拟真实环境"）

`adb uninstall` 清干净 → 全新 `install` → **全程只用 adb tap 点，不用 API 触发**：

| 步骤 | 结果 |
|------|------|
| 首页点「语音插件」 | 「↓ 未安装」、目录 `/data/user/0/...`、按钮「下载模型」、**无仓库地址输入框**（零配置） |
| 点「下载模型」 | 匿名下载 **108 s / 871.9 MB / `verify=on` / `verified=8` / 零错误** |
| 点「卸载」→ 再点「下载模型」 | 确认框正确提示"删除约 872 MB、已生成语音不受影响"；二次下载同样 8/8 校验通过 |
| 进聊天 → 长按 → 「转语音」 | 语音条「🔊 4″」出现；`voice_cache` = `story/seq 53/4.09s/392684 字节`；logcat 见 AudioTrack 播放 |

**副产品**：App 登录后**云端同步会把历史拉回来**，"卸载重装"不丢对话数据；也因此长按试听有消息可用。

### 六、真机抓出的两个 bug（已修 + 已加回归）

1. **`percent` 算出天文数字**：ModelScope 对文件 URL 的 **HEAD 返回 200 但不给 Content-Length**
   → `total` 恒为 0，而调用处写 `total or 1` → `percent = 已下字节×100/1`，真机日志出现
   **`31435628400%`**（UI 只 clamp 了进度条，文字照显）。
   修法：总大小改用清单里现成的 `Size`（还省掉 8 次 HEAD）；只在 `total_hint>0` 时按字节算并 `min(100,…)`；
   UI 再加一道 clamp。真机复验 percent 1→15→38→54→…→100 全合法。
2. **清单 URL 用写死的 modelscope.cn 做 base**：用户配自建镜像时清单会跑去魔搭取 → sha 对不上 →
   下载被误判失败。改成从模板自身 origin 推导；镜像无清单端点 → 404 → 降级跳过校验（安全）。

### 七、未做 / 待用户

- 用户上传用的令牌**在会话记录里出现过**，已建议去魔搭后台删掉重建
  （本机用户级环境变量 `MODELSCOPE_API_TOKEN` 已存好，重建后覆盖即可）。

## 十八、2026-09-19 v0.9.0 宣传物料包（promo/，未提交 git）

用户要求以开发中版本 0.9.0 制作宣传视频（手机端演示为主），允许少量真实对话但明确控制成本。

**产出**：`promo/`（52MB，不入 git 待用户拍板）——3 个成品 mp4（主片 ~103s 21.7MB + 记忆机制 46s + 角色卡工坊 52s，1080×1920 H.264）+ 3 个自播放 HTML 动画源（index/mem/pack，空格暂停→快进 R 重播，?ui=0 隐藏 UI）+ tools/（record_gen.py 一键重录、check_promo.py 布局抽检）+ 说明.md（含诚实不足清单）。全部素材来自**真实 UI 截图与真实历史对话**（user_data 151+89 轮、50 张原生贴纸、主动消息原文、haruno 旁白），**零 API 消耗、零真机占用**（用户手机拿走玩了，全程只起本地沙箱服务）。

**可复用管线（下次发版 0.10.0 直接重录）**：`FIREFLY_ANDROID=1 FIREFLY_DATA_DIR=<沙箱>` 起服务 → Playwright 390×844@3x 截图 → HTML 时间轴动画（机模+真实截图+复刻气泡）→ record_video 出 webm → imageio-ffmpeg（venv 隔离，**阿里镜像可用、清华镜像不行**）转 H.264。Playwright 自带 ffmpeg 只有 VP8 出不了 mp4。

**验收管线（识图不可用时的替代）**：vision 技能 401（opencode 套餐 key 失效，待续费）→ 改用「几何断言（每场景标题/机模/底栏边界+遮挡+溢出）+ 成品 mp4 逐时刻抽帧像素统计（黑屏/空白判定）」双管线，20/20 帧过。两个坑：① HTML 重复 id 会让 querySelector 系检查全错位（本片 11 个 id="phone" 已改 class）；② ffmpeg `select=gte(t,X)` 多条件会从 8s 起连帧取样，抽帧必须逐个 `-ss`。

**顺带发现**：① 手机视口下首页 `#home-modes`/`#home-start-btn` 是 PC 桌面布局（display:none 属正常），自动化进聊天要么点轮播卡要么 mock `/auth/state`（showChat 有登录前置检查会弹回首页）；② **沙箱 config.json 里带真实 Key**（服务日志 `Key OK: sk-310e…`），本轮靠关闭主动消息+全程零 /chat 保住零消耗——下次起沙箱应先剥 api_key；③ PC 端（≥1100px）桌面化已完善（侧栏+窗口卡+首页大卡+双栏网格），体检无值得冒险改源码的缺陷，**未动任何前端源码**；④ promo/ 未提交 git，是否入库/发布待用户拍板。

## 十八、2026-09-18 热更新可行性验证（**只验可行性，未出方案**）

用户问「APP 内热更新（无需重装）可行性如何」。按项目规矩先讨论、且用户明确选了
"先只验证可行性，不出方案"，所以只做了**实测验证**并记录事实，没有设计。

### 一、结论

| 层 | 可行 | 依据（真机实测，非推测） |
|----|------|------------------------|
| 前端 HTML/CSS/JS | ✅ | `STATIC_DIR` 是**真实目录且可写**；`server.py` 用 `SimpleHTTPRequestHandler(directory=STATIC_DIR)`，文件解析完全在我们代码里 |
| Python 业务逻辑 | ✅ | 影子测试 `verdict = filesystem_wins` |
| Kotlin/native（TTS、ORT、WebView 壳、权限、Python 版本、pip 原生依赖） | ❌ | 编译产物 + 权限清单，只能整包 |

### 二、实测方式（零副作用探针，用完已撤除）

`app/api/debug.py` 临时加 `GET /hotupdate-probe`：**只调各 finder 的 `find_spec`**，
不 import、不执行、不改 `sys.modules`；影子文件写在临时目录、用完删除。
验证完撤除 → `check_backend_sync --sync` → 重建装机 → **64/64 测试 + 四门禁全绿**。

### 三、关键数据

1. **安卓下后端是解压成真实目录的**：
   `base_dir = /data/data/<pkg>/files/chaquopy/AssetFinder/app/backend/app`，
   而且 `sys.path[0]` **就是它**（不是 zip）——这解释了 `SimpleHTTPRequestHandler` 为何能工作。
2. `STATIC_DIR = …/AssetFinder/app/backend/app/static`，`static_exists=True`、
   **`static_writable=True`**、34 个文件。
3. 路径钩子次序：`path_hooks[0]=java.android.importer.hook`、
   `[1]=ChaquopyZipImporter`、`[2]=path_hook_for_FileFinder`（资产钩子只对资产路径生效）。
4. **影子测试（核心）**：模块 `routes_update` 在 APK 里的 `origin=…/backend/app/routes_update.pyc`
   （loader `SourcelessAssetLoader`）；把同名 `.py` 放进可写目录并插到 `sys.path[0]` 后
   → **只有 `meta_path[2]`（PathFinder）作答**，返回我们的文件（`SourceFileLoader`），
   **前面没有任何 finder 抢答** ⇒ 胜出者由 `sys.path` 顺序决定。
5. 覆盖层可写往返 ✓（`…/firefly_data/_hotprobe`）。

### 四、两个必须遵守的前提（都是实测得出）

1. **必须在 Chaquopy 初始化之后再插 `sys.path`**：`java/android/importer.pyc` 的
   `initialize_importlib` 里有对 `sys.path` 的**整体重写**（`STORE_ATTR sys.path`）。
   正确时序：`Python.start(AndroidPlatform)` → Chaquopy 重写完 → 我们 `insert(0, overlay)` → 再 import 后端入口。
2. **只对"当时尚未 import 过的模块"生效**（Python 常规行为）⇒ 覆盖层必须最早插，越早越好。

### 五、"能做但不该做"的岔路

探针发现那个 `AssetFinder` 解压目录**可写** ⇒ 理论上可直接改文件。**不要这么做**：
- Chaquopy 官方文档原文：*"Do not write any files to these directories at runtime, as they may be
  deleted when the app is upgraded."*
- 它按 `extract_if_changed`（size/mtime）比对解压，改了文件可能**触发重新解压、把热更悄悄冲掉**。
→ 正解是**自己管理的可写目录 + `sys.path` 插入**（已证明能赢）。

### 六、已知限制

- **服务器模式那份前端副本**（`file:///android_asset/`，本地引擎起不来时的兜底）Python 侧够不着
  ⇒ 热更只覆盖**本地模式**（正常路径）。
- Kotlin 层完全不覆盖。
- Python 热更本质是**远程代码执行**，签名校验 + 坏补丁回退不是可选项（属方案，本次未展开）。

### 七、托管选定与规范（用户已定）

- **托管**：服务器主 + Gitee 兜底。热更包与清单放自己的服务器，整包 APK/exe 继续走 Gitee/GitHub releases。
- **已起草 `docs/热更新规范.md`（v1.0-draft）**，用户已拍的 4 条决定：
  ① **累积式**补丁（serial N = base→N 完整差异）② **下载后立即生效**（但只在**空闲**时，§5.2.1 逐条定义了空闲判据；
  含 `py/` 必须重启进程，因为 Chaquopy `Python.start()` 一进程只能调一次且无 restart）
  ③ **用户可关可查**（设置开关默认开 + 关于页显示 `0.8.1 · hot.3 · a1b2c3d4` 三元组）
  ④ **APK 版本号不动，热更只累加 per-base serial**。
- 我额外加的 5 条铁律（已在文档 §十一 标出待用户审）：禁止用热更发新功能 / 补丁不得删文件 /
  验签在 Kotlin 壳且 Python 不重复实现密码学 / 只为最新整包产出补丁 / **装新整包必须清空覆盖层**。
- 用户选择"先审规范再改"，**尚未进入实现**。

## 十九、2026-09-19 热更新 v1（web 层）实现完成

用户看规范后说"可以，你可以进行实现了"。**v1 范围 = 只支持前端（web）层**，Python 层协议已留字段。

### 一、做出来的东西

| 类别 | 文件 |
|------|------|
| 规范 | `docs/热更新规范.md`（纪律）+ `docs/热更新/01_可行性依据.md`（实测数据）+ `02_实现契约.md`（字段/URL/布局） |
| 发布端 | `tools/hotupdate_keys.py`（密钥）、`build_hotupdate.py`（产出+签名+CHANGELOG）、`check_hotupdate.py`（**第 5 门禁**）、`apply_hotupdate.py`（回归 harness） |
| 客户端 | `app/hotupdate/{__init__,net,verify}.py`、`app/routes_hotupdate.py`（4 端点）、`app/server.py` 覆盖层优先级、`app/core/paths.py::hotupdate_root()` |
| 壳 | `HotUpdateKeys.kt`（公钥）、`HotUpdateBridge.kt`（java.security 验签）、`MainActivity.startHotUpdateWatcher()` |
| 前端 | `app/static/js/hotupdate.js`（轮询/空闲上报/boot-ok/草稿保留）+ 设置页「自动修复」面板 |
| 测试 | `tests/test_hotupdate.py`（53 项，离线全链路，含本地假更新源） |

**全量 65/65 测试绿**；第 5 门禁演练 19/19 PASS。

### 二、真机端到端已跑通（不是纸面）

1. 全新 APK（前端 = 基线）→ PC 当更新源 + `adb reverse` → check：
   **真机 Kotlin 对 Python 签的 manifest 验签成功**（跨语言口径一次就对）
2. apply → 覆盖层 3 个文件、逐文件 sha256 校验、原子替换
3. 冷启动后：`pending_serial=0`（新前端起来并上报 boot-ok）、`/index.html` 与 `/js/bundle.js`
   **确实来自覆盖层**（含新面板与轮询代码）
4. `rollback` → `removed=3`、覆盖层清空、原因留痕

### 三、实现中抓到的两个**设计问题**（都写进文档了）

1. **刷新触发不能只靠前端 JS**（真机实测：apply 成功但 `reload_pending` 一直不清）。
   循环依赖 —— 要修的往往就是那段 JS 本身，它没跑起来就没人问状态。
   修法：壳（Kotlin）也盯一份，两条路径互补。已写入实现契约 §七。
2. **补丁内容又打进整包后，必须作废该补丁线并重建基线**。
   `base_version` 是底座内容的标识符；内容变了而版本号没变 ⇒ 基线漂移 ⇒ 后续补丁基于错误底座。
   已写入规范 §5.4（反向铁律）。

小坑：清单路径带 `web/` 前缀而覆盖层目录本身就是 `web/` ⇒ 解压会多一层（测试 A6 抓出）；
`canonical_bytes` 收敛到 `hotupdate/net.py` 单一实现（避免签名口径写两份）。

### 四、未做 / 待办

- ~~`DEFAULT_URL_ROOTS` 仍为空~~ → **已填**：`http://101.200.14.126:8787/hotupdate`
  （2026-09-19 已在 8787 网关加 `/hotupdate/` 路由并真机验收通过，见下）。
- **Python（`py/`）层未实现**（协议字段已留）。
- **灰度未实现**（`rollout_percent` 字段已在 manifest，客户端还没按 user_id 取模）。
- **Gitee 兜底镜像未建**（把同一批文件也传一份 release 资产即可；`DEFAULT_URL_ROOTS` 是个有序列表，
  加第二个地址就自动生效）。

### 五、服务器侧落地（2026-09-19 同日完成）

- `server/download_server.py` 新增 `/hotupdate/<x.y.z>/<latest|patch-N.json|patch-N.zip>` 路由：
  **正则白名单 + 不把用户输入拼进路径**（无穿越面）；`latest` 强制 `no-store`、
  补丁本体 `immutable` 长缓存；**不占用 DL_SEM 大文件名额**（100KB 补丁不该排在 130MB APK 后面）。
- 部署：备份 `download_server.py.bak-20260919-013432` → **单文件 scp**（规范：严禁全量）→
  `systemctl restart firefly-downloads` → 建 `/opt/firefly-downloads/hotupdate/0.8.1/`。
- **真机真通道验收全通过**：出厂零配置直连公网网关 → check（真 Kotlin 验签）→ apply →
  **t+4s 前端/壳已自动刷新并上报 boot-ok** → `/style.css` 确实是覆盖层版本 → rollback 干净。
  验收用的临时补丁与临时改动**已全部清理**，基线已重建。
- 踩到一条：`set_url_roots` 的 override 是**持久化**的，会盖住新填的出厂默认值 →
  真机上要清一次（`set_urls: []` 回落默认）。换 base 时 `on_startup` 也会重置 url_roots
  （正确：地址里含版本号，旧 base 的 override 对新 base 无意义）。

---

## 二十、2026-09-19 公告通道 + 0.9.0 发版上线 + 0.8.1 兼容回归（**已上线，已真机验收**）

用户当轮原话：最后更新公告与引导流程……确保热更新没问题，保证服务器兼容以前的用户数据
（兼容并迁移重整），**仅保证兼容 0.8.1 即可**；随后明确要求**按顺序连着做完，不要一件一件停下来问**。
下面五件一次做完。

**① 公告通道（新子系统，规范见 `docs/公告通道规范.md`）**
- 端侧 `app/notice.py`（状态机 + 结构校验 + 图片 sha256 对账）、`app/routes_notice.py`
  （`GET /notice`、`GET /notice-image`、`POST /notice/action`）、`app/static/js/notice.js`。
- **复用热更的信任根**（同一把 RSA 公钥、同一个验签实现、同一套 serial 防回滚）；
  **更新源由热更源翻译而来**（一个配置两条通道：`/hotupdate` → `/notice`；Gitee 的 `v{base}` 目录原样用）。
- **安全线是"能不能注入"而不是"能不能执行"**：只接受 `p/h/li/tip/img` 五种结构化块，
  端侧一律 `textContent` 渲染（`test_notice.py` J 组静态守卫：源码里连 `innerHTML` 字样都不许有）；
  清单 256KB / 40 条 / 单条 120 块 / 正文 4000 字 / 单条 6 图 / 单图 1.5MB，全部设上限。
- **降级三级**：联网失败用缓存 → 无缓存显示 APK 内置指南（`#notice-static`）→ 校验不过整份丢弃。
  面板 = 「服务端叠加 + 静态兜底」，**两层都在**（`index.html` 里刻意保留静态那份）。
- 内容源 `notice_src/`（进 git）↔ 运行期缓存 `notice/`（gitignored）：**两个目录刻意不同名**
  —— 第一版同名，建目录时才发现会与 PC 端的缓存路径撞车。
- 工具 `tools/build_notice.py`（产出时用**客户端自己的校验器**再验一遍）、`tools/publish_notice.py`。
- 回归 `tests/test_notice.py`（52 项，全离线）。

**② 补掉一个真洞：PC/服务器端从来没有验签实现**
- `verify.py` 原来只有 Kotlin 一条路 ⇒ PC 版与服务器版**永远验签失败**（热更与公告在 PC 上
  静默永不生效，零报错）。现补**标准库自实现** RSA-2048/SHA-256 验签
  （公钥副本 `app/hotupdate/pubkey.py`，与 `HotUpdateKeys.kt` 的逐字节一致性由门禁强制）。
- **签名（带私钥）仍只在发布机用 openssl** —— 自实现只做"验"，且只在没有 Kotlin 桥时启用。
- 固定向量测试 `tests/test_hotupdate_verify.py`（20 项）。这条洞也是"服务器能收公告"的前提
  —— 服务器走的正是自实现这条路。

**③ 0.9.0 真发版 + 部署（含 P0-1 安装包债）**
- **Gitee 之前只有到 v0.8.1 的 release**，而整包兜底取"id 最大的 tag"、热更/公告镜像根写死
  `v{base}` ⇒ 0.9.0 根本没有可下载地址、镜像必然 404。新建 **v0.9.0 release（id 1153592）**并挂 APK，
  工具 `tools/publish_gitee_release.py`（幂等；建完必须断言"id 最大者就是本 tag"）。
- **APK 132.6MB → 81MB**：Gitee 单附件上限 **100MB**（官方配额），而 `libonnxruntime.so`
  单 ABI 就 31–37MB ⇒ 发布包只带 `arm64-v8a`（x86_64 只在 debug 变体，供模拟器）。
  **不是顺手优化：不做的话整包下载会被 Gitee 直接拒绝。**
- 服务器 `/opt/firefly-downloads/firefly.apk` 同步换新包，**sha256 与 Gitee 那份一致**
  （`b881b82b…`）⇒ 两条下载通道终于同一个 0.9.0。
- **公告接收端部署到生产**（"服务器能发、手机能收"的前提）：三个新文件
  （`app/notice.py`、`app/routes_notice.py`、`app/hotupdate/*`）+
  `core/paths.py`（+`notice_root`）、`api/router.py`（**外科式只加 4 条 notice 路由**）、
  `server/app.py`（`/notice-image` 公开分支）、网关 `/notice/*` 路由。
  部署纪律：**改动前整批备份** → 上传 → `py_compile` → **干跑导入检查**（确认不会 ImportError）
  → 才 `systemctl restart`；重启后逐项验（`/health` 200、`/notice/latest` 200、非法名 404、
  `/index.html` 200、登录仍可用）。
- **特意没部署服务器网页前端**：线上 `app/` 还是 0.8.1 时代的（缺 `/memory-status`、`/archive`、
  `/memory-action`、`/pack-file/*`、`/voice/*`…），把新前端推上去会让服务器网页 UI **降级**。
  已登记 P0-8，等后端对齐再一起上。
- `version.json`（服务器两份）从"http 自下载 + tag 0.8.1"改成 **https Gitee 直链 + v0.9.0**（P2-8 关闭）。

**④ 0.8.1 用户数据兼容 —— 先证明，再改代码**
- **只读探测生产服务器**拿真实形状（不猜）：`config.json` 是 `api_key/api_base/server_url/*_model/…`
  （**没有 `providers`** → 必须走旧结构迁移；还带着已下架的 `hidden_reply_enabled`）；
  **没有 `packs.json`**；对话行两种形状（正文 `content`、旁白 `text+style`）；
  用户目录 `{uid}/{mode}/{data,images,journal,character,.sync_backups}`。
- 照真实形状造夹具写 `tests/test_compat_081.py`（32 项）：旧 config 迁移（含**迁移前自动备份**
  可回滚、二次加载幂等）、服务器版模式发现、两种对话行可读、无记忆/归档时惰性不炸、
  旧 `registry.json` 可读、旧文件原位不动、`.sync_backups` 不被碰。
- **结论：现有代码本来就兼容 0.8.1，本轮不需要写任何迁移代码** —— 是"测了才知道"，不是"觉得没问题"。

**⑤ 两条只有真机能抓到的 bug（都已修 + 已加防线）**
- **TDZ（`docs/错误总结.md` #15）**：bundle 是**单作用域拼接**，`main.js` 顶层调 `notice.js` 的函数
  撞上 `let _payload` 的 TDZ ⇒ `ReferenceError` ⇒ **bundle 后半截整个没执行**（公告全无、
  连 `window.noticeOnOpen` 都没注册），而后端与 52 项单测全绿、零日志。
  修法：模块**自己挂 `DOMContentLoaded` 自启动**，跨模块只在事件/调用期引用。
  新增 **`tests/test_frontend_boot.py`**：Node + DOM 桩把**产物 bundle 真跑一遍**
  （它当场复现了这个 ReferenceError，修好后转绿）。
- **"先回缓存、后台刷新"没人跟第二次（#13）**：首装时 `/notice` 第一次返回空 payload +
  `refreshing: true`，前端拿到空就再也不请求 ⇒ 首页不亮圆点、面板只有内置指南。
  修法：前端处理 `refreshing`，4 秒后自己再拉（最多 3 次）。

**⑥ 端到端验收（真链路，不是单测）**
- `tools/e2e_hotupdate.py`（25 项，**真私钥签名 + 真验签 + 真 HTTP + 真覆盖层**）：
  产出补丁 → check → apply → 覆盖层被 `server._static_file` 解析到 → boot_ok → 防重复应用 →
  **安全模式**（连签两版坏补丁 → 第二次启动判定后自动回滚）→ **kill switch**
  （`revoked_serials`+`min_safe_serial` → 自动回滚）→ **双源降级**（主源 404 → 兜底顶上）。
- 真机（OPPO PJE110 / 704c2441）：装 81MB 新包 → 清数据冷启动 → 首页公告栏显示**未读红点 ①**
  → 打开面板**服务端公告卡片正常渲染**（标题/日期/置顶边/列表/灰字提示）→ 1.5 秒后 `unread` 归零。
  设备侧 `/hotupdate/status` 显示 `backend=kotlin`（真机走 Kotlin 验签，与 PC/服务器的自实现两条路都对）。
- 服务器侧独立验证：服务器自己 `check(force=True)` 拿到 `serial=1 / 13 块`（走的就是自实现验签）。

**⑦ 顺手修掉三处"假失败"**（`docs/错误总结.md` #12）
`test_routes_split`（写死 70/39）、`test_snapshot`（写死教程 9 步）、`test_core_split`（写死版本号）
—— 全部改成**从唯一真相源读**（AST 解析 oracle / 只断言"从①起连续" / 现读 config.py）。

**本轮新增工具**：`tools/build_notice.py`、`tools/publish_notice.py`、`tools/publish_gitee_release.py`、
`tools/e2e_hotupdate.py`。
**本轮新增测试**：`test_notice.py`(52)、`test_compat_081.py`(32)、`test_hotupdate_verify.py`(20)、
`test_frontend_boot.py`(6)。

---

## 二十一、2026-09-19 PC 端前端重构 · 阶段 1（三栏外壳）

用户原话："PC 端 UI 有很大[问题]，依据当前版本的前后端架构，PC 端的前端需要重构以确保前端 UI
适配业务逻辑……确保 UI 风格与手机端统一，前端样式符合项目的数据逻辑和业务逻辑"，
并明确"你直接做"（不要一件一件停下问）。

**审计先行（先看图，再改代码）**：用无头 Chrome + CDP 在 1600×1000 / 1280×800 / 1024×768
逐视图截图（`tools/pc_audit.py`，产物 `.tmp_test/ui/pc/`），量出 9 条问题（A1–A9）写进
`docs/设计/PC端前端重构.md`。定性结论：**PC 端不是"专门做的桌面界面"，而是"手机界面塞进右侧卡片
+ 左边加了一条 6 项导航"** —— 没有角色包列表（而角色包是本项目第一等数据单位）、
没有常驻状态、菜单是整屏模态、设置是放大版手风琴、本地版"登录前置"这条业务规则完全没被界面表达。

**阶段 1 落地**（真三栏：图标栏 72 + 上下文列 300 + 工作区）：
- 新增 `app/static/pc.css`：**PC 唯一样式源**（旧 `style.css` 末尾那段 PC 规则整段删除，
  只留一行指路注释）。除 `:root` 变量与"默认隐藏外壳"外，**所有规则都在 `@media (min-width:1100px)` 内**。
- 新增 `app/static/js/pc_shell.js`：纯 classic script、**零框架**；渲染图标栏/上下文列/顶栏/状态栏。
  列② 的内容按导航切换：角色包（头像+名字+当前标记）/ 角色卡的九个域 / 设定文件三份 /
  设置四组 / 公告条目 / 收藏 / 日志 / 语音 / 纠错。
- `index.html`：加 `#pc-shell`(rail+list) + `#pc-main-head` + `#pc-statusbar`；
  **所有 view 的 id 一个没动**，`showChat/openSettings/openMenuTab/openPackView/…` 零改动复用。
- **删掉框架依赖**：`js/pc_nav.js` + `vendor/petite-vue.iife.js` 一并移除
  （PC 端此前为了 10 项导航按需下载一个 MVVM 框架）。
- 视图层定位：`body{position:relative}` + 视图 `position:absolute` 收进工作区
  （`!important` 是刻意的：各 view 在 style.css 里写法不一，逐个覆盖更容易漏）。

**两个当场踩到的坑（都写进设计文档 §五）**
1. **PC 覆盖样式被内联 `<style>` 压住**：`index.html` 的 `<head>` 里有内联样式块，
   其中 `#settings-panel .settings-body > *{max-width:720px}` 与 pc.css 同优先级，
   而内联块在 pc.css **之后** → 桌面设置的两栏栅格被压回一条 720 窄柱。
   **修法**：`pc.css` 的 `<link>` 挪到内联块**之后**（PC 覆盖必须最后加载）+ 提一级特异性双保险。
2. **不能跟"手风琴"对着干**：设置面板 JS 里一次只开一组（移动端刻意设计）。
   阶段 1 曾想"桌面全展开"，实现是逐个 `head.click()` → **开一个关一个**，最后只剩最后一组开着。
   **修法**：顺应它 —— 折叠组排成两列瓦片、**展开的那组独占整行**；分组导航交给上下文列。

**验收**
- 桌面四档截图：三栏齐备、无横向滚动、无视图互相压盖；首页=角色包网格、聊天列限宽居中、
  公告=阅读列、设置=瓦片+展开占整行。
- 窄屏 390×844 / 768×1024：外壳三件 `display:none`、390 满幅、768 仍是既有"居中 640 单栏"、
  设置未被强制全展开 —— **14/14 结构不变量通过**（`tools/mobile_regress.py`）。
  ⚠️ **手机被用户拿走了，本轮没有真机复验**（已登记 P2-15）。
- 新增 `tests/test_pc_shell.py`（25 项静态守卫）：断点隔离、**pc.css 加载顺序**、视图 id 未改、
  全局入口存在性判断、打包/同步登记、无框架残留。全量 **71/71 绿**，五门禁 + APK 门禁通过。
- 前端改了 ⇒ 重建 APK（81MB，`check_apk_manifest` 通过、包内自检 11 项全过、
  无 pc_nav/petite-vue 残留），**两个下载通道换成同一个构建**（sha256 `2abe9bec…`：
  Gitee v0.9.0 release 用 `--replace` 先删旧附件再传 + 服务器本地副本），
  并**重建 0.9.0 热更基线**（重建后再跑一次 build 报"没有任何变化" = 基线 == 出厂前端）。

**阶段 3（同轮补完）：聊天区「包上下文」栏** —— 把审计 A5（聊天列居中后右侧 40% 空白）转成信息位：
记忆活跃窗口进度条（`43/100` + 保留 30 轮 + 提醒/自动整理阈值）+「让角色休息」、
历史存档月份（`已有 2 个月份：2026-09、2026-08`）+「查看/编辑存档」、快捷动作（撤回/表情包/转语音/公告）。
数据源就是既有端点（`/memory-status`、`/archive`），动作复用既有按钮接线，没有新造业务链路。
`#app { right: var(--pc-aside-w) }` 只让聊天视图让位（实测：聊天区 910 + 侧栏 300，输入栏 836 居中不重叠）。

**阶段 2b（同轮补完）：角色卡域导航两栏化** —— 列② 列 `/pack-structure` 的九个域（带说明），
点某一域 → 打开包编辑页并**只展开该域**（`details.pt-domain[data-key]` 的 `open`）。
★ 出图时抓到一个只有看图才发现的 bug：**桌面单工作区，切视图前必须显式关掉其它视图** ——
菜单抽屉 z-index 更高，会把包编辑页整个盖住，看着像"没打开"。修法：统一 `closeAll()`，
`go()` 与所有"打开视图"的列表动作先清场。

**阶段 2c（待做，P2-14 仅剩此项）**：收藏/请求记录在列② 直接列条目。**刻意留到最后**：
本机开发数据里收藏 0 条、请求缓冲为空，列出来没法当场验收。

> ⚠️ 本节的"阶段 1/2a/2b/3"**已被第二版整体取代**（用户 2026-09-19 否掉了自造导航）。
> 第二版见下面「§二十二」：层级与命名 100% 沿用手机，只保留底部状态栏 + 菜单右侧停靠。

---

## 二十二、2026-09-19 PC 端前端**第二版**（第一版被否掉后重做）+ PC 0.9.0 安装包

**用户否掉第一版的理由（三条，都成立）**：
1. **"这是在首页，首页你展示一堆菜单页里的东西干什么？那你菜单页有什么用？"** ——
   我把**手机 ☰ 菜单页里的东西**（收藏/表情包/设定文件/请求记录/流程日志）抄成了全局左栏。
2. **"左边留个侧边栏不明所以（还有残留功能比如纠错）"**。
3. **"UI 风格完全的廉价 AI 味，我手机改好的 UI 不学（甚至照抄导致完全没适配）"** ——
   用 emoji 代替它自己的 SVG 精灵；把手机的三层结构原样搬成并排的栏，而不是换一种呈现。

**按用户要求上网查这类问题的解法**，找到的正式依据（微软官方 UX 指南 Windows Ribbons）：
- *"Present each command on only one tab. Avoid multiple paths to the same command …
  having duplicate commands makes it harder for users to find other commands they are scanning for."*
- *"Reserve commands that belong in the Application menu only for that menu."*
- *"Use context menus and mini-toolbars for in-place, contextual commands."* ——
  只作用于当前对象的命令（收藏/记忆/日志）不该升格成窗口级导航。

**第二版准则：层级与命名 100% 沿用手机，PC 只改"呈现"。**
- 删掉五块自造结构：`#pc-shell`(rail+list) / `#pc-main-head` / `#pc-chat-side`，以及 `pc_shell.js` 里
  的整套导航逻辑（700+ 行 → ~120 行，只剩底部状态栏）。
- ☰ 菜单**恢复为可见入口**；呈现从"覆盖抽屉"改成**右侧停靠面板**（`--pc-menu-w`），
  菜单内容、两组分组、四个动作**一字不动**。
- 首页仍是同一张卡片组件：轮播 → 多列网格（`auto-fit`，包少时卡片自动变宽）+ 内容居中一列。
- 只额外加**底部状态栏**（版本/热更/同步/公告未读）——状态是信息不是命令，不构成重复路径。
- 顺带修掉一个真实渲染 bug：`.msg-row .bubble{max-width:68%}` 与手机上 `.msg-col .bubble{100%}`
  叠加、而 `.msg-col` 又是 shrink-to-fit ⇒ 两级互相挤压，`嗯，我在` **一个字一行**（内容宽仅 29px）。
  现在上限只由 `.msg-col` 承担。
- `tests/test_pc_shell.py` 重写为第二版守卫（**33 项**）：不许有第二套导航 / ☰ 在 PC 必须可见 /
  菜单必须停靠 / 断点隔离 / 气泡上限只设一次 / pc_shell 只做状态栏且无 emoji / 打包同步登记。

**PC 0.9.0 安装包（P2-12 关闭）**：`python -m PyInstaller firefly.spec --noconfirm` → 清
`dist/firefly/user_data` → `ISCC package/firefly.iss` → `package/firefly-setup.exe`
（**22.09MB / ProductVersion 0.9.0**）。新增冒烟工具 `tools/smoke_pc_exe.py`（**14 项**）：
起冻结版 exe，验 `/config` 200、包内是第二版前端（无自造外壳 / 有状态栏 / 有 ☰ 与菜单本体）、
`/modes`、`/hotupdate/status`（`backend=python`）、`/notice`。
**两个通道同一构建**：Gitee v0.9.0 资产与服务器本地副本 sha256 均 `9a189dbf…`。

**阶段 2a（同轮补完）：设定文件两栏化** —— 列② 选「用户记忆 / 历史对话存档 / 手账」，
右栏**只显示这一段**。实现是 `#tab-char` 上的 `data-pc-sec` + pc.css 三条显示规则，
**没设属性时三段全显示**（所以窄屏与"pc_shell 没跑起来"都退回手机行为，不会丢内容）；
`tools/mobile_regress.py` 加了对应断言（窄屏必须 3/3 可见）。

**重发**：阶段 2a 改了前端 ⇒ 重建 APK 并两通道重换（`a88339a1…`）；
阶段 3 又改了 ⇒ 再重建再重换（`fdf417d9…`，Gitee `--replace` + 服务器本地副本两边 sha 一致，
公开链 `/download/resolve` 实测落到 foruda 直链、Content-Length 84,900,735 与 Gitee 资产一致），
热更基线每次都重建。**0.9.0 的产物本轮共被替换三次** —— 用户明确"暂不修改版本名"，
所以同版本号换构建；将来要区分应改用 0.9.1（versionCode 需递增）。
