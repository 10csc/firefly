# 协作记忆（跨会话）

> 作用：新会话通过 CLAUDE.md 自动读到本文件，快速恢复「用户是谁、怎么协作、现在在做什么」。
> 维护规则：每个会话结束前由 AI 主动更新（追加/修订/删除过时条目）；用户口头修正永远优先于本文件内容；本文件只记事实，不记猜测。
> 首次建立：2026-08-13。

---

## 一、用户的协作习惯

| # | 习惯 | 说明 |
|---|------|------|
| 1 | 中文交流 | 所有对话、文档、注释用中文 |
| 2 | 先讨论再动手 | 范围广的改动先确认再执行；用户明确指派的小任务可直接做到底（含构建+真机验证） |
| 3 | 绝不 push GitHub | 只允许本地 git 提交；发布到 github 仅在用户明确要求时做 |
| 4 | 不删构建缓存 | android/sdk、.gradle、build、dist 等一律保留（删了构建全量重跑） |
| 5 | 模型分工 | 默认挂 flash 执行；规划/架构决策/系统级错误排查，AI 在回复中标记「建议切 pro」，用户手动切换后同会话继续 |
| 6 | 真机验证 | 用户会提供 adb 手机链接（设备 704c2441，装有本地版+服务器版两个 APP）；安卓改动应装真机验证 |
| 7 | 服务器可运维 | 可 SSH 连服务器 101.200.14.126（root + C:\Users\FANGL\.ssh\id_rsa）；生产环境改前先备份 user_data；已有每日 3 点备份 cron（/opt/backups）+ attack_watch 每分钟 cron；8765 只本机、公网走 8787 反代 |
| 7 | 图片兜底 | 会话模型无图像输入时，用 Windows 内置 OCR（WinRT PowerShell）识别截图；uiautomator dump 读页面文字；**识图用 vision 技能（mimo-v2.5）**——已接入会话工具并固化到个人预设（见下方「进行中/最新」） |
| 8 | 沙箱按需升级 | 被沙箱拒绝时可按规则升级重试；用户曾直接「去掉沙箱」授权全权限 |
| 9 | 文档规范 | 树状分文件放 docs/，单文件 ~200 行内；重要结论在聊天里给摘要，全文进文档 |
| 10 | 服务器版隔离 | server/ 代码不入 git；前端以 app/static 为唯一源，server/frontend 与 android assets 经 tools/sync_frontends.py 同步 |
| 11 | 发布前校验 | 发版必须跑 tools/check_version.py；5 处版本号同步 |

## 二、用户关注的方向

- **当前主线**：双线版本推进——本地版「场景真实化」；服务器版「部署验证与推广」。
- **近期关注点**：产品体验与设计感（如服务器版 APP 顶部 ActionBar 修复）；**小白使用者的易用性**（新用户是否会被门槛劝退）。
- **历史待办清单**：原 `docs/三版本APP缺陷分析执行文档.md` 已随 0.8.0 完成归档至 `封装（没指定就不读）/2026-08-16_归档/docs/`。
- **技术偏好**：依赖最小化（能自己实现不引库）；架构上显式记录取舍（YAGNI）；改动前先讨论。

## 三、当前工程状态快照

| 项 | 状态 |
|----|------|
| 版本 | 0.8.0（app_config.APP_VERSION / versionCode 805） |
| 服务器 version.json | 0.8.0（含 exe/apk 下载链接格式，已部署服务器） |
| 服务器代码 | **已部署 0.8.0**（含模型锁/安全加固/额度展示；firefly-server + firefly-downloads active） |
| 服务器版/本地版 APK | 均已重新构建 + 真机验证（导出下载链路实测通过） |
| 本地版 PC exe | console=False 无黑窗口版已构建验证 |
| 前端三副本 | 已同步（app/static、server/frontend、android/app/src/main/assets；login.html 已消除历史漂移） |
| check_version.py | 已扩展覆盖 8 处，全 PASS |
| 已知未修 | 微信输入法键盘 bug、PC 图标白方块；数据导入、HTTPS/域名、主动性自然语言文案待做 |

## 五、进行中 / 最新

- **Kimi 项目梳理+加固部署+大重构（2026-08-28 完成，版本号不动 0.8.1）**：①安全加固（08-25 GLM 审查产物）**已提交 8922a56 并已部署服务器**——部署前对生产 md5 全量比对（差异=加固+2 处落后：sticker_picker.py 停在 W5 前、frontend/api.js 停在 8996675 前，均已一并带齐）；备份 `/opt/backups/{user_data,code}_pre_harden_20260828-1605.tar.gz`；重启后验证全绿（db v2 迁移 14 条明文 token 透明哈希化、公网实测假 2GB Content-Length→413、resolve→foruda 直链、CORS 预检正常）；顺手删除生产 `/opt/firefly/app/` 下 4 个 server 文件惰性重复副本（ExecStart 证实跑的是 server/ 副本）。**版本号未动，旧客户端无感**（多次强调勿升版本）。②超大文件拆分（路由 oracle 键集对比+全量 33/33 测试绿为验收）：routes.py 2680→750+8 子模块（routes_common/auth/config/assets/fix/data/update/relay）；proactive.py 1009→337 facade+proactive_gate+proactive_gen（`modules.proactive.<名字>` 全量 re-export，测试打桩点兼容）；前端 panels.js 1218→498、chat.js 1098→574，新增 settings/update/sync/chat_render/chat_media/history 六模块（**CURRENT_VERSION 已移到 update.js，check_version.py 同步改验**，bundle ORDER 更新，函数集合 171==171 不变）。③死代码清理 21 项（逐项 grep 取证）：**安卓 FireflyJs of JS 桥已补注册**（考古结论：该桥从未在 Kotlin 存在，此前服务器模式后台主动通知静默失效；现对齐「仅后台打扰」语义）、删 set_admin/_SCHEMA_VERSION/ASSETS_DIR/VALID_MODELS/server_url 死配置、迁移 bak 保留 5 份上限、GLOSSARY/协作记忆/.gitignore 清 android_server 幽灵引用、本次更改说明.md 归档至 封装/2026-08-28_归档。④新增 `项目架构图/index.html`（交互式架构可视化，单文件离线，数据为拆分后实测口径）。**注意：以上②③只提交本地 git（5 个 commit），服务器跑的还是拆分前代码（行为等价），安卓 APK 未重建（allowBackup/FireflyJs 修复待下次构建生效）**。剩余债：明文 HTTPS 未做（最高优先，待域名决策）、quota_api_proxy 字段未启用、_SESSION_MAX 全局帽、docs/项目结构报告.md 全文过时（可用项目架构图替代，待用户决定是否重写）。

- **安全加固集中修复（2026-08-25 完成，2026-08-28 已提交 8922a56 并部署服务器，版本未动）**：三端安全审查后修复 10 项——①网关 `_proxy` 请求体上限 16MB（原任意 Content-Length 直接 read 可单连接 OOM；411/400/413 三段校验）；②relay SSRF 双层拦截（X-API-Base 入口 + relay_proxy 出口均过 `api_client.is_public_endpoint` 公网校验：DNS 全记录、环回/私网/链路本地/CGNAT 全拒，host 级 300s 缓存）；③**定性修正**：img_id「跨用户穿越」实测不可达（pathlib glob 的 `..` 是字面量匹配，Python 3.12.6 实证），真实问题是绝对路径 pattern 抛未捕获 NotImplementedError——已加 `[A-Za-z0-9_-]{1,64}` 白名单（routes 的 _load_image_data_url 与 get_image 两处统一）；④sessions token 落库改 sha256 哈希（db schema v2 迁移自动改写存量 + 旧明文行命中时就地升级，auth.py 零改动）；⑤verify_password 改 hmac.compare_digest；⑥loginfail 键写路径自愈清理（db.prune_login_fail_keys，防邮件喷洒撑 settings 表）；⑦Android allowBackup→false（**重建 APK 才生效**）；⑧multipart 畸形 Content-Length 安全拒绝；⑨AdminHandler body 64KB 帽。顺带修 test_image_unseen flaky（硬编码 sleep 0.2s 撞窗口注册竞态→轮询等待；stash 验证确认与安全改动无关）。**验证**：新增 tests/test_security_hardening.py 59 项断言全过；全量 33 测试文件退出码全 0；check_version / sync_frontends --check / build_bundle --check 三门禁 PASS。文档：docs/安全加固执行记录-2026-08-25.md（含未实施建议清单——公网 HTTPS 为最高优先遗留项）。**server/ 四文件（download_server/server_app/auth/db）改动前备份在本机 QoderWork 工作区 backup_20260825_security/；部署生效提示见报告第七节（db v2 迁移在服务重启时自动执行，用户无感知）**。另：用户已明确知识库全量注入是有意设计（RAG 无法处理抽象用户语言 + 安卓不可行），勿再作为问题提出。

- **消息引用与收藏（2026-08-17 完成，PC 验证通过，待装机）**：聊天页长按消息（触屏按住 500ms / PC 按住或右键）弹出 QQ 式菜单「📎 引用 / ⭐ 收藏」。引用 = 输入框上方引用条 → 消息带 `quote` 快照落 jsonl（白名单字段 + 2000 截断），LLM 侧 `compose_user_text` 合成 `[引用流萤：「…」]` 前缀（重启回灌 hydrate 同样带），气泡上方渲染 `.quote-preview` 小卡片。收藏 = 快照式存储 `{mode}/data/favorites.json`（`app/modules/favorite_store.py` 新模块，上限 300、按 seq 去重、无 seq 生成 f 前缀 id、随导出/同步 zip 自动打包），☰ 菜单新增「收藏」页签（6 页签网格）。接口 POST /favorite、GET /favorites、POST /favorites/delete（server_app 复用 routes 自动生效）。测试：tests/test_quote_favorite.py 37 项 + Playwright `.tmp_test/verify_longpress.py` 13 项全过（含「重复长按菜单唯一」断言）。**踩坑记录**：① `_showMsgMenu` 漏 `_msgMenu = menu` 赋值导致菜单叠加（用户 PC 复现），已修；② Playwright `mouse.down()` 不派发 pointer 事件，需 dispatchEvent 合成 PointerEvent；③ 首次引导教程会中途 showHome 干扰浏览器测试，需 init_script 预置 guide done 标记；④ **浏览器自动化测试会真实触发流水线消耗用户 Key 额度并写污染进 user_data**——先临时改名 config.json 进无 Key 模式再测，测完恢复；用户数据已截断恢复（seq 119 止）。文档：docs/消息引用与收藏.md。未部署服务器、未发版、未装机（安卓侧用户自行处理）。
- **管理后台一键进入（2026-08-17 完成，链路实测）**：`_harden/`（gitignore）新增 `启动管理后台.cmd` + `admin_launcher.ps1` + `admin_proxy.py`——双击自动 SSH 隧道（8766）→ 本地令牌注入代理（127.0.0.1:8768）→ 打开浏览器直达管理台，免输令牌。**关键根因发现**：生产 AdminHandler 对 admin.html 页面本身也校验 X-Admin-Token（`do_GET` 全体 `_authed()`），浏览器导航不带自定义头 → 页面 403，这就是「本地无法进入管理界面」的直接原因；API 带令牌头是通的。方案：代理对所有转发请求附加 X-Admin-Token 头 + HTML 注入 localStorage 双保险，服务器零改动、直连 8766 仍 fail-closed（实测 403）。全链路实测：无头浏览器经代理加载生产 admin.html → 用户列表/请求面板正常渲染、无 JS 错误；真实令牌（_harden/admin_token.txt）有效。服务器侧若想支持直连免代理，可考虑后续把页面路径（/ /admin /admin.html）从令牌校验中放行（仅要求本机来源）——待用户拍板，未动生产。
- **表情包缩减逻辑 + 服务器安全加固（2026-08-16 深夜）**：
  - **表情包**：`tests/test_sticker_picker.py` 原在 Windows GBK 控制台打印 ✓ 崩溃，且旧测试没覆盖 enabled 逻辑——已修 stdout + 新增“50 项/启用35/停用15”回归与 `_migrate_enabled_defaults()` 迁移测试（107/0）；`test_sticker_upload.py` 14/0。
  - **模型锁**：`server_app.py` 在 import app_config 前设 `FIREFLY_SERVER=1`；`_load_config`/`save_config` 服务器版四模型强制 flash；`set_config` 服务器分支忽略 pro；proxy 模式无论是否注册配额钩子都走 `QuotaClient`（每次 create 强制 `model=deepseek-v4-flash`）。新增 `tests/test_server_model_lock.py` 12/0。
  - **配额展示**：管理台额度卡改为“今日对话额度（约）/每日上限”，另加“今日托管请求/请求上限”卡，并在用户列表注册时间下方加共享池剩余小进度条；当前 = 5000 次请求/天 ÷ 每轮约 4 次 ≈ **1250 轮对话/天**（`FIREFLY_PROXY_REQS_PER_CONV=4` 可调）。按用户要求**不做单账号防刷**（proxy_usage 保留 user_id 记账维度，需要时再启用）。
  - **审计修复**：会话 key 加入用户作用域（同 session_id 不再跨账号串上下文）；服务器版用户表情图片落到 `user_data/{uid}/stickers/` 且 `resolve_asset` 按用户目录解析；zip 导入拒绝 Windows 盘符绝对路径；reset-send 对未注册/发送失败统一返回 ok（防邮箱枚举）；服务器版不再把 env 兜底 Key 写入 config，`/config` 不再返回 key_prefix；`_read_body`/`_read_json` 类型校验；表情包 label/file、更新版本号/URL 等 innerHTML 全部 `escapeHtml`。**遗留风险（待办）**：服务器 `/set-config` 仍写全局共享配置（模型已被 flash 锁，但 proactive 等设置仍全局）；聊天文本进入提示词属角色扮演预期，无跨用户影响；以上服务器代码尚未部署到生产。
- **设定纠错助手 v1.2（2026-08-16 完成并装机）**：按用户反馈补齐/修复——① 顶部新增**状态点+状态文字**（空闲=状态正常/对齐中/已对齐/方案待确认/处理中/出错）；② 模式切换下新增**「最近聊天记录」可折叠面板**（复用 `/history?limit=20&mode=`，展示对应模式最近 20 条，带刷新；真机展开后聊天区仍不溢出）；③ **修复 AI 回复不刷新 bug**：`sendFixMessage` 等操作在 `_fixBusy=true` 时调用 `loadFixStatus()` 被其 `if(_fixBusy)return` 吞掉，改成 `loadFixStatus(force)` 并在消息/开始/应用/放弃/回滚/清空后强制刷新；④ **清空入口去重**：删除头部 ↺，只留修正记录右上「清空当前问题」；⑤ 提案区加「修改清单（尚未生效）/ 待确认」标题；⑥ 详细引导改为 **13 步“实际操作教程”**（不再只靠“下一步”阅读）：每步高亮目标 + 等待用户真的点击对应功能（点设置→点 Key 输入框→点开主动消息/模型/数据→点聊天页 ☰→点表情包页签→点映射表→点添加表单→点纠错入口→展开最近聊天→点输入框→点反馈），操作成功自动进入下一步，气泡有「没反应？下一步」兜底；目标在下半屏时气泡自动移到顶部避免遮挡；纠错步骤打开页面时不再自动弹键盘。基础引导同样改为 4 步实际操作。**补丁1**：从反馈页点教程按钮时，`_guideEnsureHome()` 必须先 `closeFeedback()` 再 `showHome()`，否则反馈面板 z58 仍盖在首页上（真机复现后修复）。**补丁2**：教程只变暗不拦截的问题——新增四块透明 `.guide-block`（top/bottom/left/right）在高亮目标四周盖住其余区域（pointer-events:auto），目标处开洞可点、其余按钮真的点不到；Playwright `elementFromPoint` + 真机误点验证通过。已装机验证布局；Playwright 模拟消息流确认 AI 回复立即出现。
- **表情包启用开关 + 默认集合（2026-08-15 完成，已装真机验证）**：用户通过 `tools/sticker_selector.py`（本地 127.0.0.1:8767 选择器，已用 vision 验证图片正常）选定默认集合并细化描述词，保存进 `app/assets/stickers/registry.json`（备份 registry.bak-20260815-192648.json）。当前 **50 项：默认启用 35 / 停用 15**。代码：`sticker_picker.enabled` + `_migrate_enabled_defaults()`（老安装自动合并 bundled 默认集合/描述词，且补齐新增条目）；组织器与聊天面板只取启用项（`GET /stickers?enabled=1`）；菜单→表情包管理页从列表改为**手机端卡片网格**（大图预览 + 启用中/已停用开关 + 分类 + 详细描述词 + 保存/删除，vision 验收无重叠/裁切；2026-08-15 深夜进一步改为手机单列、≥460px 两列），新增描述词上限 120 字。APK 已重建装机：`/stickers` all=50、enabled=35；停用项（嘴炮/车票等）不会进组织器清单。全量单测 18/18。
- **前端显示漂移修复（2026-08-15 深夜完成，已重装真机 + vision 明暗两主题验收）**：用户反馈菜单按钮漂移、表情包映射表按钮不适配、设置页右侧文字被隐藏。根因与修复：① `.settings-body button{width:100%}` 把「保存 Key」按钮撑满，导致旁边提示文字被挤到右边缘逐字竖排——改 `.set-save-row #key-save{width:auto;flex:0 0 auto}`；② 菜单操作排改为 4 等分网格 + 图标上/文字下（不再被图标挤出按钮）；③ 表情包映射表改为手机单列、≥460px 两列，卡片左缩略图+启用开关、右侧分类/描述/保存·删各占整行（**踩坑追加**：`.menu-content button{width:100%}` 会继承到卡片内保存/删按钮，导致删按钮 stretch 到整行红框、保存被压成 20px 文字溢出——必须给 `.stk-save/.stk-del` 显式 `width:auto`，已用 Playwright 计算样式确认 save=184.65px / del=48px 并装真机 vision 复核）；④ 接口地址下拉选项缩短为「DeepSeek 官方 / OpenCode Go」并把说明移到下方；⑤ `--panel-solid` 改完全不透明，杜绝下层聊天内容透出。已重装 704c2441 并用 vision 逐页验收（亮/暗主题菜单、映射表、设置高级区域均无溢出）。
- **设置页体验优化 P0+P1（2026-08-15 实施，待真机验收）**：用户拍板“主动消息全开但大幅降频 + 设置页分组重构 + 模型两档预设”。已做：① 默认值 `proactive_hard 4→6`、`soft 0.5→0.35`、`prob 0.3→0.10`（隐藏式保持开启，随概率整体降频）；② **修复概率式 30%×10s 轮询≈空闲 1 分钟内必触发**——`prob_gate_open` 增加 10 分钟静默窗（`_PROB_LAST_CHECK`）；③ 设置页改为五组卡片，默认只展开「账号与连接」，主动消息自然语言化（较少/偶尔/经常），模型区「快速/更强」两档 + 自定义折叠，隐藏式仅安卓壳显示，low 思考档从 UI 移除（后端兼容）；④ 非 Key 设置改动 400ms 防抖自动保存，Key 仍显式保存；⑤ 旧 config.json 当前值已按新默认更新。全量单测 18/18（proactive 82 项）通过、check_version/sync --check PASS。方案见 `docs/设置页体验优化方案.md`。
- **设定纠错助手（2026-08-15 实施，2026-08-16 v1.2 已装机验证）**：按用户拍板方向**作废旧 👍/👎 反馈与 harness_rules 路线**，改为“用户描述问题 → AI 多轮对齐（聊天+选择题）→ 点「开始修改」→ AI 生成六文件修改清单（不生效）→ 用户点「应用」才原子写入，可回滚”。设计文档 `docs/设定纠错助手设计.md` v1.2。代码：`app/modules/setting_fix.py`（对齐 Agent + 提案 Agent + 静态校验，核心三文件只 replace，用户设定/memory/手账可 append，haruno world/plot 只作只读上下文）、`setting_fix_store.py`（`{mode}/.setting_fix/` conversation/pending/manifest/audit/backups vN）、`routes.py` `/setting-fix/{status,message,start,apply,dismiss,rollback,reset}`；删除 `prompt_gate.py`/`prompt_registry.py`/`harness_rules.md`/`test_feedback.py`/`test_harness.py` 及 `/feedback` `/chat/reroll` `/prompt-*`；polisher/analyzer 已移除 `{harness_rules}` 注入。前端：首页 `#fix-module` 入口卡片 + **独立全屏页 `#fix-view`**（v1.2：状态点/最近聊天记录面板/去重清空入口/强制刷新）；反馈页只留 GitHub/QQ/邮箱；新增**纯代码引导**：基础 4 步（`firefly_guide_v1_done`）+ 结束时可点「深入了解」进入 8 步详细教程（设置五分组/主动消息/模型/数据/菜单页签/纠错助手/反馈，`firefly_deep_guide_v1_done`），反馈页可随时重开（v1.2 起点按钮必定重开，不再被完成标记挡住）。验证：全量单测 **18/18 通过**、check_version PASS、sync --check 一致、本地 HTTP 冒烟 `/setting-fix/status` 200、旧端点 404。**注意**：已随 v0.8.0 部署服务器并推送 Git；后续服务器改动单独维护（server/ 目录 gitignored）。
- **提示词时间线统一（2026-08-14 深夜完成）**：接续本日早些时候未收尾的「提示词动刀」工作——流萤状态从「重伤医疗舱恢复初期」推进为「**恢复得不错、能重新开机甲活动（依然以机甲为主，不能长时间脱离装甲待在外面）**」。三层已对齐：polisher/analyzer/proactive 模板、bundled 与 user_data 的 core/identity/sms_samples（user_data 副本已同步）、知识库 knowledge/story/firefly.md 当前状态。测试全绿（polisher 14/analyzer 16/proactive 81+68/retriever 12/orchestrator 17/organizer 13/llm_base 全过）；本地 git 提交 **5bda55d**（未 push）。提示词动刀方案与实现设计已于 2026-08-16 归档至 `封装（没指定就不读）/2026-08-16_归档/docs/`。
- **反馈模块定稿 + We-need 思维链（2026-08-15，提交 d965b2d 未 push）**：按用户真机反馈重构——反馈从聊天页移入「✉ 反馈页」独立模块（纯反馈收集供开发者/AI 参考）；列表**按对话轮数分组、最多 10 轮、默认收起（手风琴）、最新轮在最上、轮标签带模式名**、行可展开全文；👍/👎+标签理由按消息 seq 定位，后端幂等去重 + 反馈队列上限 500 条；“换一条”前端入口与“行为改进”区块已移除（后端端点保留，审批入口位置待定）。**思维链前缀**（A/B 实测后定稿）：polisher 两个模板最开头三行 `You are a helpful Firefly assistant.` + `Bootstrap with shell/read...tool catalog...` + `When you think, think in ENGLISH, start with "We need..."`（完整三行结构 + Firefly 身份 3/3 触发英文 We need；单换角色或单加身份句都失效，description 句是关键变量）；analyzer 开头只加 We-need 行（JSON 稳定）。8 轮完整流水线实测评测：用户判定**整体更好**。**遗留观察**：实测对话中“周六看电影”被回成“今天”——已澄清**非 bug**（测试当天 2026-08-15 恰为周六，模型按环境推断“周六=今天”属正确行为；真人当天也不会说“周六”）；“想看哪部”跨轮重复一次，待观察；服务器未部署新代码；本地 git 未 push。
- **harness 实施 P0–P2a（2026-08-14 深夜，提交 4bf07a5 未 push）**：方案 v2（批准制+安全三闸+召回式回滚+用户行为修正，提交 acfc0af）与实现设计文档已定稿。代码：① P0 槽位注入点（polisher/analyzer 模板 `{harness_rules}`，core 之后固定位，空文件零影响，proactive 复用）；② P1 反馈闭环（气泡 👍/👎/换一条 + 标签理由弹层；`POST /feedback` 落 feedback.jsonl 含上下文快照；`POST /chat/reroll` 复用 undo+全流水线重跑并落 preference.jsonl）；③ P2a 安全闸（prompt_gate 静态校验：元指令黑名单/引文出处/事实冲突/预算；prompt_registry pending→apply→rollback→dismiss 版本化；批准 API + 反馈页「✨ 流萤 · 行为改进」入口——入口定稿在反馈页，不放设置面板）。前端三副本已同步。测试：新增 test_feedback 12/12、test_harness 23/23，回归全绿。**待办**：P2b 优化器脚本（归因→候选→双盲 judge，依赖回归样本集）、P3 服务器管理端点/灰度、真机联调；4 个初定参数待用户拍板（批次触发/通过线/召回阈值/样本集）；设置面板减负另行讨论（未动）。
- **上会话清理任务核实（2026-08-14 深夜）**：上个会话报告的清理已确认完成——tools/ 恢复为原有 8 工具、docs/搜索资料结果/ 保留 15 份研究（含 流萤剧情精读研究_确认版.md），14 个垃圾文件与被否定的 2 份文档无残留；被删文件均为未跟踪文件，git 无 D 变更，无需补提交。

- **DSH 桌面壳**（已完成）：`F:\DeepSeek harness\dsh-gui\` —— DeepSeekHarness.exe 单文件壳 + 黑虎鲸图标 + update_dsh.ps1 一键更新 + 桌面快捷方式。壳子动态定位 harness、固定 `node lib/bin.js web` 启动，DSH 升级不用重打包。
- **手机远程访问 DSH**（已完成）：自研安卓壳 `F:\DeepSeek harness\dsh-mobile\`（内置 JSch SSH 隧道 + WebView，点开即连，已装真机验证）。链路：手机 App → SSH 隧道 → 云服务器 127.0.0.1:9090 → 电脑反向隧道（任务计划「DSH Reverse Tunnel」，脚本 dsh-gui/dsh_tunnel.ps1）→ 电脑 DSH 3080。手机密钥 dsh_mobile_rsa（RSA PEM，ed25519 与 JSch 不兼容踩过坑）。注意：官方禁用 `--host 0.0.0.0`（防远程代码执行暴露），勿绕过。
- **识图技能 vision**（已装为 DSH skill）：`C:\Users\FANGL\.agents\skills\vision\`（SKILL.md + vision.py）。命令行直调识图：`python "...\vision\vision.py" "<图片路径>" "<问题>"`，走 opencode go 套餐付费 **mimo-v2.5**（免费版 mimo-v2.5-free 次数少）。Key 内置脚本。这是本会话的"眼睛"（模型不支持图像输入时全靠它）。
- **个人预设 firefly（2026-08-15 创建）**：`C:\Users\FANGL\.dsh\.agent-presets\firefly\`，基于 standard 复制，显示名「流萤·标准模式」。改动：`agent.cordis.yml` 的 skill-filesystem 加了 `customSkillDirs` 指向预设自带 `skills/`，vision 技能随预设走（不依赖用户根）；persona 加了识图提示；`preset.yml` 描述已注明识图。**注意：cordis 预设（创造模式）的 tool-cordis 注册进程全局 Inspect Provider，同一 DSH 进程内 cordis 系预设互斥**——firefly 刻意不含 tool-cordis，与 cordis 共存；需要动态改自己时用 cordis 预设。
- **全局识图指令（2026-08-15 创建）**：`C:\Users\FANGL\.dsh\AGENTS.md` 是 DSH 用户全局指令文件，**自动注入所有预设、所有项目的所有会话**（优先级低于项目 CLAUDE.md）。内容：要求模型涉及图片任务时**主动直调 vision 技能**（`python "...\vision\vision.py" "<图>" "<问题>"`），无需用户提醒。这是「跨项目跨会话自然调用识图」的机制载体，已实测本会话立即注入生效。
- **手机端 DSH UI**（最终方案：只 viewport，不注入 CSS）：dsh-mobile 的 MainActivity 里只加 useWideViewPort/loadWithOverviewMode（解决文字重叠）。**重要教训：任何 CSS 注入（侧边栏折叠/图标统一/按钮省略/16px）都会在"交互时"破坏 DSH 前端布局（用户反馈"使用的时候乱"），已全部去掉。** 盲写 CSS 不可行——DSH 前端是 React+CSS Modules（类名哈希 `pI_x6G_sidebarCol`）+ 疑似 Shadow DOM，无法可靠读 DOM 精确适配；静态截图看着对，交互就崩。若要真正做移动端布局，需要 DSH 前端原生支持或彻底读清 DOM 结构，不要靠盲写 CSS 打补丁。CDP 读手机 WebView DOM 会 403（origin 限制），改用 JS console.log + WebChromeClient 桥到 logcat。
- **安全加固阶段一（已完成，2026-08-14）**：链路「手机→服务器→PC」评估发现手机私钥（APK 内置）原可直接 root 登服务器，已修复——详见 [docs/安全加固执行记录.md](安全加固执行记录.md)。要点：① 新建受限账号 `dsh-tunnel`（nologin，key 限制 no-pty/command=/bin/false），手机 USER 已改并重装；root 的 authorized_keys 已删 dsh-mobile，root 仅剩 PC 主 key（C:\Users\FANGL\.ssh\id_rsa）；② 8766 管理后台加令牌双因子（/opt/firefly/.admin_token，本地副本 _harden/admin_token.txt，admin.html 弹窗输入）；③ 明文 root 密码等危险文件移入 /root/harden-backup/；④ X11Forwarding 关闭。**阶段二待议**：9090 DSH 入口仍无鉴权（拿到 dsh-mobile 私钥仍可 -L 进 DSH 指挥开发机），方案 A 令牌代理 / B 手机改 HTTPS 直连；另建议阿里云账号开 MFA（云助手 aliyun-assist 以 root 运行）。运维材料：_harden/ 目录存私钥副本、token、加固脚本（幂等可重跑）。
- **用户日常手机维护（2026-08-14 完成）**：用户日常机 = **OPPO A92s**（PDKM00，Android 12 / ColorOS 12.1，天玑800，8GB，adb 序列号 `7HJ7VSE6WSZ959GA`，与开发测试机 704c2441 不同）。本次：只读体检 → 内存紧张（8GB 已用 5.5GB、zram 4GB 已用一半、微信 PSS 584MB+被换出 298MB 为最大户、开机 10 天碎片 795MB）→ 按用户要求卸载 45 项 OPPO/heytap 增值全家桶（`pm uninstall --user 0` 可恢复；主题商店+软件商店被 ColorOS 系统保护无法卸载/停用）→ 可用内存 1.97GB→3.17GB；用户自行手动重启收尾。**经验**：该机默认 USB 只暴露 MIDI 接口、adb 看不到设备，须先开开发者选项+USB 调试并授权；本机 SDK adb 在 `F:\Android_SDKset\platform-tools\adb.exe`（微信开发者工具自带 adb v39 会杀 v41 服务端，避免混用）；ColorOS 12 的 `pm disable-user` 对受保护应用无效（弹回 default）。

## 六、2026-08-14 服务器版 0.7.2 体检 + 修复（已部署）

- 体检结论：8 处版本号一致；服务器双服务 active；部署代码与仓库 md5 一致；15 个单测脚本全绿。
- **8 个 bug 已全部修复并部署生产**（备份在 /opt/backups/user_data_pre_quota_20260814-1117.tar.gz）：
  ① CORS Allow-Headers 补 X-API-Mode（8787 网关已实测含该头，托管模式 APP 内可用）；② 托管配额强制（proxy_usage 表 + 全站每日池 5000 次 ≈ 15w÷30 + 每用户每日 200 次，admin quota 字段=用户每日上限，admin 页显示今日用量）；③ 无 Key 立即 need_key（不再 4 分钟 relay 超时）；④ relay 错误分类（401/402/429→人话提示）；⑤ 表情包 registry 按用户隔离（旧全局池=只读公共贴纸，新上传只进自己目录）；⑥ 服务器禁用 /set-key、/update-download、/set-config 剥离 api_key；⑦ index.html 双份已同步（粒子场）；⑧ check_version.py GBK 修复。
- 服务器版 APK 已重建（android_server/firefly-server.apk，versionCode=2 未变）；**体积 0.6MB→8.9MB**：StarRailFont.ttf(12MB)+background.jpg 等资产随包打入（符合"资源本地化省流量"方向；下载页"0.6MB"文案待更新）。
- **2026-08-14 追加**：防刷（每用户每日上限）已按用户要求撤销并部署（只保留全站每日池 5000 次；proxy_usage 表保留 user_id 维度，将来恢复防刷不用改表；admin 台 quota 输入框/端点/函数已删）。
- **方向（用户已拍板的偏好）**：通用一套前端；一个安卓 APP 默认完全本地、可开「服务器后端处理」（内含「使用服务器托管额度池」按钮）；砍掉独立本地安卓 APP，三版本 → 两版本（安卓合并版 + Windows 本地版）；60MB 体积可接受；服务器数据与本地数据可同步（切换后重启加载数据）。**实施时机：AI 建议现在做、分阶段（A 数据同步三件套 → B 前端合一+壳改造+删 android_server → C 模式切换与全量回归 0.8.0），等用户最终拍板。**
- 待用户拍板：① 双模式整合开工（阶段 A 可立即做）；② 配额池参数 5000 是否合适；③ 发布：新 APK 是否上传 Gitee/服务器下载通道；下载页 0.6MB 文案；④ 本地版 APK/PC exe 重建时机；⑤ git 本地提交（工作区含此前多会话未提交改动，未代提交）。

## 七、2026-08-14 双模式整合 0.8.0（已实施并验收，等发布拍板）

- **已完成**：① 数据同步三件套（`POST /import-data` zip 覆盖导入+自动备份+zip slip 防御、`POST /sync/upload` 账号云端备份留 3 份、`GET /sync/download` 取回）；② 前端合一（`app/static` 唯一源 + `window.FIREFLY_MODE` 分支；登录/注册/重置表单**内联进首页**——不再跳 login.html；relay/登录模块/API 来源/Key/更新源/导出按模式开关；401 toast 仅对 /chat 提示）；③ 安卓壳合并（SharedPreferences `firefly_mode` 模式开关 + FireflyMode JS 桥（切换后 finishAffinity+killProcess 重启）+ Chaquopy 按需启动 + config.js 拦截注入 + onShowFileChooser 导入 + shouldOverrideUrlLoading file:// 导航 post 处理；KeepAliveService 按模式分流）；`android_server/` 已删除；`tools/sync_frontends.py` 自动同步（app/static → server/frontend + 安卓 assets，`--check` 校验）；④ 版本 0.8.0（7 处一致，versionCode 800）。
- **真机验收（704c2441，vision 识图）**：本地模式首页/设置（模式开关显示、API 来源隐藏、导入导出按钮、v0.8.0）✓；服务器模式首页登录卡片、设置（API 来源+账号备份/恢复按钮）✓；模式切换（select→confirm→壳重启→服务器模式）✓；**内联登录表单真机展开成功**（QQ 邮箱/密码/登录按钮）✓。**待用户手测**：注册/登录/重置提交（需真实 QQ 邮箱）、托管额度链路、导入文件选择器。
- **「崩溃」根因与修复**：用户观察到的「应用内页面内容消失只剩背景 + 请先登录后使用 toast 反复弹出」——服务器日志零错误，根因在客户端：file:// 页面跳转 login.html 被 allowFileAccess=false 拦截/导航竞态破坏渲染帧；401 toast 对后台轮询（10s 一次）无差别弹出刷屏。修复：登录表单内联（单页不跳转）+ toast 仅对 /chat 提示。**已装机验证无复现。**
- **追加修复（2026-08-14 下午）：聊天页「内容消失」的第二根因**——用户报告剧情模式/春日手信进入后同样症状。根因：合并引入的粒子场 `<canvas id="firefly-field">` 在 index.html/style.css 里**没有任何定位规则**（login.html 有 `position:fixed;inset:0` 而 index.html 漏了），canvas 被 JS 撑成全屏后作为**流内元素**占满一屏高度，把流内定位的聊天容器 `#app`（position:relative）推出视口——首页 `#home-view` 是 fixed 不受影响，所以只有聊天页「只剩背景」。修复：style.css 给 `#firefly-field` 加 `position:fixed;inset:0;z-index:0;pointer-events:none`。已重建 APK 装机验证：剧情模式（历史消息+输入框完整渲染）与春日手信（旁白+开场+输入框）均正常；已部署服务器。
- **部署状态**：服务器已再部署（备份 user_data_pre_080_20260814-1430.tar.gz，firefly-server active，新前端 grep 确认在位）；PC exe 已构建（dist/firefly/firefly.exe 3.4MB）；合并版 APK 已构建装机（app-release.apk，versionCode 800）；单测 15/15、sync --check 一致、check_version PASS。
- **待用户拍板**：① 真机手测登录/注册（内联表单）；② 服务器全局 `/set-config` 的 per-user 化（当前模型已锁 flash，proactive 等仍全局）；③ 未跟踪的研究草稿文件（bwiki_* / moegirl / gitbook_content 等）是否保留或清理。

## 八、2026-08-16 发布 v0.8.0（GitHub + Gitee 已上线，服务器已部署）

- 版本 0.8.0 / versionCode 805；`check_version.py`、`sync_frontends.py --check`、全部单元测试通过。
- 资产：GitHub release v0.8.0（firefly.apk 60.4MB + firefly-setup.exe 24.0MB）；Gitee release v0.8.0（release id 799961，附件 id 3050357 / 3050358）。
- 下载小技巧沿用：下载页主按钮 `resolve` Gitee foruda 直链（前端 fetch blob + `a.download` 强制 .apk，规避 Gitee 把 APK 按 zip MIME 下发）；服务器 `/opt/firefly-downloads` 的大文件已移入 `local_bak_20260816`，`/download/firefly.apk` 与 `firefly-setup.exe` 现在 302 到 Gitee foruda，服务器只跑 resolve/zip 处理脚本；`FALLBACK_TAG=v0.8.0`。
- 服务器已部署：`app/`、`knowledge/`、`server/` 全部更新，`firefly-server` 与 `firefly-downloads` active；公网 `version.json` tag=0.8.0；`/download/resolve?file=firefly.apk` 实测返回 foruda.gitee.com 直链。
- git：master 已推 GitHub（origin，SSH 推送）与 Gitee；tag v0.8.0 双端已推。GitHub 提交 8aaa95d。
- 提交审查：恢复被外部内容覆盖的 `readme.md` 与被误删的 `CLAUDE.md`；未提交任何 user_data/配置/备份/研究草稿（`.eval`、`.tmp_test`、bwiki_*、gitbook_content.json、registry.bak 等仍为未跟踪文件）。
- **部署约定已定稿**：`docs/部署与发布约定.md`——提交≠发版（无明确指示不 bump 用户可见版本）；下载 Gitee 优先、服务器兜底；部署后必须汇报 resolve/更新优先级/数据安全/推送结果四关键点。
- **项目整理**：过时计划（0.8.0 检查点、提示词动刀方案/实现设计、三版本缺陷分析执行文档）与无研究草稿已归档到 `封装（没指定就不读）/2026-08-16_归档/`（已 ignore，不进 git）；`docs/协作记忆.md` 引用已更新。

## 八、2026-08-14 发布 v0.7.2（GitHub + Gitee 已上线）

- **版本回归 0.7.2**（双模式整合沿用此版本；versionCode 801 保覆盖升级；check_version 映射规则改为递增校验）。
- **git 历史清洗**（用户明确要求"别把服务器后端代码/本地隐私交过去"）：发现本地与远端历史均含 server/（旧误 push）；已用 git-filter-repo 删除 server/ 与 _trash/ 全部历史痕迹（77 commits 重写），v0.6.0 tag 经 commit-map 重建到清洗版提交，两端 master + 全部 tags 强制更新；验证 GitHub/Gitee 远端树均无 server/。**备份**：`F:\CodeFile\firefly_backup_pre_filter_20260814.bundle`（清洗前全量）。
- **发布**：GitHub release v0.7.2（assets: firefly.apk 60MB + firefly-setup.exe 23.9MB）；Gitee release v0.7.2（id 796794，同名附件）；两端 tag v0.7.2。
- **下载路由**：download_server.py FALLBACK_TAG → v0.7.2、白名单删除 firefly-server.apk（实测 /download/firefly.apk 302 → Gitee v0.7.2 资产、server.apk 404）；version.json（tag 0.7.2、apk 链接 firefly.apk）已部署；双服务 active。
- **本机工具**：Inno Setup 7.1.0 已装（C:\Program Files\Inno Setup 7，经 127.0.0.1:7897 代理下载 GitHub 资产）；downloads/innosetup-*.exe 安装器留档。
- **装机验证**：手机 com.firefly.android = 0.7.2/801，登录态保持（1280728905@qq.com），首页正常。
- 提醒：GitHub/Gitee 历史中的 server/ blob 即使重写历史，GitHub 端旧对象可能延迟 GC（可联系 GitHub 支持彻底清除）；服务端源码无明文凭据（SMTP 走环境变量），逻辑泄露风险有限但已知。
- **2026-08-14 追加发布（ffac186）**：① 按用户要求**移除聊天气泡主题功能**（菜单入口 + 6 套主题 + 选择网格样式全删，消息气泡默认样式保留）；② **提示词防编造/防重复补强** 5 处：memory_manager 整理铁律（只记真实发生的事、不记推断）、polisher story/haruno「引用记忆不越界」（找不到原文不引用）、proactive「引用前先定位」。APK versionCode 803、PC exe+安装器重打、服务器已部署新提示词、两端 release 资产已全部更新。

## 四、本次会话用户明确确认的约定

1. 建立「协作记忆」机制（本文件 + CLAUDE.md 索引 + 会话结束前主动更新）。
2. 模型分工：默认 flash 执行、规划/系统级错误切 pro（AI 提示、用户切换、同会话继续）。
3. 三版本 APP 缺陷分析：以小白视角写执行文档，设计缺陷/工程缺陷/取舍代价三类区分（已完成）。
4. 执行文档中能独立完成的改动已实施（7 项 ✅；文档已归档至 `封装（没指定就不读）/2026-08-16_归档/docs/`）。
