# 服务器版生产环境审计记录（只读）

> 审计对象：生产服务器 `101.200.14.126`（hostname `iZ2zedbksqxvyh37gnhmdiZ`）
> 审计时间：2026-09-10 21:17 CST
> 审计身份：root（经 `C:\Users\FANGL\.ssh\id_rsa` 登录）
> **合规声明：全程只读。未修改代码/配置/数据库/用户数据/systemd/权限；未导出、未打印任何用户原文、对话内容、角色卡内容、密钥、token；所有用户数据相关输出仅为聚合计数、路径存在性、权限、大小、版本号。**

---

## 0. 连接与访问路径

| 项目 | 结果 |
|---|---|
| SSH 客户端 | `C:\Git\usr\bin\ssh.exe`（Git Bash 版） |
| 首次连接失败原因 | **不是密钥问题**：Git Bash ssh 把 `userknownhostsfile` 解析为 MSYS 路径 `/home/FANGL/.ssh/known_hosts`（不存在）→ 等于无已知主机库 → `Host key verification failed` |
| 服务器实际主机密钥指纹 | ED25519 `SHA256:TjQ8GBRsHzbq390uY7H3yppJ7hisp74Ojj9zTeJS1Lk`(256) / RSA `SHA256:S9Wm5I1wbuox9P6AEpvOmW5I6mRCYkrCDNz7Zdq4nKM`(3072) / ECDSA `SHA256:94Czk16RUwj5ne5Ur+SOOSrLBHderK6swB4iSlu10NY`(256) |
| 与本地 `known_hosts` 比对 | **三条指纹完全一致** → 无中间人迹象，非密钥轮换 |
| 修复方式 | 显式传 `-o UserKnownHostsFile=C:/Users/FANGL/.ssh/known_hosts`（**未关闭验证**） |
| 命令执行方式 | 脚本 base64 经 stdin 送入 `base64 -d \| bash`（**不在服务器落盘**）；管道会带 CR，故远端加 `tr -d '\r\n'` |

---

## 1. 线上版本与部署基线

### 1.1 服务与产物

| 项目 | 实测值 |
|---|---|
| `firefly-server` | **active**（`User=www-data`，`WorkingDirectory=/opt/firefly`，`Restart=always`，`ExecStart=/opt/firefly/venv/bin/python /opt/firefly/server/server_app.py`） |
| `firefly-downloads` | **active**（`User=www-data`，`ExecStart=/usr/bin/python3 /opt/firefly-downloads/download_server.py`） |
| 监听 | `0.0.0.0:8787`（公网，下载网关）、`127.0.0.1:8765`（主服务）、`127.0.0.1:8766`（管理台）→ **拓扑与规范一致** |
| `/opt/firefly-downloads/version.json` | `"tag": "0.8.1"`，mtime **2026-08-23 22:10:45** |
| `/opt/firefly/version.json` | **缺失**（主服务不读它；`_current_tag()` 会回落 `_OLD_CLIENT_NOTICE_LATEST = "0.8.1"`） |
| 下载两件套 | `firefly.apk` 60,606,325 B（08-23 22:22:46）；`firefly-setup.exe` 26,182,586 B（08-23 22:22:45） |
| 进程 | `server_app.py` ×1、`download_server.py` ×1 |
| 服务器是否有 `.git` | **缺失** → 服务器端**无版本历史**，回滚只能靠 `/opt/backups` 与本地副本 |
| 资源水位 | 根分区 3.4G/40G（**10%**）；内存 562MB/1612MB；`user_data` 30M |

### 1.2 线上代码与 v0.8.1 tag 的逐文件比对（关键结论）

方法：服务器导出 `app/**/*.py` 的 md5sum（27 个文件）→ 与 `git cat-file blob v0.8.1:app/<file>` 的**原始字节** md5 比对；对不一致者再做**去 CR 后**的 md5 比对（scp 从 Windows 部署会把 LF 变 CRLF，属正常）。

| 分类 | 数量 | 文件 |
|---|---|---|
| 与 v0.8.1 **逐字节一致** | 15 | 含 `modules/{auth_store,context_manager,conversation_store,favorite_store,llm_base,metrics,setting_fix,setting_fix_store,storage,sync_engine,vision}.py`、`server.py`、`tools/{__init__,sticker_picker}.py` 等 |
| 仅 **CRLF 差异**（内容 = v0.8.1） | 9 | `analyzer.py`、`app_config.py`、`llm_retriever.py`、`memory_manager.py`、`organizer.py`、`polisher.py`、`proactive.py`、`orchestrator.py`、`shared_http.py`（差值均 **恰好等于行数** ⇒ 纯 LF→CRLF） |
| **真实内容偏离 v0.8.1** | **3** | **`modules/api_client.py`**、**`modules/multipart.py`**、**`routes.py`** |
| 与 HEAD 一致 | 0 / 27（HEAD 匹配率仅 9/27，且多个文件 size 差异巨大 → 线上**不是**开发版） |

**结论**：
1. 线上**不是开发版**——`bundled preset.json` 数量 **0**、`routes_pack.py`/`pack_forge.py` **缺失**、`routes.py` 中 `/pack-` 命中 **0** → **角色卡预设包架构完全没有上线**（沙箱复现的丢包风险在线上**不存在**）。
2. 线上**也不与任何 tag/commit 完全对应**：3 个文件（含承载全部 LLM 通信与代发的 `api_client.py`、承载路由与导入导出的 `routes.py`、承载请求体解析的 `multipart.py`）是**非标准版本**，与 v0.8.1、`origin/master`、HEAD 均不同（`routes.py` 去 CR 后仍比 tag 大 ~1.2KB 且 2680 行 ≠ tag 的 3737 行）。
3. 时间线：`app_config.py`/`orchestrator.py` mtime = 08-23 21:09（v0.8.1 发版日）；**`routes.py` mtime = 08-28 16:06**，与 `/opt/backups/code_pre_harden_20260828-1605.tar.gz`（08-28 16:05）**同一分钟** → 08-28 有一次"先备份代码再更新"的加固动作。
4. 路由表实测：线上仍是**单体 `routes.py`**（`app/routes*.py` 只有 1 个文件），**没有** `routes_common/routes_data/routes_pack/...` 拆分。

---

## 2. 多用户隔离风险清单

| ID | 等级 | 结论 | 证据 | 线上存在？ |
|---|---|---|---|---|
| **S-1** | **P0** | **`/pipeline` 跨账号返回全局流水线日志** | 线上 `orchestrator.py`：`_PIPELINE_LOG` 命中 6 次；`def get_pipeline_log` 在**第 258 行**；该函数体内 `user_scope_key|scope|uid` 关键词命中 **0**（**无任何用户过滤**）。条目结构（本地同源代码 `orchestrator.py:423-459`）含 `user_input`、完整回复 `messages`、`analyzer.summary/reasoning/raw_json`、`polisher.raw/reasoning`、`retriever.knowledge` | ✅ **线上确认存在** |
| **S-2** | P1 | `/requests`、`/metrics` 无用户维度 → 全站用量/成本可见 | 线上路由表**确认两个端点都在**；同源实现为全局 `_REQUEST_LOG` 与全局计数器 | ✅ **端点在线上**；跨账号内容未实测 |
| **S-3** | P1 | `_SESSION_MAX=30` 进程级全局 + `/undo`、`/clear-history` 无需 Key 即建会话 → 跨账号挤内存态 | 本地代码 `routes_common.py:29,66-68`；线上为单体 routes.py，会话上限逻辑同源 | [待确认] 未逐行核对线上单体实现 |
| **S-4** | P2→**降低** | `/assets/*` 免登录 —— 但**无用户资产可泄** | 免登录状态码实测：`/assets/character/story/assets/avatar.png` → **404**（线上无角色包资产目录）；`/assets/index` → **401**（走认证） | ✅ 机制在线上；**当前无实际泄漏面** |
| **S-5** | — | **文件面隔离成立** | `user_data` 根 `mode=755 owner=www-data:www-data`；5 个数字账号目录**权限无异常**（均非宽松于 755）；账号目录结构为 `{uid}/{story,haruno}/{character,...}` | ✅ 未发现跨账号文件读写路径 |
| **S-6** | P2 | 根部存在**非数字遗留目录**：`user_data/story/character/`（3 个文件：18461/4284/2678 B，mtime 08-12~08-16）与 `user_data/stickers/`（1 个文件） | `find -maxdepth 2`；线上 `app_config.py` 的 `_migrate_legacy_layout` 命中 2 次（该迁移以 `USER_DIR` 为目标，仅服务器全局路径，不涉及 `{uid}`） | ✅ 线上存在，但**不被任何账号读取**（每账号读自己的 `{uid}/{mode}/character/`），**非泄漏** |

**免登录可达性实测（HTTP 状态码，未取正文）**：

| 路径 | 状态码 | 判读 |
|---|---|---|
| `/health` | 200 | 设计公开 |
| `/config` | 401 | ✅ 需登录 |
| `/pipeline` | **401** | ✅ 需登录（但**登录后无用户过滤** → S-1） |
| `/requests` | **401** | ✅ 需登录（同上 → S-2） |
| `/modes` | 401 | ✅ 需登录 |
| `/assets/index` | 401 | ✅ 走认证 |
| `/assets/character/story/assets/avatar.png` | 404 | 线上无该资产路径 |

**说明：我没有、也不会用两个账号去实际读取对方的 `/pipeline` 内容**（违反"不拉取用户原文"铁律）。S-1 的判定基于**线上代码取证**（无过滤关键词命中 0）+ **端点在线确认**，已足以定级。

---

## 3. 托管与额度风险

| ID | 等级 | 结论 | 证据（生产实测） |
|---|---|---|---|
| **Q-1** | **P0（机制）** | `X-API-Mode: proxy` **仅凭请求头生效** | 线上 `server_app.py` 中 `X-API-Mode` 命中 **2 次**（判定 + 注释），与本地同源实现一致：`proxy = (headers.get("X-API-Mode","")).lower()=="proxy"` → **无账号授权校验** |
| **Q-2** | **P0（机制）** | `users.quota_api_proxy` **从未参与授权** | DB 只读聚合：**账号总数 5**、**`quota_api_proxy>0` 的账号数 = 0**、`role=admin` = 1、`status=active` = 5 → 即**全站没有任何账号被授予配额**；而代码侧该列仅出现在 schema 与 `/auth/me` 回显 |
| **Q-3** | P1 | 托管池**全站共享** | `proxy_usage` 近 3 日：**08-18 = 91 次**、08-17 = 11、08-14 = 4（**08-18 之后无记录**）→ 托管通道当前**闲置**，但机制仍在；日预算 5000 次为全站池 |
| **Q-4** | **P2（下调）** | 托管 Key 回落链**不会**消耗运营者 DeepSeek 余额 | systemd `firefly-server` 的 `Environment=` 实测键：`PYTHONUTF8`、`FIREFLY_SMTP_USER`（17）、`FIREFLY_SMTP_AUTH`（16，掩码 `pqu***`）、**`FIREFLY_PROXY_KEY`（68，掩码 `sk-***`）**；**`DEEPSEEK_API_KEY` 未设置**（`grep` 命中 0，`EnvironmentFile` 计数 0）→ 回落链第一段即命中，**不触发** `DEEPSEEK_API_KEY` 分支 |
| **Q-5** | P2 | `/config.has_key` 走 `get_api_key()`（含 env 兜底） | 因 `DEEPSEEK_API_KEY` 未设置，该泄漏面**当前不成立** |

**净判定**：Q-2 + Q-1 组合 = **任何已登录账号只要加一个请求头即可使用运营者 Key（`FIREFLY_PROXY_KEY`，OpenCode Go 套餐）**，且全站无任何账号被授予配额（`quota>0` 为 0）→ 单个账号可吃干 5000 次/日全站池，导致其他全员托管不可用。实测该通道自 08-18 起未被使用，**当前未发生滥用**，但**授权机制缺失属实**。

---

## 4. 启动迁移与共享根风险

| ID | 等级 | 结论 | 证据（生产实测） |
|---|---|---|---|
| **M-1** | P0（机制） | `_migrate_legacy_layout()` 在 **import 期**执行 → 服务器每次启动都跑 | 线上 `app_config.py` 中 `_migrate_legacy_layout` 命中 **2 次**（定义 + 顶层调用），与本地同源（本地 `app_config.py:509` 顶层调用；`server_app.py:37` 导入 app_config） |
| **M-2** | P0（机制） | `move` + `except OSError: pass` + **零备份** | 同上（本地逐行：`app_config.py:493-497`、`:501-506` 两处 `_sh.move` + 两处 `except OSError: pass`，全段无日志） |
| **M-3** | P1 | 迁移过程**无任何日志** | 本地全段无 log 调用；线上同源 |
| **M-4** | **未触发** | 共享根**当前无旧布局残留**（迁移目标均已存在，源不存在） | `user_data/character/` **缺失**、`user_data/data/` **缺失**、`user_data/story/手账.md` **缺失** → 迁移为 no-op |
| **M-5** | P2 | 但根部**残留迁移产物**：`user_data/story/character/`（3 文件，08-12~08-16）+ `user_data/stickers/`（1 文件） | 说明历史某次迁移**未清理源**（或因目标是 `{uid}` 而只对全局路径生效）；当前**不被读取**（无账号引用），属休眠垃圾 |
| **M-6** | P1 | journal 中出现**迁移相关日志 6 条**（近 200 行） | `journalctl -u firefly-server -n 200` 中「迁移/migrat」命中 6 → 启动迁移确有执行痕迹 |

---

## 5. 聊天数据包 / 角色卡风险

| ID | 等级 | 结论 | 证据（生产实测） |
|---|---|---|---|
| **D-1** | **P0** | `import_data` **先 `rmtree` 再解压，失败不回滚** | 线上 `routes.py` 非标准版本，但 `/import-data` 端点在路由表中**确认存在**；同源 v0.8.1 实现为 `v0.8.1:app/routes.py:1481-1490`（`_sh.rmtree(root)` → 逐条写 → `except: return False`） |
| **D-2** | **P0** | 导入前自动备份**失败被静默吞掉** | 同源 `v0.8.1:app/routes.py:1475-1478`：`try: _backup_current_mode(...) except Exception: pass # 备份失败不阻塞导入` |
| **D-3** | P1 | 角色卡/数据打包**按 `cfg.MODES`（写死两包）**，非按包清单 | 线上 `MODES` 已验证为写死（`bundled preset.json = 0`，无扫描机制）；`/export-data`、`/import-data` 均 `if mode not in cfg.MODES` |
| **D-4** | — | **线上无 per-user 角色包** | DB 表清单实测：`notices, proxy_usage, proxy_usage_failed, sessions, settings, sqlite_sequence, users` —— **7 张表（含 sqlite 内部表），无任何角色卡/包表**；`schema_version = 2` |
| **D-5** | — | 线上**不存在**自建包 | 5 个账号目录中 `character/preset.json` 计数 = **0**（无自建包，与服务器版禁用自建包一致） |
| **D-6** | P1 | **前端无导出入口** → 用户无法自助备份 | 前端属 `app/static`（线上同源）；`/export-data` 与 `/backup/*` 端点在线上**存在但前端不调用**（本地全前端 grep 仅命中一处冲突提示文案） |
| **D-7** | P1 | 自建包快照恢复丢弃（**沙箱复现**） | **仅开发版**；线上无快照端点（`/snapshot/*` 不在线上路由表）→ **线上无此风险** |

**聊天数据现状（仅聚合计数）**：5 个账号的 `conversation.jsonl` 行数分别为 **4 / 2703 / 16 / 5 / 470**；其中 1 个账号为主力（2703 行，9 月后修改 69 个文件）。→ **存在真实用户与真实对话数据**，这使 S-1 的实际影响不可忽略。

---

## 6. 备份与回滚能力

| 能力 | 实测结果 | 评级 |
|---|---|---|
| 每日全量备份 | crontab：`0 3 * * * tar -czf /opt/backups/user_data_$(date +%Y%m%d).tar.gz -C /opt/firefly user_data && find /opt/backups -name "user_data_*.tar.gz" -mtime +7 -delete` → **保留 7 天** | ✅ 有效 |
| `/opt/backups` 现状 | 11 个文件、46,929,892 B（≈44.8 MB）；最近 8 份为 `user_data_20260903..20260910.tar.gz`，体积 **3.3MB → 7.6MB 单调增长**（日增 ≈1MB，与主力账号活跃度吻合） | ✅ 但**增长趋势需盯** |
| 代码备份 | `code_pre_harden_20260828-1605.tar.gz`（10.4 MB）—— 08-28 加固前的**整份代码快照**；另有 `firefly.db.before_delete_user2_20260815_214248` 与 `firefly_user2.tar.before_delete_...`（08-15 删用户前的备份，**规范执行**） | ✅ 有一次性的加固前代码备份 |
| **单账号 `backups/`** | `find user_data -maxdepth 3 -type d -name backups` → **0** → 线上**从未有任何账号执行过本地备份**（与 D-6 前端无入口一致） | ⚠️ 用户侧**零自助备份** |
| **快照 `snapshots/`** | **0 个**（线上无快照功能） | — |
| **服务器代码版本历史** | `/opt/firefly/.git` **缺失**；`git ls-files server` = 0 → **无版本历史** | ❌ **回滚只能靠 `/opt/backups` + 本地副本** |
| **发布级回滚流程** | 5 份发布/部署文档全文 grep「回滚」= **0 命中**；无 tag 撤回 / release 删除 / 资产替换步骤；`download_server.py:204 FALLBACK_TAG = "v0.8.1"` 为**人工同步常量** | ❌ 缺失 |
| **灰度 / feature flag / 强制升级** | 全仓 grep 各 **0 命中**；唯一软闸门是 `_OLD_CLIENT_PATHS`（2 个端点）+ 一次性升级邮件 | ❌ 缺失 |
| 误操作救火 SOP | 未在文档中发现（`/opt/backups` 有数据，但无恢复步骤文档） | [待确认] |

**回滚能力净判定**：**数据侧可用（每日 3:00 全量、留 7 天），代码侧薄弱（无 git、无流程、无灰度）**。最坏情形（08-28 那类代码更新引入回归）只能靠 `code_pre_harden_20260828-1605.tar.gz` 这**一份**人工备份。

---

## 7. 待确认清单

| # | 待确认 | 影响 |
|---|---|---|
| C1 | 线上 `api_client.py` / `multipart.py` / `routes.py` 三个非标准文件的**来源与改动内容**（既非 tag 也非 HEAD） | 决定"仓库审计结论能否外推到线上"；这 3 个文件承载 LLM 通信、请求解析、全部路由 |
| C2 | 线上 `routes.py` 的 `/pipeline` handler 是否与 tag 同源（我只确认端点存在 + `orchestrator` 无过滤） | 进一步固话 S-1 |
| C3 | 08-28 那次 `routes.py` 更新（16:06）具体改了什么 | 需与 `code_pre_harden_20260828-1605.tar.gz` 对比 |
| C4 | journal 中 18×`AttributeError` / 17×`Traceback` 是否已解决 | 时间戳集中在 **08-28 13:40**，此后无新 Traceback → 疑似已修复，但未确认根因 |
| C5 | `/opt/backups` 的 7 天保留 + 日增 1MB：主力账号继续增长后的容量规划 | 当前 40G 用 10%，短期无风险 |
| C6 | 是否有"误导入/误删"救火 SOP | 决定 D-1 的实际损失 |
| C7 | `user_data/story/character/` 3 个遗留文件的处置意愿 | 休眠垃圾，可清理但非必须 |
| C8 | 管理台 `8766` 的 admin token 文件权限 | 未检查（避免触碰敏感面） |

---

## 8. 发布前必须修复项（按线上证据排序）

### 立即（线上活跃 · 与开发版发布无关）

| 序 | 项 | 线上证据 | 建议动作 |
|---|---|---|---|
| **1** | **S-1 `/pipeline` 跨账号内容泄漏（P0）** | 线上 `orchestrator.py:258` 的函数体内**过滤关键词命中 0**；`/pipeline` 端点在线上；存在 2703 行对话的真实用户 | 给 `_PIPELINE_LOG` 条目加 `user_scope_key()` 并在 `get_pipeline_log` 过滤；或直接删内存分支只走 per-user 落盘 |
| **2** | **Q-1+Q-2 托管无授权（P0）** | `X-API-Mode` 命中 2 处且无授权；DB 中 `quota_api_proxy>0` 账号数 = **0**；`FIREFLY_PROXY_KEY` 已配置（sk-***，68 位） | 托管入口加白名单（`quota_api_proxy>0` 或 admin）；把 `quota_api_proxy` 接进放行判定 |
| **3** | **代码可追溯性（P0·工程）** | 线上 3 个文件与任何 commit 都不对应；`/opt/firefly/.git` 缺失 | 把 `server/` 纳入 git（或私有仓）；部署后记录 commit hash 到 `version.json` |
| 4 | D-1/D-2 导入非原子 + 备份失败吞错（P0） | `/import-data` 在线；同源实现先 `rmtree` | 改 tmp→rename；备份失败即中止导入 |
| 5 | M-1/M-2 启动迁移（P0·机制） | `_migrate_legacy_layout` 线上命中 2 次、import 期执行 | 移出 import 期 + 失败可观测 + 迁移前打 `pre-migrate` 包；**当前未触发（M-4），故非紧急** |
| 6 | S-2 `/requests` `/metrics` 无用户维度（P1） | 两端点在线上 | 加用户过滤或仅限 admin |
| 7 | B/D-6 用户侧零自助备份（P1） | `backups/` 目录数 = 0；前端无导出入口 | 前端补导出按钮（调既有 `/export-data`） |
| 8 | 回滚流程缺失（P1） | 无 git、无流程文档 | 补"代码回滚 + 数据回滚 + 下载链回滚"三步 SOP |

### 结论：开发版 vs 线上

> **角色卡预设包架构（R-01 丢包、R-04 更新遮蔽、E-1 `backup=False`、F-1 无导出入口等）全部只存在于未发布的开发版，线上 v0.8.1 不受影响。** 线上真正需要处理的是**通用数据路径**（D-1/D-2 导入非原子与备份吞错）、**服务端授权**（Q-1/Q-2）、**隐私隔离**（S-1）与**代码可追溯性**（C1）四类。
