# 流萤项目 — 问题清单与待办（交接文件）

> 更新：2026-08-13 | 会话交接用，新会话先读本文件

---

## ⚠️ 铁律（新会话必须遵守）

1. **git 只提交本地仓库，绝不 push 到 github**（用户明确要求；之前错误 push 过多次，历史无法撤回，从现在起禁止）
2. **不要删除任何构建缓存/依赖**：`android/app/build/`、`android/.gradle/`、`android/sdk/`（4GB）、`dist/`、`build/` 全部保留——删了构建会全量重跑很慢（曾误删导致构建从"秒级增量"变"全量重编译"）
3. 修改前先讨论，用户说"先别动手"时只思考/总结

---

## 2026-08-13 服务器版 APK 顶部 ActionBar"流萤"大字修复（本次已完成）

**现象**：服务器版 APP（com.firefly.server）首页顶部多出系统 ActionBar，标题"流萤"大字（高约 168px），把整个页面内容下推 ~159px，影响设计感；本地版 APP 无此问题。

**根因**：`android_server/app/src/main/AndroidManifest.xml` 的 `<application>` 标签中，`android:usesCleartextTraffic="true">` 后紧跟 XML 注释 `<!-- ... -->` **把标签闭合**，导致后面的 `android:supportsRtl` 与 `android:theme="@style/AppTheme"` 变成标签外无效文本（aapt2 xmltree 中 theme 显示为未解析的 T: 行而非 A: 属性）。主题从未生效 → 回退系统默认主题（带 ActionBar）→ ActionBar 显示应用名"流萤"。

**修复**：注释移到 `<application>` 标签之前，属性完整保留（`android:theme` 恢复为有效属性，supportsRtl 一并恢复）。已重新构建 APK（versionCode=2 / 0.7.2）并安装真机验证：action_bar 消失，首页顶部元素位置与本地版完全一致（反馈 y=171 / 公告 y=195）。

**教训（防再犯）**：Manifest 标签属性列表中间禁止插入 XML 注释；改完 Manifest 后可用 `aapt2 dump xmltree --file AndroidManifest.xml <apk>` 核对属性是否解析为 `A:` 行。

---

## 2026-08-13 服务器版逻辑完善记录（本次已完成）

**根因**：relay 客户端逻辑（资产本地化/占位符填充/轮询代发）在 41d603f 随 chat.html 删除后未回迁，服务器版聊天链路断裂（120s 超时降级）+ 角色设定不生效。

| # | 修复项 | 位置 |
|---|--------|------|
| 1 | relay 引擎回迁：initAssets（清单指纹→差异下载→localStorage 缓存，key 带 mode 维度）+ fillPlaceholders + relayLoop（1s 轮询代发） | `android_server/assets/app.js`、`server/frontend/app.js`（双份同步） |
| 2 | 资产端点 mode 化（消灭硬编码 story；haruno 资产独立） | `app/routes.py` |
| 3 | **CORS 中转降级**：OpenCode Go 端点不支持浏览器跨域（OPTIONS 404）→ 新增 `/relay/proxy`（call_id 必须匹配队列中真实 pending 项=非开放代理；Key 经 X-API-Key 头内存即弃）；前端直连失败自动降级 | `app/routes.py`、`app/modules/api_client.py`、前端 relayTick |
| 4 | 网关限流 60→300 次/分钟（relay 1s 轮询下 60/分钟 必然撞限流；relay_pending 为微秒级端点，300 无压力） | `server/download_server.py` |
| 5 | 后台主动复刻：KeepAliveService 移植（evaluateJavascript 触发页面 `__serverProactive()`，hidden_reply_enabled 门控）+ FireflyJs 通知桥 + 权限/电池引导 + Manifest | `android_server/.../KeepAliveService.kt`、MainActivity.kt、app.js、AndroidManifest.xml |
| 6 | version.json 补内容（tag 0.7.1 + exe/apk 链接） | 服务器 `/opt/firefly/server/version.json` |
| 7 | 部署：scp 全量代码 + systemd SMTP 环境变量（原授权码，用户决定不轮换）+ 重启 | 服务器 |

**验证**：E2E（直连 53s / 中转 30.6s 全绿，占位符零残留，回复命中机体颜色/橡木蛋糕卷角色设定）；浏览器实测（登录→GO 端点→发消息→中转降级→角色记忆回复）；app.js stub 冒烟；gradle 编译通过。

### 遗留（已知天花板）

| 项 | 说明 |
|----|------|
| 推送通道 | 轮询方案（KeepAliveService 定时触发）；FCM 需域名/费用，明确不做 |
| OPPO 冻结 | ColorOS 冻结后台进程 → 后台主动静默失败；依赖设置引导（已移植） |
| GO 端点直连 | 不支持 CORS，只能中转（DeepSeek 官方支持 CORS 可直连）；中转时 Key 经服务器内存（用后即弃不落盘） |
| 双份前端 | `assets/` 与 `server/frontend/` 人工同步（改一处需 cp 另一处） |

---

## 一、当前问题清单

| # | 问题 | 状态 |
|---|------|------|
| 1 | ~~首页覆盖层定位 bug~~：`#home-view` 用了 `position: fixed; inset: 0` + 桌面 @media 里 `left:50%` 冲突（PC 上首页面板右偏） | **已修复**（style.css 桌面断点：`right:auto + margin:0 + width:520px + translateX(-50%)`，实测对齐） |
| 2 | ~~首页功能（公告栏/轮播/开始聊天切换）未打包验证~~ | **已完成**（随 0.7.0 发版真机验证） |
| 3 | 键盘 bug：安卓上弹出输入法时输入框 UI 未跟随上移（用户键盘跟随上移了）。变量：用户用微信输入法（开发者用讯飞）；可能变量：悬浮键盘模式 | **暂缓**——等用户用讯飞输入法测试对比：若讯飞正常 → 微信输入法兼容问题；若讯飞也异常 → 查代码（当前配置：Manifest `adjustResize` + `100dvh` + `WindowCompat.setDecorFitsSystemWindows(true)`） |
| 4 | 桌面快捷方式图标显示白方块+蓝箭头（exe 自身图标已正确：混合 ICO 16~128 BMP + 256 PNG，已像素验证） | 已接受为小 bug，不阻塞 |
| 5 | 构建缓存被误删（速度问题） | 已发生，下次构建自动重建恢复 |
| 6 | ~~发送后切后台丢消息~~：前端 5s 提交窗口在切后台时 JS 定时器冻结 → 消息未提交丢失；且后台回复完成无通知 | **已修复（0.7.2 前）**：发送即达后端 + 后端 5s 滑动合并窗口（`/chat/hint` 打字中重置、`/chat/flush` 提前结束、切后台窗口自然到期兜底）；后台回复完成走 KeepAliveService 通知通道。验证：PC 三场景（合并/信号/兜底）全过，真机待用户手测 |
| 7 | ~~安卓后台回复被冻结~~：切后台约 80s 后 OPPO/ColorOS 的 OplusHansManager 冻结进程（即使前台服务+WakeLock+deviceidle 白名单都挡不住）→ 回复流程暂停，切回才继续 | **已处理（0.7.2 前）**：根因是 ColorOS 私有冻结机制，标准 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 无效，唯一解法是「应用启动管理 → 允许后台运行」。已加启动引导弹窗（所有设备统一引导：OPPO 系 → 应用详情页开启后台运行；其他厂商 → 系统授权弹窗）。真机验证弹窗与跳转正常。**用户需在设置中开启后重新验证** |

---

## 二、项目背景速览（新会话上下文）

- 项目：流萤角色扮演 Agent（DeepSeek API + LLM 子代理检索 + 剧本系统）
- **双线版本**：
  - **本地版**（`app/`）：PC（PyInstaller）+ 安卓（Chaquopy），用户设备独立运行
  - **服务器版**（`server/`）：云服务器多用户，用户自带 API Key（存浏览器），数据按账号（user_id）隔离——见 docs/部署说明.md
- 仓库：本地 git（不要 push github！）；备份：`firefly_backup_*.bundle` + `firefly_keystore_backup_*/`（根目录，gitignore）
- 关键路径：
  - 本地版后端：`app/`（server.py/routes.py/orchestrator.py/modules/）
  - 服务器版：`server/`（server_app.py/frontend/，复用 app/modules + app/routes）
  - 前端：本地版 `app/static/`，服务器版 `server/frontend/`（**双份维护，改通用逻辑需同步**）
  - 知识库+剧本：`knowledge/`（剧本在 `knowledge/剧本/版本/`）
  - 安卓：`android/`（构建用 gradle-8.9，签名在 `android/keystore/`）
  - 安装器脚本：`package/firefly.iss`（Inno Setup）
- 测试：`tests/test_*.py` 独立脚本运行（`python tests/test_xxx.py`）
- 服务：本地 `python app/server.py` → http://localhost:8765；服务器版 `python server/server_app.py` → 0.0.0.0:8765
- 版本发布：GitHub Release（v0.6.0 已有 firefly-setup.exe + firefly.apk）——**发布到 github 的操作仅限用户明确要求时**

---

## 2026-08-14 双模式整合 0.8.0（已实施）

- 三版本 → 两版本：安卓合并版（本地/服务器双模式）+ Windows 本地版；ndroid_server/ 已删除；一套前端（app/static 唯一源，sync_frontends.py 自动同步）。
- 数据同步：/import-data（导入+自动备份）、/sync/upload、/sync/download（账号云端备份）——P0-4「安卓数据导出/导入」至此全部完成。
- 登录/注册/重置表单内联进首页（修复 file:// 跳转被拦导致的「页面内容消失」伪崩溃）；401 toast 降噪。
- 服务器已部署 0.8.0（备份 user_data_pre_080_20260814-1430.tar.gz）；APK/PC exe 已构建未发布。
- 待用户手测：内联表单注册/登录提交（需真实 QQ 邮箱）、托管额度链路、导入文件选择器、模式切换后的数据同步体验。
