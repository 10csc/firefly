# 未完成事项清单

> **这是什么**：本项目的**唯一待办真相源**。所有审计/审查/排查发现的问题，结论都汇总到这里；**做完一条就删掉一条**。
> **为什么要有它**：此前审计结论散在 5+ 份报告里，每份只讲"发现了什么"，没有一份讲"现在还剩什么没做"。结果同一个问题被反复审出来：**明文传输被发现 ≥4 次、部署债 ≥5 次、安装包未重建 ≥3 次**。这份清单存在的唯一目的就是终结这个循环。
>
> **维护规则（硬性）**
> 1. 新发现 → 追加一行；**不许只写进审计报告而不进本清单**。
> 2. 做完 → 直接删行（不标"已完成"，避免清单变成历史堆）。
> 3. 每条必须写清 **依据**（哪份报告的哪一节）和 **验收标准**（怎么算做完）。
> 4. 每条都要记 **首次发现** 与 **复发次数**——复发次数是给"为什么又没做"留的证据。
> 5. 审计**过程记录**写完就归档（移到 `封装（没指定就不读）/`），不在 `docs/` 长期堆着。
>
> 最近更新：2026-09-18（建立）

---

## 复发规律（先看这个，再看表）

| 问题类型 | 首次发现 | 复发次数 | 为什么总也不做 |
|---|---|---|---|
| 明文 HTTP | 一期架构重构（2026-08） | **≥4** | 需要域名/证书，用户明确不买不申请 → 每次都被"记下"然后跳过，没人给零成本方案 |
| 部署债（改完没上线） | 2026-08-25 记录 | **≥5** | 本地改完就算"完成了"，没有"部署"这一步的责任人；`server/` 不进 git，无自动提醒 |
| 安装包未重建 | 2026-09-15 第二批 | **≥3** | 修了源码但没重建 exe/apk，用户拿到的还是旧包（**本轮实测：包是 8-23 的**） |
| 原子写类 | 2026-09-15 第一批（B 簇） | 2 | B 簇只修了部分文件（配置/记忆/对话/手账），**同步链路没覆盖**；且修了也没部署 |

**对症的两条新规矩（建议写进 `docs/开发规范.md`）**
- **"改完即列部署项"**：任何改动落地时，同步在 `docs/部署清单-待上线.md` 或本清单里登记"哪些文件要传、传给谁（app/ 还是 server/ 还是两处）"。
- **"发版三步不可跳"**：`sync_frontends.py` → 构建 → **`tools/check_apk_manifest.py` 验收** → 才允许上传。

---

## P0 —— 影响用户或数据安全（尽快）

| # | 事项 | 首次发现 | 复发 | 依据 | 验收标准 |
|---|---|---|---|---|---|
| P0-1 | **用户下载的安装包是旧构建**：① 服务器 `/opt/firefly-downloads/{firefly.apk,firefly-setup.exe}` 是 8-23 的；② Gitee 只有到 v0.8.1 的 release（**没有 v0.9.0**，而整包兜底取 id 最大的 tag，热更/公告的 Gitee 镜像根又写死 `v{base}`=v0.9.0） | 2026-09-15 第二批 | **3** | 审计-2026-09-18 §1.4；2026-09-19 实测 Gitee release 列表只有 v0.7.0–v0.8.1 | 新 APK：`check_apk_manifest.py` 返回 0；包内含公告前端；下载页版本号与包内一致；**Gitee 有 v0.9.0 release 且挂上 APK**；服务器两个本地副本同步到 0.9.0 |
| P0-2 | **明文 HTTP 传输**：登录凭证与用户 API Key 在网络裸奔（无域名/证书） | 一期架构重构 | **4** | 审计-2026-09-18 §2.1 | 抓包看不到明文 token/Key；已落地"敏感头应用层加密 + token 30 天→7 天"（零成本方案） |
| P0-3 | **部署债**：确认缺 4 个文件 `server/download_page.py`(A4)、`app/shared_http.py`(A1/A2)、`app/server.py`(A1)、`app/infra/sync/restore.py`(B7)；另有 19 个文件差异方向待定。**2026-09-19 部分还债**：公告接收端（`app/notice.py`、`app/routes_notice.py`、`app/hotupdate/*` + `core/paths.py`/`api/router.py`/`server/app.py` 外科式补丁）与网关 `/notice/*` 已上线并验证 | 2026-08-25 | **5** | 审计-2026-09-18 §1.1/§1.2/§1.3；本次实测线上 `app/` 无 `hotupdate/`、无 `notice.py`、`api/router.py` 缺 7 类端点 | 逐文件核对后上线；**严禁全量 scp**（本地有 123 项未提交改动） |
| P0-4 | **同步链路非原子写**（约 6 处 `write_text`），中途失败会截断正式对话文件 | 2026-09-18 | 1 | 审计-2026-09-18 §4.1 | 全部改 `storage.atomic_write_*`；补"写到一半抛异常 → 原文件不变"的测试 |
| P0-5 | **两个 HTTP 入口无全局异常兜底** + 上传异常穿透 → 断连 → 前端误报"请先登录账号" | 2026-09-18 | 1 | 审计-2026-09-18 §4.2/§4.3；`app/static/js/sync.js:65,73` | 人为 `raise` 后前端收到带 trace_id 的 JSON 错误；断连场景显示"同步失败（错误号）" |
| P0-6 | **确认旧 API Key 已在 DeepSeek 后台删除**（泄露在 git 历史 `f0bc87c`/`5c4cbb0`/`ec9922d`） | 2026-09-18 | 1 | 审计-2026-09-18 §2.7 | 后台列表确认 `sk-913…` 已不存在（**不要调用测试**） |
| P0-7 | **服务器入口从不执行换入残留回收**：`recover_swap_leftovers` 全仓唯一调用点在 `app/server.py`（本地入口），`server/app.py` 的 main() 没调 → 服务器上恢复/导入中断会导致用户数据目录缺失、读取静默回退内置包，且永不自愈。**只传 restore.py 等于白修** | 2026-09-18 | 1 | 审计-2026-09-18 附录 D「坑 1」 | 给 `server/app.py` 启动链加调用；构造"恢复中断"现场验证能自动回滚 |
| P0-8 | **服务器网页前端的后端没对齐**：线上 `app/` 是 0.8.1 时代的（`api/router.py` 缺 `/memory-status`、`/archive`、`/memory-action`、`/pack-file/*`、`/pack-structure`、`/voice/*`、`/hotupdate/*`）。仓库里的新前端（`server/frontend/`）调用这些端点会 404 ⇒ **因此本次只部署了公告接收端，没有部署新前端**（部署了反而让服务器网页 UI 降级）。安卓"服务器模式"同样拿 APK 里的新前端打旧后端 | 2026-09-19 | 1 | 本次部署探测（见 `docs/公告通道规范.md` §八 与协作记忆） | 线上 `app/` 与仓库对齐（并入 P0-3）；对齐前**不要**单独更新 `server/frontend/` |


## P1 —— 异常反馈机制（用户本轮明确要求）

| # | 事项 | 首次发现 | 复发 | 依据 | 验收标准 |
|---|---|---|---|---|---|
| P1-1 | **错误码 + trace_id 贯穿**（`模块.阶段.原因`） | 2026-09-18 | 1 | 审计-2026-09-18 §5.3 | 失败响应含 `{code, trace_id}`，前端显示错误号 |
| P1-2 | **诊断日志落盘 + 脱敏**（`user_data/logs/diag-*.jsonl`，7 天滚动） | 2026-09-18 | 1 | §5.4 | 日志含阶段/耗时/错误码；Key/token/邮箱/正文已脱敏 |
| P1-3 | **一键导出诊断包** `/export-diagnostics` + 设置页按钮 | 2026-09-18 | 1 | §5.5 | 导出 zip 含 summary/diag/requests/pipeline/state/env；界面提示"发给我" |
| P1-4 | **服务器版诊断按 user_id 过滤**（现 `/metrics` `/requests` `/pipeline` 全 403，用户无证据可交） | 2026-09-18 | 1 | §5.5 / §3.3 | 服务器用户能导出自己的诊断包，且看不到他人数据 |
| P1-5 | **管理台按 uid 拉取用户诊断包** | 2026-09-18 | 1 | §5.6 | 管理台可指定用户导出，复用同一打包函数 |
| P1-6 | **自动同步失败可见化**（现节流/未登录/后端未就绪全部静默） | 2026-09-18 | 1 | §4.7；`sync.js:48,52,53` | 连续失败≥3 次或 >24h 未成功 → 界面黄条提示，状态进诊断包 |

## P2 —— 代码级加固（中低）

| # | 事项 | 首次发现 | 复发 | 依据 | 验收标准 |
|---|---|---|---|---|---|
| P2-1 | B 簇原子写修复**未部署**（`core/config.py`、`conversation_store.py`、`memory_manager.py`、`preset_parse.py`） | 2026-09-15 第一批 | 2 | 审计-2026-09-18 §1.1（在差异集内） | 随 P0-3 一并对齐 |
| P2-2 | **登录无 IP 维度失败限流**（现只有邮箱锁定） | 2026-09-18 | 1 | §2.4 | 同 IP 15 分钟失败≥20 次 → 拒绝；正常用户无感 |
| P2-3 | 备份打包非原子（`backup_create` 可能留半截 zip） | 2026-09-18 | 1 | §4.4 | 改 `.tmp` + rename |
| P2-4 | 快照恢复 stickers 分支直接写正式目录且无备份 | 2026-09-18 | 1 | §4.5 | 改临时目录 + 换入，或先备份 `stickers/` |
| P2-5 | 恢复过程"备份失败"处置不一致（阶段2a 静默 pass，阶段1/2b 中止） | 2026-09-18 | 1 | §4.6 | 统一口径 |
| P2-6 | 邮箱验证码用 `random`（应用 `secrets`）；比较改 `hmac.compare_digest` | 2026-09-18 | 1 | §2.5；`server/mail.py:86` | 改完注册流程可用 |
| P2-7 | SSRF 防护 DNS 时间差（校验与实际请求两次解析） | 2026-09-18 | 1 | §2.6 | 校验后锁定 IP 再请求 |
| P2-8 | ~~指向 HTTP 自下载~~ **2026-09-19 已改**：线上两份 `version.json` 一直写着 `http://101.200.14.126:8787/download/...`（tag 还是 0.8.1）→ 随本次发版改为 **https Gitee 直链 + tag v0.9.0** | 2026-09-18 | 1 | §2.3 | 已改并实测 `/version.json` 返回 https |
| P2-9 | **任务 4.1（语音插件）原卡有三处与实测不符**：①「PC 本地版第一刀」而实际决策为**安卓优先**；②「服务器版强制关闭」实际应改**资源就绪判定**（服务器没装模型自然不可用，换大内存机器无需改代码）；③「`domain/voice/` 懒加载引擎」隐含 Python 侧跑 ONNX，但**Chaquopy 装不上 `onnxruntime`**（索引 `pypi.org/simple` + `chaquo.com/pypi-13.1` 均无 Android wheel）→ 引擎须在独立执行体（PC 子进程 / 安卓 Kotlin） | 2026-09-18 | 1 | `docs/工具/tts.md` §10；实测 `ERROR: No matching distribution found for onnxruntime` | 任务 4.1 卡按 `docs/工具/tts.md` §10 修订，并在 `docs/架构重构计划/执行日志.md` 追加一条 |
| P2-10 | **记忆活跃窗口三项阈值只有 config 键、没有界面**（`memory_window_turns` 触发 / `memory_keep_turns` 保底 / `memory_warn_turns` 提醒 —— 现为 100/30/80）。它们直接决定"多久花一次 LLM 调用"，`/set-config` 已能写（含钳制），但设置面板没滑条，用户只能改 `config.json` | 2026-09-18 | 1 | `modules/auto_rest.py`；`docs/设计/提示词系统/01_调用链与实测体量.md` §四 | 设置面板「与角色相关」组加一行（上限滑条 + 0=关自动整理），改后落 config.json 且下一轮生效 |
| P2-11 | **自动整理暂无服务器端策略**：服务器版多用户下会按每用户各自的轮数触发，服务器侧没有全局开关与额度护栏（`client` 已改为请求线程取好再传进后台线程，堵住了取错客户端的雷） | 2026-09-18 | 1 | 同上；`docs/服务器管理规范.md`「绝不静默新增调用」 | 服务器版默认关闭或纳入配额记账，管理台可见触发次数 |
| P2-12 | ~~PC 安装器未随 0.9.0 重建~~ **2026-09-19 已做**：`PyInstaller + ISCC` 产出 `package/firefly-setup.exe`（22.09MB / ProductVersion **0.9.0**），新增冒烟工具 `tools/smoke_pc_exe.py`（14 项）；Gitee v0.9.0 资产与服务器本地副本 sha256 均 `9a189dbf…` | 2026-09-19 | 1 | 本次发版实测 | 已完成并双通道验证 |
| P2-13 | **热更/公告的 Gitee 镜像尚未真跑过一次发布**：`tools/publish_notice.py`、`tools/publish_hotupdate.py` 走的是 `attach_files` + 回拉比 sha256，本次只验到"release 存在且 APK 能拉回" | 2026-09-19 | 1 | `tools/publish_gitee_release.py` 与两个 publish 工具 | 首个真实补丁（修 bug 时）走完 `build → check → publish_hotupdate`，两个源都能拉回且字节一致 |
| P2-14 | **PC 端前端完成阶段 1 + 2a + 2b + 3**（三栏外壳 / 设定文件两栏化 / 角色卡域导航两栏化 / 聊天区「包上下文」栏；见 `docs/设计/PC端前端重构.md`）。**仅剩阶段 2c**：收藏与请求记录在列② 里只给一个入口、没有直接列条目（**本机数据为空，列出来没法当场验收**，故留到最后） | 2026-09-19 | 1 | 同文档 §五「分阶段」 | 阶段 2c 落地，用 `tools/pc_audit.py` 出图 + `tools/mobile_regress.py`（18 项）保证窄屏不回退 |
| P2-15 | **PC 重构后没有真机/真浏览器的人工复验**：手机被用户拿走，窄屏结论来自无头 Chrome 的**结构不变量断言**（`tools/mobile_regress.py`，14/14）与截图，不是真机点按 | 2026-09-19 | 1 | 本轮验收记录 | 手机回到手边后装新 APK 走一遍：首页/聊天/公告/设置/角色卡 + 引导，确认与重构前观感一致 |


## P3 —— 运维收尾（低风险）

| # | 事项 | 首次发现 | 复发 | 依据 | 验收标准 |
|---|---|---|---|---|---|
| P3-1 | `PermitRootLogin` 改 `prohibit-password` | 2026-09-18 | 1 | §2.8-2 | `sshd -T` 确认；现有连接不断 |
| P3-2 | 服务器加 swap（1.6G 内存、swap=0，有 OOM 风险） | 2026-09-18 | 1 | §2.8-3 | `free -m` 显示 swap>0 且写入 `/etc/fstab` |
| P3-3 | 权限收紧：`user_data` 750、`firefly.db` 640 | 2026-09-18 | 1 | §2.8-4 | 服务仍能读写（www-data） |
| P3-4 | 处理 `/root/harden-backup/firefly_root_pass.txt`（root 密码明文） | 2026-09-18 | 1 | §2.8-6 | 确认用途后改强密码或删除 |
| P3-5 | 发布检查清单改为"扫描整个 `dist/` 有无密钥" | 2026-09-18 | 1 | §2.8-8；`readme.md` | readme 更新并与 `docs/构建规范.md` 一致 |
| P3-6 | 清理线上残留 `app/modules/pack_forge.py`（本地已下架，线上仍被引用） | 2026-09-18 | 1 | §1.1 | 与 P0-3 同批处理 |
| P3-7 | git 历史清不清（泄露 Key 已轮换，可选） | 2026-09-18 | 1 | §2.7 | 决定后执行或明确"接受现状"并备注理由 |

---

## 已完成的收尾（本清单外，仅备查，不占行）

<details>
<summary>2026-09-18 已做（点击展开）</summary>

- 归档 4 份被取代的旧报告 → `封装（没指定就不读）/2026-09-18_归档/docs/`
- 更新 `docs/README.md`（索引 + 新增"部署状态口径"节，纠正过期的"未部署"说法）
- 6 处文档加"2026-09-18 实测更正"批注：README、协作记忆、审计修复执行记录 ×2、复核补充、安全加固记录-08-25；`部署说明.md` 加"历史手册"横幅
- 本机 5 份含 API Key 明文的配置备份 → 送 Windows 回收站（可恢复）
- 服务器垃圾文件 `/opt/firefly/]*` → `/root/harden-backup/stray-bracket-file-20260918.txt`
- 服务器网关目录 3 个旧 `.bak` + `__pycache__` → `local_bak_20260816/`
- 服务器 `/opt/firefly/.backup-20260813` → `/opt/backups/legacy-20260918/`
- 新增发版验收工具 `tools/check_apk_manifest.py`（当场验出当前 APK 不合格）

**同日（第三批：记忆机制 P0/P1/P2 + 菜单 chip + 清除历史范围）**
- 「清除历史」现在**连记忆一起清**：`{mode}/data/memory.md`、`{mode}/journal/手账.md`、
  `.memory_index`、`{mode}/data/archive/` 全删；**出厂记忆（`{mode}/character/memory/default.md`）不动**
  （它是角色卡资产）。同时刷新进程内 `memory_head` 与手账缓存。
- 「设定文件」页签**只剩聊天产物**（用户记忆 + 手账）；「用户设定」编辑器移出，
  统一在「角色卡管理 → 人设与口吻 → 用户设定」编辑（那里本来就有）。
- 菜单 chip 容器重做：`repeat(5,1fr)` 固定 5 列 → flex 按组自适应等分
  （4 字标签「设定文件/请求记录/流程日志」原先被硬裁，实测内容宽=容器宽、左 0 右 0）。
- P0：修 rest 的「内存窗口当全量」bug（见 `docs/错误总结.md` #10）；
  `ContextManager.token_capacity` 从死字段接上（`BUDGET_*` + 整轮裁剪）。
- P1：新增 `modules/auto_rest.py`（后台自动整理，默认 10 轮触发）；
  `_compose_memory_text` 去重 + 分节标题只写一次 + 已完成条目上限。
- P2：新增 `modules/memory_archive.py`（按月只增不改的历史归档），检索器把它当知识源。
- 新增回归 `tests/test_memory_auto.py`（36 项，沙箱 USER_DIR）。**全量 60/60 绿。**

**同日（第四批：记忆机制二次设计——用户否掉上一轮形态）**
- **活跃窗口 = 30–100 轮完整对话原文**，分析器/回复器读它；跑满 100 轮时把"最近 30 轮以外"
  的部分搬走。阈值 = `memory_window_turns`/`memory_keep_turns`/`memory_warn_turns`（100/30/80），
  已接入 `/set-config`（有钳制）；**UI 滑条仍缺（P2-10）**。
- **历史存档改成"完整对话原文"**（上一轮存的是压缩后的事实条目，作废）：按月分片、只增不改、
  只进检索器；用户可在设定文件页直接编辑（改完检索器看到的就是改后的）。
- **设定文件副页新增「历史对话存档」**：月份下拉 + 原文 + 「AI 压缩」（压进用户记忆，原文保留）。
- **新增 3 个端点**（67 POST / 38 GET）：`GET /memory-status`、`GET /archive`、`POST /memory-action`。
- **token 预算 8000 → 30000**：这是缓存命中率红线（从窗口头部裁剪会把增长期变成滑动期，历史块永远不命中）。
- **修掉当天自己埋的形状 bug**：`rest` 入参换 `load_all()` 后口径对、形状不对（`who` vs `role`）→
  切片恒空。新增 `load_all_context()`（与 `count_user_turns` 同源）。已记入 `docs/错误总结.md` #10。
- **提醒形态改版**：整行横幅 → 名字行尾小灰字 `· 记忆 92/100`（用户反馈"太影响观感"）。
- `tests/test_memory_auto.py` 扩到 **72 项**；全量 60/60 绿；四门禁 PASS；真机实测通过。

**同日（第五批：攻击哨兵「一次封禁弹 4-5 次」修复 + 全功能检查）**
- **用户报障**「服务器频繁提示遭到攻击」→ 查清**与真机测试无关**（触发字段是 `fail2ban_bans`，
  即从公网进来的 SSH 密码尝试；测试全程只碰手机本机 `127.0.0.1:8765`）。
- 根因：`attack_watch.sh` 统计"最近 5 分钟的 Ban 行数"→ 一次封禁每分钟都命中 → 标记文件每分钟重写
  （`time` 每次都变）；而 `attack_sentinel.py` 的去重键正是 `flag["time"]` → **去重完全失效**。
- 修：服务器侧改「新封禁」语义（状态文件记住已上报 Ban + `time` 用封禁时间 + 标记保留 10 分钟
  + 标记带被封 IP），**已单文件部署并备份**；本地侧去重键换内容签名 + 30 分钟冷却 + 单次封禁走
  噪声档（只写日报不弹窗不调模型）+ 模型空输出本地兜底；哨兵已重启（PID 23268）。
- 新增 `tests/test_attack_sentinel.py`（22 项）；`docs/错误总结.md` **#11**。
- **全功能检查**（真机 704c2441）：端点层 **33/33**；长按菜单/记忆提醒/转语音/播放/插件页/设定文件
  存档块 全部 ✅；核心流水线 **19.5s / 4 条回复 / 检索-分析-回复-组织四阶段齐全**；
  **自动整理在 t+15s 自行触发**（active 11→8，存档 599→1141 字，追加带时间戳原文）；
  清除历史 **13/13**（用户记忆/手账/游标/存档全清，出厂记忆与另一模式逐字不动）。

**同日（第六批：聊天 UI 对齐官方短信页 + 角色卡化补债）**
- 按官方截图（591 宽）**逐像素量**出几何比例，改：头像 8.5%→10.5%、左边距 4%→7%、间距 2%→3%、
  单行气泡高 42.6→**28.1px**（根因是上下内边距+行高，**不是**左右内边距——那个本来就对）、
  圆角 4/16→3/10、**删掉气泡尾巴**（官方左缘是直线）。
- **名字行**：气泡/贴纸/图片上方加 `.msg-who`（左/右缘与气泡对齐，字号 0.75×气泡）。
- **头像改顶对齐**（`align-items: flex-start`）——修「第二行头像又下去了」。
- **角色卡化补债**：新增 `views.charName()/userName()` 统一出口；清掉前端 8 处 + 后端 2 处角色名
  字面量；HTML 动态文案改走 `data-tpl` 模板。新增守卫
  `tests/test_frontend_generic_names.py`（17 项），只留 2 处有理由的白名单（应用名/资产文件名）。
- 新增 `tests/test_chat_ui_geometry.py`（24 项，同源不变量 + 官方验收线 + 无尾巴守卫）。
- **全量 63/63 绿**；四门禁 PASS；真机复验。

**2026-09-19（第七批：产品改名 + 公告通道 + 0.9.0 发版上线 + 0.8.1 兼容回归）**

*产品名改动*：应用显示名 → **Firefly（萤火虫）**，内部称「萤火虫平台」；版本定为 **0.9.0**。
**角色内容一处未动**（`app/assets/character/**`、`database/**`、内置卡 `char_name` 仍叫「流萤」）
—— 产品名与角色名是两回事，改名前先做了区分。

*公告通道*（新子系统，规范 `docs/公告通道规范.md`）：端侧 `app/notice.py` + `routes_notice.py` +
`js/notice.js`，**复用热更的 RSA 信任根**、更新源由热更源翻译而来（一个配置两条通道）；
只接受结构化块 + 端侧 `textContent` 渲染（**结构上无注入面**）；三级降级
（缓存 → APK 内置静态指南 → 整份丢弃）；版本定向、已读、撤回、kill switch 齐备。
工具 `build_notice.py` / `publish_notice.py`；回归 `tests/test_notice.py`（52 项）。

*补掉一个真洞*：PC/服务器端从来没有验签实现（原来只有 Kotlin 一条路）⇒ 热更与公告在
PC/服务器上**静默永不生效**。补标准库自实现验签 + 公钥副本，门禁强制两份公钥逐字节一致；
固定向量测试 `tests/test_hotupdate_verify.py`（20 项）。

*0.9.0 发版上线*：Gitee **新建 v0.9.0 release（id 1153592）**并挂 APK
（此前只有到 v0.8.1 ⇒ 0.9.0 无下载地址、热更/公告镜像必然 404）；
**APK 132.6MB → 81MB**（Gitee 单附件上限 100MB，`libonnxruntime.so` 单 ABI 31–37MB
⇒ 发布包只带 arm64-v8a）；服务器本地 APK 与 Gitee 上那份 **sha256 完全一致**；
公告接收端 + 网关 `/notice/*` **已部署生产**并逐项验证；`version.json` 改 https Gitee 直链。

*0.8.1 兼容回归*：**只读探测生产真实数据形状** → 照形状造夹具写 `tests/test_compat_081.py`（32 项）
→ **结论：现有代码本来就兼容 0.8.1，本轮没写任何迁移代码**（测出来的，不是猜的）。

*两条只有真机能抓到的 bug*：① bundle 单作用域 TDZ（`错误总结.md` #15）导致公告整块静默失效
→ 新增**产物级冒烟测试** `tests/test_frontend_boot.py`；② "先回缓存、后台刷新"前端不跟第二次
→ 前端处理 `refreshing` 重试。两条都已真机复验通过。

*端到端验收*：`tools/e2e_hotupdate.py`（25 项，真签名/真 HTTP/真覆盖层：安全模式、kill switch、
双源降级全过）；真机装 81MB 新包 + 清数据冷启动，首页亮未读红点、面板渲染服务端公告、
已读回写成功，`backend=kotlin`。

*全量 70/70 测试绿；五门禁 + APK 门禁全过。*

**2026-09-19（第八批：PC 端前端重构 · 阶段 1）**

用户原话："PC 端 UI 有很大[问题]，依据当前版本的前后端架构，PC 端的前端需要重构以确保前端 UI
适配业务逻辑……确保 UI 风格与手机端统一，前端样式符合项目的数据逻辑和业务逻辑。"

- **先审计再动手**：用无头 Chrome 在 1600×1000 / 1280×800 / 1024×768 逐视图截图取证，
  9 条问题（A1–A9）写进 `docs/设计/PC端前端重构.md`：本质是"手机界面塞进右侧卡片 + 左边加一条导航"，
  没有角色包列表、没有状态栏、菜单是整屏模态、设置是放大版手风琴。
- **阶段 1 落地**：`pc.css`（新增，PC 唯一样式源，旧 PC 块从 style.css 整段移除）+
  `js/pc_shell.js`（新增，纯 classic script，零框架）+ `index.html` 的三栏外壳
  （图标栏 / 上下文列 / 工作区顶栏 / 底部状态栏）。**视图 id 一个未改**，所有既有入口零改动复用。
- **后续同轮补完**：阶段 2a 设定文件两栏化（列② 选段 → 右栏只看这一段）、
  阶段 2b 角色卡域导航两栏化（点域 → 只展开该域）、阶段 3 聊天区「包上下文」栏
  （记忆活跃窗口进度 / 存档月份 / 快捷动作；把审计 A5 的右侧空白换成信息位）。
  **仅剩阶段 2c**（收藏/请求记录在列② 直列条目，本机数据为空无法当场验收）。
- **删掉框架依赖**：`pc_nav.js` 与 `vendor/petite-vue.iife.js` 一并移除（PC 端不再为 10 项导航下载 MVVM）。
- **两个当场踩到的坑**：① PC 覆盖样式被 index.html 内联 `<style>` 压住（`<link>` 必须在它之后）；
  ② 不能跟设置面板的"手风琴"对着干（连点每一组只会开一个关一个）→ 改成"折叠瓦片 + 展开占整行"。
- **验收**：桌面四档截图（`tools/pc_audit.py`）+ 窄屏结构不变量 14/14（`tools/mobile_regress.py`）+
  新增 `tests/test_pc_shell.py`（25 项静态守卫：断点隔离、加载顺序、视图 id、打包/同步、无框架残留）。
</details>
