# 流萤服务器 · 安全日报

> 每晚 21:00 自动生成（CronCreate：automation-b9469330）。检索源：NVD CVE API / 安全新闻站点。
> 服务器快照：101.200.14.126，公网仅开 22/8787，8765/8766 仅本机，www-data 降权，scrypt+验证码+限流，SSH 仅密钥+fail2ban，ufw 默认拒绝。

---

## 2026-08-12 安全日报（首篇，含完整威胁建模）

### 一、今日值得关注的安全新闻

NVD 近 7 天（08-05 ~ 08-12）发布 CVE 28 条。与本服务器技术栈（Python 3.12 / OpenSSH / Ubuntu 24.04 / SQLite / 自研 http.server 服务）**直接相关的漏洞为零**。高危及值得注意的：

| CVE | 组件 | 类型 | 相关度 |
|---|---|---|---|
| CVE-2026-45809/46334 | OpenSIPS | 栈溢出/空指针 DoS (8.7) | 无关（非本服务器组件） |
| CVE-2026-66257/66273 | Apache Qpid Proton-J | 未认证 DoS (7.5) | 无关 |
| CVE-2026-18322 | WordPress 插件 | 未认证权限提升 (8.8) | 无关（未使用 WordPress） |
| CVE-2026-55707 | OpenStack Neutron | 越权 (7.1) | 无关 |

**结论**：今日无需要行动的外部威胁。unattended-upgrades 持续自动打补丁。

### 二、与服务器相关的风险点评估（完整威胁建模）

资产清单：用户账号（QQ 邮箱+scrypt 哈希）、用户对话数据（user_data/{id}/）、SQLite 库、SSH 控制权、下载文件、服务器代调 Key（未来）。

| 攻击路径 | 最坏结果 | 现状防御 | 评估 |
|---|---|---|---|
| 登录爆破/撞库 | 账号被盗→对话泄露 | 验证码+失败限流+scrypt | ✅ 挡住 |
| SQL 注入 | 数据库全泄露 | 全参数化查询 | ✅ 挡住 |
| 路径穿越 | 任意文件读取 | realpath+`..`拦截 | ✅ 挡住（实测 404） |
| 反代滥用（代理打内网） | 内网探测 | 代理目标固定 127.0.0.1:8765，路径不可控 | ✅ 无 SSRF |
| 注册滥用（刷账号） | 资源耗尽 | IP 注册限流 10 次/时 | ⚠️ 弱（IP 池可绕过） |
| ~~验证码内存增长（_captchas）~~ | ~~慢速 OOM~~ | 2026-08-12 已随滑块验证码移除 | ✅ 已解决 |
| **验证码生成无限** | CPU 打满（Pillow） | 无生成限速 | ❌ **需修复（P0）** |
| 代理层 DoS（业务路径无限流） | 服务不可用（非数据泄露） | 认证 401 兜底 | ⚠️ 建议全局限流（P1） |
| 服务器入侵（0day 提权） | 全部数据 | www-data 降权+系统补丁 | ⚠️ 无法 100% 防，靠隔离+备份 |
| 数据丢失（无自动备份） | 全部数据永久丢失 | 仅手动 bundle | ⚠️ **需备份策略（P1）** |
| 密码离线爆破 | 撞库风险 | scrypt（每次~100ms） | ✅ 成本高 |

### 三、需要采取的行动

1. **P0（内存/CPU 漏洞，建议立即做）**：
   - ~~`_captchas` 字典加过期清理 / `/auth/captcha` 按 IP 限速~~（2026-08-12 已随滑块验证码移除，不再适用）
   - 邮箱验证码 `mail.py _codes` 已有过期清理 + 60s 重发 + IP 10 次/时限速，无需再补
2. **P1（建议近期做）**：
   - 8787 网关反代路径加 IP 维度全局限流（令牌桶，如每 IP 20 req/s）
   - 备份策略：服务器每周自动 `tar` user_data → /opt/backups/ + 阿里云控制台开自动快照（或用户定期下载）
3. **持续**：unattended-upgrades 已自动打补丁；fail2ban 守护 SSH；每晚本日报持续巡检。

---

### 攻击事件 2026-08-12 17:30:16
- 标记: {"detected": true, "time": "2026-08-12 17:30:16", "fail2ban_bans": 3, "rate_limited": 2, "ssh_fails": 8, "gateway_429": 15}
- 模型分析: {"severity": "low", "attack_type": "暴力破解/扫描尝试", "analysis": "检测标记显示有SSH失败和网关429，但当前诊断数据中fail2ban封禁数为0，且近30分钟SSH失败、429均为0，说明攻击未持续或已被缓解。服务均正常，无近期错误，实际风险较低。", "actions": ["持续监控SSH登录日志和网关429指标", "检查历史日志中是否有异常IP来源并加入黑名单", "确保fail2ban正常启用并定期更新规则"], "need_user": false}
- 诊断: fail2ban=[Status for the jail: sshd
|- Filter
|  |- Currently failed:	0
|  |- Total failed:	4
|  `- Journal matches:	_SYSTEMD_UNIT=sshd.service + _COMM=sshd
`- Actions
   |- Currently banned:	0
   |- Total banned:	0
   `- Banned IP list:] 网关429=[0] API429=[0] SSH失败=[0]
- 服务: active
active
active

---
### 21:00 网络安全深度巡检（每日例行）

#### 一、服务器巡检结果（2026-08-12 21:00）

| 项目 | 结果 | 状态 |
|---|---|---|
| 磁盘 | 40G 总量，已用 2.9G（8%），可用 35G | ✅ |
| 内存 | 508 / 1612 MB（31%） | ✅ |
| firefly-server | active | ✅ |
| firefly-downloads | active | ✅ |
| fail2ban | active，封禁 0，失败 0 | ✅ 无攻击迹象 |
| unattended-upgrades | active | ✅ |
| 24h 错误日志 | 28 条（全部为今日调试期 traceback / proxy broken pipe，已修复，非攻击） | ✅ |
| 待更新包 | 1（cloud-init，被 keep back，非安全关键） | ⚠️ 低 |
| ufw | 仅开放 22/8787，默认拒绝 | ✅ |
| 备份 | /opt/backups 为空，cron 未执行 → 已手动补备份 | ⚠️ 见风险点 1 |

#### 二、今日安全新闻（2026-08-11/12）

- **PyPI 恶意 LiteLLM 包（供应链）**：两个恶意版本在 PyPI 存在约 40 分钟，窃取云密钥/SSH 密钥/K8s 令牌/数据库密码，可能波及 2100+ 组织。→ 与 Python 生态直接相关；本服务器不引入第三方 PyPI 包（venv 固定依赖），风险低。
- VMware vCenter CVE-2026-59310（CVSS 9.8）已遭在野利用（目录遍历→RCE，攻击者部署恶意 cron + reverse_ssh 持久化）。→ 不涉及本技术栈。
- Adobe ColdFusion CVE-2026-48362（10.0）、SAP Commerce CVE-2026-58231（10.0）：未认证 RCE。→ 不涉及。
- 微软 8 月补丁 398 个漏洞，含在野利用零日 CVE-2026-68820（Windows 内核驱动提权）。→ 不涉及。
- Cisco ASA/FTD CVE-2026-20349（8.6）在野利用（SSL VPN DoS）。→ 不涉及。

#### 三、风险点评估与行动

1. **备份 cron 未执行（P1）**：cron 服务 active，`0 3 * * * tar ...` 任务在但 /opt/backups 无产物（可能部署后未到 03:00 或 cron 环境问题）。已手动执行备份 `user_data_20260812.tar.gz`（14KB）✓。**待观察明日 03:00 是否自动产出；若仍无则查 crontab 转义与 cron 日志**。
2. cloud-init 更新被 keep back：非安全关键，随下轮 dist-upgrade 处理。
3. PyPI 供应链攻击与本服务器无关（无第三方 PyPI 依赖）；用户侧本地版 APP 同样无第三方 pip 依赖（requests 等已固定）。
4. 今日修复回顾：滑块验证码已整体移除（改为 QQ 邮箱验证码 + install_id + QQ 群注册门槛）；WebView 资产本地化（CORS/file:// 跨域）已部署验证。

#### 四、备份状态

- 手动备份：`/opt/backups/user_data_20260812.tar.gz`（14KB，今日 21:03 生成）✓
- 自动备份：cron 待观察（明日 03:00）
