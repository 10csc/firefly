# 阶段 0 · 保护网（先测试后重构）

> 目标：给后续所有改动装上保护网。本阶段**不修任何 bug、不动任何生产代码行为**，只加测试与修门禁。
> 预计 2-3 天。全部任务零行为变化。

---

### 任务 0.1：路由键集 oracle 测试
- 级别/来源：保护网 / 阶段 2 拆分的验收基础
- 目标：固化当前全部 HTTP 端点集合，拆分前后对比不变
- 证据：`app/routes.py:694-782`（POST_ROUTES/GET_ROUTES 分发表）
- 改动范围：新增 `tests/test_routes_oracle.py`（只允许新增文件）
- 步骤：
  1. 测试内 `import routes`，取 `sorted(POST_ROUTES)` 与 `sorted(GET_ROUTES)`；
  2. 与硬编码的期望集合逐字对比（期望集合从当前代码实际导出后贴入）；
  3. 断言每个端点可调用对象为函数。
- 验收：`python tests/test_routes_oracle.py` 退出码 0；此后每次路由变更必须同步更新期望集合（这本身就是防护）
- 回滚：删文件

### 任务 0.2：快照恢复三回归（R-01 / R-02 / E-1）
- 级别/来源：P0 已修复项的回归固化 / 审计报告 9.2
- 目标：把 2026-09-10 修掉的三个数据丢失路径用测试钉死
- 证据：`app/routes_data.py:294-475`（两阶段恢复）、`:169-199`（原子换入）、`app/routes_snapshot.py:226-239`（backup=not pre_ok）
- 改动范围：`tests/test_snapshot.py`（追加用例，不改既有断言）
- 步骤：
  1. 用例 A：构造含未注册自建包（`custom_t01/character/preset.json` + data）的全包快照 → 沙箱空环境恢复 → 断言包目录存在、`preset.json` 被注册、对话文件落位；
  2. 用例 B：monkeypatch 解压函数中途抛异常 → 断言旧目录原样（`.restore_old` 存在或未被触碰）；
  3. 用例 C：monkeypatch `build_full_snapshot_zip` 抛异常 → 断言 `_restore_full_snapshot` 以 `backup=True` 被调用（逐包备份回落）。
- 沙箱纪律：**绝不用真实 `user_data/`**；临时目录注入（参考 `.tmp_test/audit_pack_restore.py` 的手法，可复制其沙箱模式到测试里）
- 验收：三用例绿 + 全量测试绿
- 回滚：revert 测试文件改动

### 任务 0.3：门禁修复（D-1 + D-3）
- 级别/来源：P2 / 附录 D
- 目标：门禁在干净 clone 上可用；漏加模块/资产必失败
- 证据：`tools/check_version.py:20-40`（缺失文件返回 None 置 FAIL）；`tools/build_frontend_bundle.py:23-29`（ORDER 硬编码 18 项）；`tools/sync_frontends.py:30-34`（ASSET_FILES 硬编码 9 项）
- 改动范围：上述三个 tools 文件
- 步骤：
  1. `check_version.extract()`：文件不存在时标记 `SKIP`（显式打印"本次未校验 N 项"）而非 FAIL；存在但不一致的仍 FAIL；
  2. `build_frontend_bundle.py`：ORDER 改为「列 `app/static/js/*.js`（排除 bundle.js）→ 与期望集合比对，集合不等即 FAIL」；
  3. `sync_frontends.py`：ASSET_FILES 同理改集合断言（或目录约定+断言）。
- 验收：① 当前仓库三门禁 PASS；② 临时新增 `js/dummy.js` → bundle 门禁必 FAIL（测完删除）；③ 临时挪走 `server/version.json` → check_version 输出 SKIP 且退出码 0（测完还原）
- 回滚：revert

### 任务 0.4：后端一致性门禁入发版清单（R-03 收尾）
- 级别/来源：P2-partial / R-03
- 目标：`tools/check_backend_sync.py`（已存在，123 行）纳入发版校验链
- 改动范围：`docs/构建规范.md`、`docs/版本更新规范.md`（发版 checklist 加一项）
- 步骤：文档加「发版前必跑 `python tools/check_backend_sync.py --check`（或其实际参数，先读源码确认）」；顺带修正文档中"33/33 测试"口径为当前实际数量
- 验收：文档 checklist 与 `tools/` 实际脚本一致
- 回滚：revert 文档

---

## 阶段 0 出口标准
- [ ] `test_routes_oracle.py` + 快照三回归全绿
- [ ] 全量测试绿
- [ ] 干净 clone 上 check_version 可跑（SKIP 语义）
- [ ] 四门禁（check_version / sync_frontends / build_bundle / check_backend_sync）全部 PASS
