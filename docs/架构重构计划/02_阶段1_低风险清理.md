# 阶段 1 · 低风险清理（12 张任务卡）

> 目标：修掉全部「开放」的 P1/P2 小项。每张卡独立、可单独 revert；每张卡必须带一条回归测试。
> 预计 ~5 天。前置：阶段 0 出口标准全过。

---

### 任务 1.1：托管(proxy)额度加账号授权（C-2，P0）
- 证据：`server/server_app.py:287`（仅凭 `X-API-Mode` 头入托管）、`:77-95`（只查全站池）；`server/db.py:29` `quota_api_proxy` 列从未参与放行
- 目标：无授权账号不得使用托管模式；授权账号按日限额
- 改动范围：`server/server_app.py`、`server/db.py`（可加查询函数）、新增 `tests/test_proxy_quota.py`
- 步骤：
  1. `_setup_user_context` 中 `proxy` 判定加前置：`user["quota_api_proxy"] > 0 or user["role"] == "admin"`，否则 proxy 头静默降级为 relay（并在响应/日志可观测）；
  2. `_proxy_quota_check` 增加用户维度：该用户当日 `proxy_usage` ≥ 其 `quota_api_proxy`（作为每日次数语义）→ 返回额度用尽文案；全站池检查保留；
  3. `app/modules/app_config.py:973` 托管 Key 回落链移除 `DEEPSEEK_API_KEY`（只留 `FIREFLY_PROXY_KEY`）——防止消耗运营者主余额。
- 禁止：改 `proxy_usage` 表结构（列已够）；改 relay 链路
- 验收：测试——普通账号带 proxy 头 → 走 relay/被拒；quota=5 账号第 6 次被拒；管理台设额度后放行
- 回滚：revert（表结构未动）

### 任务 1.2：FIREFLY_SERVER 硬置（E-2b，P0）
- 证据：`server/server_app.py:35` `os.environ.setdefault`（环境置空串时失效）；`:622` main() 才硬置
- 步骤：`:35` 改 `os.environ["FIREFLY_SERVER"] = "1"`；`main()` 中重复行删除；日志打印一次平台标记确认
- 验收：构造 `FIREFLY_SERVER="" python server/server_app.py` → 启动日志与 `_discover_presets` 仍按服务器版（注册表仅 bundled）；新增测试覆盖
- 回滚：revert

### 任务 1.3：自动备份统一裁剪（C-3，P1）
- 证据：`app/routes_data.py:154-166`（`_backup_current_mode` 产 `auto-*`/`pre-restore-*`）；`:559-563` 裁剪只管 `{mode}-*` 手动备份
- 步骤：`_backup_current_mode` 写入后统一裁剪：`auto-*` 留最近 10 份、`pre-restore-*` 留 3 份（与 snapshot 侧口径一致）；裁剪失败仅告警
- 验收：测试连续触发 15 次导入 → `auto-*` ≤ 10
- 回滚：revert

### 任务 1.4：快照上传校验（C-6.14，P1）
- 证据：`app/routes_snapshot.py:96-99`（客户端上传 zip 不验内容）；恢复侧上限 60MB < 上传 120MB
- 步骤：上传分支校验 `PK` 魔数 + 可被 `zipfile.ZipFile` 打开 + 走 `_zip_safe_entries`；上传上限改为 60MB 与恢复对齐
- 验收：测试——非 zip/坏 zip 上传 → ok=False；合法快照正常
- 回滚：revert

### 任务 1.5：clear-history 主动性状态复位（C-6.1，P1）
- 证据：`app/routes.py:503-504` `_IGNORED.pop(mode)`；键实为 `(mode, scope)` 元组（`proactive_gate.py:32 _state_key`）
- 步骤：proactive_gate 增加 `reset_states(mode)`（遍历删该 mode 全部 scope 键），routes 改调它
- 验收：测试——清空历史后 `_ignored_count(mode)==0`、隐藏冷却重置
- 回滚：revert

### 任务 1.6：REGISTRY glob 匹配修复（C-6.3，P2）
- 证据：`app/modules/storage.py:126-135` 对 `{mode}/character/*.md` 按字面前缀匹配永不命中
- 步骤：`get_type` 对含 `*` 的注册项改两段式匹配（目录前缀 + 扩展名集合）；`character/*.md` 命中 `sync=newest`
- 验收：单测——`story/character/core.md` → newest；`story/character/x.exe` → 不命中
- 回滚：revert

### 任务 1.7：API 冷却加用户维度（C-6.4，P2）
- 证据：`app/modules/api_client.py:109-123` `_COOLDOWNS` 按端点全局
- 步骤：冷却 key 改 `(endpoint_key, user_scope_key())`
- 验收：测试——账号 A 触发冷却，账号 B 不受影响
- 回滚：revert

### 任务 1.8：/set-config 写锁（C-6.12，P2）
- 证据：`app/routes_config.py:38-72` 服务器版无锁读改写 `settings.json`
- 步骤：加 per-user 文件锁（`threading.Lock` 字典按 uid），读-改-写整体持锁
- 验收：测试并发双写均生效
- 回滚：revert

### 任务 1.9：当前包持久化（F-6.3，P2）
- 证据：`app/static/js/views.js` localStorage 仅 ui-scale/theme/particles；`/modes` 响应 `default` 字段前端未读
- 步骤：切包时 `localStorage.setItem("firefly_last_mode", id)`；启动读取顺序：last_mode → /modes.default → "story"；无效 id 回退默认
- 验收：手工——切到 haruno 刷新页面仍在 haruno；`sync_frontends.py` + `--check` 一致
- 回滚：revert

### 任务 1.10：孤儿包可清理 + 建包失败回滚（R-09，P2）
- 证据：`user_data/custom_f447527f/` 空目录真实存在；`routes_pack.py:324 delete_pack` 要求已注册、`:255 create_pack` 失败不回滚目录
- 步骤：① `create_pack` 任一步失败删已建目录；② `delete_pack` 增加分支：目录符合 `_PACK_ID_RE` 且未被注册且只含空 `character/` → 允许删除
- 验收：测试——中断建包残留目录可被删除；真实 `custom_f447527f` 可被清（手工确认一次）
- 回滚：revert

### 任务 1.11：knowledge_dirs 越界校验（R-10，P2）
- 证据：`app/modules/app_config.py:363-365` 仅 strip；`llm_retriever.py:52-59` 直接 `ROOT / d`
- 步骤：`_parse_preset` 拒绝 `..`/绝对路径/解析后不在仓库内的目录，告警并跳过该项
- 验收：测试——`../../etc` 被拒；合法目录正常
- 回滚：revert

### 任务 1.12：包详情页代际保护（F-3，P2，前端）
- 证据：`app/static/js/panels.js` `loadPackView` 无 `_modeGen` 捕获（对比 `chat_history.js:113,119` 已有正确范式）；写回用点击时 `CURRENT_MODE`
- 步骤：`loadPackView` 开头捕获 gen、await 后校验；保存/删除写回改用捕获的 mode
- 验收：手工——A→B 快速切换后保存，A 文案不写进 B；sync 三副本一致
- 回滚：revert

---

## 阶段 1 出口标准
- [ ] 12 张卡全绿（各带回归测试）
- [ ] 全量测试绿 + 四门禁 PASS
- [ ] architecture-refactor.html 视图⑫ 中这 12 项状态改为 fixed（下次刷新数据时）
