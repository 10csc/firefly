# 阶段 6 · 收尾、文档、验收、持续治理

> 目标：把本轮重构的成果固化成常态机制。预计 2 天 + 持续。

---

### 任务 6.1：文档对齐
- 更新 `readme.md`（项目结构段）、`docs/需求设计.md`（模块表）、`GLOSSARY.md`（新术语：packs.json/capabilities/archived）
- `docs/一期归档-架构重构/` 不动（历史归档）；本计划执行完后本目录整体归档到 `封装（没指定就不读）/` 并在此留指针
- 验收：文档与代码逐点核对无漂移

### 任务 6.2：观测指标进管理台
- 证据：审计报告 §11 观测指标表
- 步骤：管理台新增卡片——恢复失败率（/snapshot/restore、/import-data 的 ok=false 计数）、ignored_packs 非空次数、mode_fallback 出现次数、自建包数量
- 落点：主服务内存计数 + settings 表日级快照（沿用现有机制，不引新组件）
- 验收：制造一次失败恢复，管理台计数 +1

### 任务 6.3：发版四门禁固化
- `docs/版本更新规范.md` 发版 checklist 定稿：
  1. `python tools/check_version.py`
  2. `python tools/sync_frontends.py --check`
  3. `python tools/build_frontend_bundle.py --check`
  4. `python tools/check_backend_sync.py`（参数以源码为准）
  5. 全量测试（35+ 新增）全绿
- 验收：checklist 与实际脚本一一对应

### 任务 6.4：architecture-refactor.html 数据刷新
- 步骤：按最终态更新视图⑫ 问题状态（fixed 标注）、视图⑩ 行数、视图② 依赖图、视图⑬ 目标目录树改为现状树
- 验收：HTML 双击打开无 JS 错误（node --check + stub 跑渲，方法见本文件历史或重问我）

### 任务 6.5：协作记忆与 CLAUDE.md 更新
- `docs/协作记忆.md` 追加本轮重构结论与踩坑；CLAUDE.md 仅在结构约定变化时更新对应段

---

## 最终验收（全部阶段完成后）

- [ ] 无文件 >500 行（产物与大静态件除外）
- [ ] 全量测试绿；四门禁 PASS；PyInstaller 冒烟；安卓 assembleRelease 构建通过
- [ ] packs.json 权威 + 快照 manifest + 两级删除上线
- [ ] 老数据三时代演练零丢失；迁移用户可见
- [ ] C-2 托管授权等阶段 1 全部 P0/P1 关闭
- [ ] 架构图 HTML 与代码现状一致
