# 00 · 归档说明（先读）——架构重构方案集 · 分期状态

> 2026-08-23 归档。本目录 = **架构重构整体方案与第一期实施记录**，从主 docs 导航移出，内容保留可追溯。
> **主 docs 不再引用本目录**；本目录只作历史查询与后续规划参考用。

---

## ⚠️ 重要：本目录 ≠ 全部完成！

| 部分 | 内容 | 状态 |
|---|---|---|
| **方案 A（第一期）** | 02 + 08~12 + 0.8.1 | ✅ **已实施完成**（W0-W7 + Kimi 复审 + 发布部署） |
| **方案 B（服务化重构）** | `03_方案B_服务化重构.md` | 📝 **仅方案设计，未实施**（曾被评为"推荐路径"，但二期未启动） |
| **方案 C（平台化生态）** | `04_方案C_平台化生态.md` | 📝 **仅方案设计，未实施**（评委"现阶段不推荐直接上"） |
| 规划基础 | 01 / 05 / 06 / 07 | 方案依据与调研，供 B/C 后续实施参考 |

**说明**：B/C 方案文档与本目录一起归档，是因为它们与 A 属于同一套"架构重构"规划（共享 01/05/06/07 基础）。**将来若启动二期（方案 B），请在主 `docs/` 新建实施文档（如 `docs/二期-服务化重构/`），本归档目录仅作历史依据，不再追加。**

---

## 一、第一期（方案 A）完成状态 —— ✅ 已实施完成

| 阶段 | 内容 | 状态 |
|---|---|---|
| W0 | 热身（死资产清理 / 文档漂移 / 记忆修正） | ✅ |
| W1 | 配置地基（A8 多供应商：providers / 旧 SK 迁移 / 模型锁仅托管） | ✅ |
| W2 | 上游容错（error_code 透传 / 分类重试 / 预算超时 / 配额记账） | ✅ |
| W3 | 登录前提化（A7：/auth/verify 滚动续期 / 本地认证代理 / 登录闸门） | ✅ |
| W4 | 同步引擎（A1：清单 / 行级合并 / 新者胜 / 冲突备份 / 增量端点） | ✅ |
| W5 | 存储收口（A2：原子写 / 注册表 / per-user 覆盖 / WAL+schema 迁移） | ✅ |
| W6 | 图片消息（A9：压缩落盘 / desc / 识图 / 字节不出设备） | ✅ |
| W7 | 收尾（A5：入口收敛 / 菜单分组 / 数据面板） | ✅ |
| A7c | 安卓壳（本地优先 / 12s 探测回落服务器 / 模式开关删除） | ✅ |
| **Kimi 复审** | 3 严重 + 6 中修复 + 用户 3 条新拍板落地（图片经服务器+预压缩 / 云备份下线 / 压缩图=正式数据） | ✅ |
| **0.8.1** | 真机 E2E / 3 中修复（context time / cancel 监听 / /time 免登录）/ 双平台发布 / 服务器部署 | ✅ |

> 后续 bug 修复与功能迭代**不属于本期范围**，在本目录之外进行（见主 `docs/`）。

## 二、Kimi 最终审查结论（2026-08-23）

**裁决：无阻断缺陷，服务器侧干净，可进入部署流程。**

- 3 个中等问题（全部已修）：
  1. 记忆整理时间戳机制空转 → `context_manager.py` 三条写入补 `time` 字段；
  2. PC 取消选图卡死 `_mediaBusy` → `chat.js` 补 `cancel` 监听；
  3. 服务器 `/time` 免登录缺失 → `server_app.py` 公开 `/time`（本地版时间优先可用）。
- 5 个低危项（已记录，攒下轮）：`_insertRow` 回复动画 / proactive 默认参数（本次顺手修了函数体内硬编码）/ 邮件先标记后发送 / READ_MEDIA_IMAGES 死声明 / image:unseen 死代码。
- 验证：`tests/test_kimi_fixes.py`（9 项）+ 全量 37 脚本 PASS + check_version PASS。

**详细结论见 `12_最终修改确认.md` §六。**

## 三、文档查找指引

| 想找什么 | 看哪 |
|---|---|
| 做了什么・改了哪些文件 | `10_第一期执行日志.md`（完整施工记录） |
| 为什么要这么做（方案对比） | `02/03/04` + `01_现状诊断.md` |
| 每步怎么验收 | `08_第一期实施指导.md` + `09_第一期任务卡.md` |
| Kimi 审核了什么・修了什么 | `11_第一期复审与修订.md` + `12_最终修改确认.md`（§六） |
| 0.8.1 发布/部署细节 | `12_最终修改确认.md` + `10_第一期执行日志.md`（0.8.1 节） |

## 四、后续迭代提示

- 第 2 期（方案 B 服务化重构 / 前端 IA 重构）**未开始**；届时新方案文档请在主 `docs/` 新建（如 `docs/二期-服务化重构/`），勿放入本归档目录。
- 本目录文件**只读**：除非追溯历史，否则不改内容；确需修订请在副本上改并注明「修订于：…」。
