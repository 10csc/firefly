# 09 · 第一期任务卡（A1-A9 施工明细）

> 2026-08-21。配套 `08_第一期实施指导.md` 使用；每卡 = 目标 / 改哪里 / 新建 / 要点 / 验证。
> 行号出处以 2026-08-21 工作区为准（git HEAD `1232567` 之后有前端拆分未提交改动，实施前先 `git status` 确认基线）。

## W0 热身

### 卡 A6 · 顺手清理（零风险）

- **改**：删 `user_data/story/data/.round_count`（全仓库无引用，01 §3.4）；`wiki-compiled/`、`scenes/` 归档进 `_trash/`（默认拍板，08 §3）；文档漂移四处：`docs/需求设计.md:22,66-72` 旧布局、`docs/服务器管理规范.md:55,94` 模型锁描述、`android/README.md` 旧形态、备份口径（W5 才定稿，本轮先标注）；`build.gradle.kts` GBK 乱码注释 6 处转 UTF-8（`:10-11,31-34,38,73,84,102,110`）。
- **验证**：grep 确认 `.round_count` 无引用后删；应用启动回归一轮 chat。

### 卡 A4 · 记忆子系统修正

- **改**：「跨会话」→「这段对话的过往摘要」五处（`polisher.py:37,347`、`proactive.py:432`、`routes.py:1237`、`panels.js:727`）；修三真 bug——rest 后刷新当前会话 memory_head（`routes.py:526-545`）、undo 回退 `.memory_index`（`routes.py:826-838`）、rest 结果文案先藏遮罩导致不可见（`chat.js:681-692`）；精力值 `energy/fatigue_visible` 标注废弃不删（`context_manager.py:251-258`）。
- **验证**：`python tests/test_memory_manager.py`；手动 rest → 不发新消息直接确认新头部生效；undo 后 rest 确认撤回轮不被整理。

## W1 配置地基

### 卡 A8 · 多供应商模型配置

- **目标**：providers 配置结构 + 存量 SK 自动迁移 + OpenAI 兼容默认 + DeepSeek 体验做好。细则见 02 A8。
- **改**：
  - `app/modules/app_config.py`：`_load_config()`（`:246-260`）增加 `providers` 结构；启动迁移旧 `api_key`/`api_base` → deepseek provider（迁移前备份 config.json）；`VALID_MODELS`/端点白名单（`:127-132,270-278`）改内置建议清单；模型锁三道按模式分支——仅 proxy 托管锁（`:275-278,347-350`）。
  - `app/modules/api_client.py`：`extra_body` thinking 注入（`:67-68`）与 `reasoning_content`/缓存字段解析（`:145,152-153`）移入 provider `caps` 分支；unknown param 报错 → 剥离 extra_body 重试一次并回写 caps；`QuotaClient`（`:192`）锁逻辑保留但改读服务器路由配置。
  - `app/modules/llm_base.py`：`_PRICING`（`:164-168`）按 provider 组织 + 峰谷标注；未知模型成本显示「未知」（废 `:171-173` 静默回退）。
  - `app/routes.py`：`/set-config`（`:128-210`）改写 providers 结构；`/config`（`:1107-1135`）返回 providers；新增 `GET /models?provider=` 转发 `{base_url}/models`（本地版后端带 Key）；`/balance`（`:1143-1154`）仅 deepseek provider 返回。
  - 前端：`panels.js:234-247,264-268,307` 删中文 preset；`index.html:497-500,507-549` 档位文案改官方英文名（deepseek-v4-flash / deepseek-v4-pro / deepseek-v4-flash-vision-exp）；`:424` 获取 Key 教程按 provider 动态化；`:434-438` api-base 下拉改供应商管理 UI + 「获取模型列表」按钮；relay 模式前端直连供应商 `/models`（Key 在浏览器，同 `relay.js:103-107` 链路）。
  - `server/server_app.py:242-245`：relay 头部 base 白名单放开为 http(s) 校验；proxy 模式不接受用户 base_url。
- **要点**：思考档位 low/high/max 直通（删 effort_map 的 low→high 映射，四处：`llm_retriever.py:136-139`、`analyzer.py:146-149`、`polisher.py:318-322`、`organizer.py:133-136`）；同步改 CLAUDE.md 同句（E1）与 `docs/需求设计.md:5`（E5）。
- **验证**：新建 `tests/test_providers.py`（迁移幂等/旧字段删除/caps 探测回写/锁仅托管生效）；readme 测试集回归；本地 mock 一个 OpenAI 兼容端点跑通一轮 chat。

## W2 止血

### 卡 A3 · 上游托管容错

- **目标**：error_code 透传第一刀，再补重试/冷却/超时。细则见 02 A3。
- **改**：四模块 `except Exception` 降级输出携带 error_code（`llm_retriever.py:181-186`、`analyzer.py:221-226`、`polisher.py:417-426`、`organizer.py:202-207`）→ `orchestrator.py` 聚合首个非空 code 进 /chat 响应；`fix.js:243-248` 补读 error_code；`api_client.py` 分类重试（429/5xx/网络 Full Jitter ≤3 次，401/400/余额不重试，遵守 Retry-After）+ relay 链路补重试（`:315-324`）+ 端点冷却 60s + 超时分档（non-think 30s / think 90s / 单轮硬顶 210s）；relay 活性快速失败（`/relay/pending` 轮询即心跳，>30s 无轮询立即失败）；前端消费 `waited`（`routes.py:917-918`）做等待预警；配额条目按 08 §3 默认拍板（失败记 `proxy_usage_failed`）。
- **验证**：mock 各类 ApiError 断言透传到 /chat 响应；`tests/test_orchestrator.py` 回归；手动断网/错 Key 看前端 toast。

## W3 登录

### 卡 A7 · 登录基本操作化 + 自动后端选择

- **目标**：登录 = 使用前提；本地优先、失败自动连服务器；删双模式切换。细则见 02 A7。拆三个子任务：
- **A7a 服务器侧**：新增 `POST /auth/verify`（token 校验+续期）；确认 30 天会话逻辑（`server/auth.py`）对本地缓存 token 的语义；首次启动引导流程文案（登录优先于注册，决策 10）。
- **A7b 本地版 + 前端单态**：`user_data/auth.json` 凭证缓存（与 config.json 分离）；无凭证 → 引导页；30 天离线宽限；`app/static/config.js` 与 `server/frontend/config.js` 合并为一份（当前后端形态由 `/config` 下发）；`api.js` 恒带 Bearer token；relay 激活条件改「当前后端是服务器」（`relay.js:139`、`main.js:33`）。
- **A7c 安卓**：删 SharedPreferences 模式与杀进程切换（`MainActivity.kt:59-72,489-506`）、移除 `FireflyMode` bridge；启动健康检查（`/health` 连续失败）→ 自动连服务器，界面显示「云端连接中」。
- **验证**：新装 E2E（注册→登录→宽限期断网可用）；安卓真机测内嵌启动失败回落（可临时改端口模拟）；`tests/test_auth_reset.py` 扩展 verify 用例。

## W4 同步

### 卡 A1 · 文件级双向同步

- **目标**：登录态常驻同步（文字类）。细则见 02 A1。
- **新建**：`app/modules/sync_engine.py`（清单协商/冲突裁决/原子替换）；服务器端点 `POST /sync/manifest`、`POST /sync/push`、`GET /sync/pull`（挂 `routes.py:1687-1742` 分发表）。
- **改**：`/sync/upload|download`（`routes.py:1058-1104`）标注「整包备份」；`export_data`（`:921-943`）纳入 config.json（剥 api_key）+ 表情包元数据，修 docstring（`:922`）。
- **要点**：append-only 按 seq 行级合并；文档类新者胜+双方备份；媒体不进清单（表情包只同步 `{label,category,enabled,内容哈希}`）。
- **验证**：新建 `tests/test_sync_engine.py`（合并/冲突/中断恢复）；双端（PC + 安卓真机）互同步回归。

## W5 存储收口

### 卡 A2 · 统一存储层 + 服务器加固

- **改**：新建 `app/modules/storage.py` 收口路径公式（4 处重复迁移：`app_config.py:180`、`conversation_store.py:104`、`memory_manager.py:55`、`sticker_picker.py:70`）与原子写（3 处）；数据类型注册表含「媒体类?」执行点；`/add-sticker` 服务器版改经过式不落盘（`routes.py:548-584`，前端存放形态按 08 §3 默认 IndexedDB）；`/assets/` 只服务内置/公共池；补 gif MIME（`shared_http.py:15-21`）；删表情包清孤儿文件（`sticker_picker.py:316`）；`/set-config` 按用户隔离；`server/db.py` 加 `schema_version` 表 + WAL + 限流计数落 settings（`server/auth.py:9`）；HTTPS Caddy 前置 8787 + 关 `usesCleartextTraffic`（`AndroidManifest.xml:24`）+ 落地 `server/frontend/config.js:2-3` 待办。
- **验证**：store 系列测试回归（conversation/favorite/setting_fix/sticker）；服务器部署演练（备份→升级→回滚）。

## W6 图片

### 卡 A9 · 图片消息与视觉理解

- **目标**：铁律「服务器只传输不保存」。细则见 02 A9（首轮直接识图主路径）。
- **改**：`conversation_store.py` 加 `type=image`（`:243-254` 校验、`:45` 引用白名单、`:78-93` quote、`:379-399` hydrate）；新增 `POST /upload-image`（本地版落盘 `user_data/{mode}/images/`；proxy 托管经过式内存 TTL 队列）；desc 生成器在 orchestrator 之前（路由层完成，`[图片：desc]` 并入 user_input）；polisher 输入装配支持 content blocks（仅 caps.vision 时首轮带图）；relay 占位符 `__IMG_{img_id}__` + `relay.js` 填充；pipeline.jsonl 落盘前剥离图片 block（`orchestrator.py:372-409`）；前端 `chat.js` 渲染/快照/引用加 image 分支（`:345-347,757-759,152-174,67-72`）+ 输入区发图按钮；安卓文件选择器 accept 扩展（`MainActivity.kt:441-456`）。
- **验证**：`tests/test_conversation_store.py`、`test_quote_favorite.py` 加 image 用例；mock vision 响应测 desc 降级；铁律核验按 08 §4.4-4.5 执行。

## W7 收尾

### 卡 A5 · 前端入口收敛

- **改**：安卓三视图改底部 tab（聊天/首页/我的）；菜单 6 tab 分「与她相关/与系统相关」；设置「数据与系统」整合为「数据」子页并明示媒体本地策略；PC 仅同步数据子页。细节按 05 A 档。
- **验证**：三端 UI 走查 + `sync_frontends.py --check`；总验收按 08 §4 全表过。
