# 0.8.0 双模式整合 — 检查点（上下文压缩用）

> 本文件供压缩上下文后的会话/子代理恢复状态。最后更新：2026-08-14。

## 一、任务总目标
流萤双模式整合 0.8.0：一个安卓 APK 默认完全本地、可开服务器后端（含托管额度池）；三版本缩减为两版本（安卓合并版 + Windows 本地版）；服务器后端保留。已获用户全权授权（含真机验证）。

## 二、已完成（已验证）
1. **阶段 A 数据同步**：`POST /import-data`（zip 覆盖导入+自动备份+zip slip 防御）、`POST /sync/upload`（账号云端备份留 3 份）、`GET /sync/download`（取回）——`app/routes.py`；`parse_multipart` 增加 max_bytes 参数。冒烟测试全过。
2. **阶段 B 前端合一**：`app/static` 为唯一源；`window.FIREFLY_MODE` 分支（config.js 注入）；登录/注册/重置表单**内联进首页 auth 模块**（不跳 login.html）；relay 引擎/登录模块/API 来源/Key 存取/更新源/导出方式按模式分支；导入+账号备份按钮；401 toast 仅对 `/chat` 提示（轮询降噪）；style.css 字体/背景改相对路径。
3. **阶段 B 安卓壳**：`android/MainActivity.kt` 双模式（SharedPreferences `firefly_mode`；local 启动 Chaquopy 引擎，server 直接 file:// 加载；config.js 拦截注入；`FireflyMode` JS 桥 setMode/getMode（保存后 finishAffinity+killProcess 重启）；`onShowFileChooser` 文件导入；`shouldOverrideUrlLoading` file:// 导航 post 到主循环 loadUrl）；`KeepAliveService.kt` 按模式分流（local=Python backdoor / server=页面 __serverProactive）；`build.gradle.kts` 加 `syncFrontendAssets` 任务（app/static 全量 + app/assets 9 件 + server/frontend 的 config.js/login.html → assets）。
4. **工具**：`tools/sync_frontends.py`（同步/`--check` 校验）；`check_version.py` 7 处（去 android_server）；`server/download_server.py` 下载页文案（单 APK 双模式）。
5. **版本 0.8.0**：7 处全部一致（check_version PASS）。
6. **已删除 `android_server/` 目录**。
7. **已部署过一次服务器**（app/ + server/ 全量 scp + 双服务重启 + health OK）——但**内联表单与 toast 降噪是其后改动，服务器前端需再部署**。
8. **APK 已构建并装机**（704c2441，服务器模式）：首页干净、无启动 toast、登录卡片显示「登录 / 注册」按钮（内联 toggle）。待验证点见下。

## 三、关键环境
- 工作区 `F:\CodeFile\firefly`；adb `F:\CodeFile\firefly\android\sdk\platform-tools\adb.exe`；设备 704c2441。
- 识图：`python "C:\Users\FANGL\.agents\skills\vision\vision.py" "<图片绝对路径>" "<问题>"`（mimo-v2.5）。
- SSH：`ssh -i C:\Users\FANGL\.ssh\id_rsa -o BatchMode=yes root@101.200.14.126 "<cmd>"`；服务器 /opt/firefly（firefly-server 8765 本机）+ /opt/firefly-downloads（8787 网关，download_server.py 单独 scp 到该目录）。
- 版本校验 `python tools\check_version.py`；前端同步 `python tools\sync_frontends.py` / `--check`。
- 单测：`tests/test_*.py` 逐个 `python` 直跑（非 pytest；e2e_20turns/manual_10turns 需真实 Key 跳过）。

## 四、剩余任务（按序）
### 1) 真机验证（服务器模式，应用当前就在服务器模式首页）
- 注意：tap 坐标随公告面板开合/轮播变化——每次 tap 前先 screencap，用**当帧截图**定位（vision 问坐标或本地 PIL 像素扫描）。
- a. 点登录卡片右侧「登录 / 注册 ▾」toggle → 内联登录表单出现（邮箱/密码/登录 + 没有账号？注册 + 忘记密码？）。
- b. 点「没有账号？注册」→ 注册表单（QQ 邮箱/验证码/密码/QQ群号/注册）。
- c. 输入假邮箱 `abc@163.com` → 点「获取验证码」→ 应报「请使用 QQ 邮箱」（本地校验）。**禁止对真实 QQ 邮箱触发发码**（会真发邮件）。
- d. 首页静置 ~30s → 确认无「请先登录后使用」toast（此前每 10s 弹一次）。
- e. 反复开合表单数次 → 页面内容保持完整（此前症状：点登录后页面内容消失只剩背景）。
- f. 每步截图 + vision 记录结论（截图存 F:\CodeFile\firefly\verify\ 目录，最后清理或保留前 4 张）。

### 2) 服务器再部署（内联表单前端）
```powershell
scp -i C:\Users\FANGL\.ssh\id_rsa -r -q F:\CodeFile\firefly\app F:\CodeFile\firefly\server root@101.200.14.126:/opt/firefly/
ssh -i ... root@... "systemctl restart firefly-server; sleep 2; systemctl is-active firefly-server; curl -s http://127.0.0.1:8765/health"
```
验证公网：`http://101.200.14.126:8787/login` 200；`/config.js` 含 FIREFLY_SERVER_BASE。

### 3) PC exe 构建（后台，不发布）
`python -m PyInstaller firefly.spec --noconfirm`（工作区根；~5-10 分钟）。产物 dist/firefly/firefly.exe。**只构建不发布**（发布须用户明确指示）。

### 4) 文档更新
- `docs/协作记忆.md`：追加 0.8.0 双模式完成状态 + 真机验证结论 + 登录页「崩溃」根因（file:// 导航被 allowFileAccess 拦/导航竞态 → 已改内联表单）与 toast 降噪修复。
- `docs/部署说明.md`：补双模式 APP 说明 + 数据同步端点（/import-data、/sync/upload、/sync/download）。
- `docs/问题清单与待办.md`：追加 0.8.0 记录（P0-4 导入完成）。
- `docs/README.md` 如需则补索引（可选）。

### 5) 清理
- 杀残留本地引擎：检查 127.0.0.1:8765（此前冒烟启动过 `python app/server.py`，若在跑：找到 PID kill）。
- 删除仓库根临时截图：firefly_*.png、v1*.png、h.png、r*.png、d2*.png、pk.png、now.png、ui.xml、check_db_remote.py（已删）、*.zip 无关文件等（保留 verify/ 中前 4 张验证图即可，或全删）。

### 6) 终检
- `python tools\check_version.py` PASS；`python tools\sync_frontends.py --check` 一致；15 个单测脚本全绿（沙箱已解除，直接跑）。

## 五、输出要求
返回结构化报告：① 真机验证每步结论（截图文件名）；② 服务器部署结果；③ PC 构建结果（成功/失败+产物）；④ 文档更新清单；⑤ 清理项；⑥ 终检结果；⑦ 任何未完成/异常。不要替用户做发布动作、不要 git 提交。
