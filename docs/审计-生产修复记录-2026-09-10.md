# 生产环境漏洞修复记录（2026-09-10）

> 目标：生产服务器 `101.200.14.126`（hostname `iZ2zedbksqxvyh37gnhmdiZ`）
> 执行方式：SSH（`C:\Users\FANGL\.ssh\id_rsa`），仅改必要文件；改前逐文件备份
> 本地验证：仓库 35 个测试脚本全绿 + 4 个新增专项测试（79 项断言）全绿
> **合规：未读取/未导出任何用户原文、对话内容、角色卡内容；密钥仅输出长度与前 3 位掩码**

---

## 一、修复前的关键判定（线上代码基线）

线上 `app/` 与 `v0.8.1` tag 逐文件 md5 比对（28 个文件）：

| 分类 | 数量 | 说明 |
|---|---|---|
| 逐字节一致 | 15 | — |
| 仅 CRLF 差异（内容 = v0.8.1） | 9 | scp 从 Windows 部署所致（size 差值恰好等于行数） |
| **内容偏离** | **3** | `api_client.py`、`multipart.py`、`routes.py` |
| 与 HEAD 一致 | 0 | 线上**不是**开发版 |

对 3 个偏离文件做**结构级 diff**（去 CRLF 后比对函数签名/导入/hunk）：

| 文件 | 差异 | 结论 |
|---|---|---|
| `modules/api_client.py` | +64 行，新增 `_ip_public()`、`is_public_endpoint()` + `ipaddress/socket/urlsplit` 导入 | **SSRF 双层防御**（提交 `8922a56` 的内容） |
| `routes.py` | +19 行 / −1 行（4 个 hunk） | 同一加固：导入 `is_public_endpoint` + 相关校验 |
| `modules/multipart.py` | +4 行 / −1 行 | 同一加固：畸形 multipart 头安全拒绝 |

→ **C1 疑虑解除**：线上不是未知分叉，而是 **v0.8.1 + 安全加固补丁**。修复以线上文件为基线，未覆盖加固内容。

---

## 二、修复清单

| 序 | 等级 | 问题 | 修复内容 | 涉及文件 |
|---|---|---|---|---|
| 1 | **P0** | `/pipeline` 跨账号泄漏：`get_pipeline_log` 在内存非空时返回**进程级全局** `_PIPELINE_LOG`（含用户原话/完整回复/思考过程/注入的知识库） | ① `_record_pipeline` 写入 `_scope=user_scope_key()`；② `get_pipeline_log` **不再读全局列表**，只读本用户落盘文件并校验 `_scope`；旧格式（无 `_scope`）一律跳过（宁可少显示不跨界） | `app/orchestrator.py` |
| 2 | **P0** | 托管模式无授权：`X-API-Mode: proxy` 仅凭请求头生效；`users.quota_api_proxy` 从未被校验 | ① 入口授权门：仅 `role==admin` 或 `quota_api_proxy>0` 可进入托管（否则 `proxy=False`，回落自带 Key 流程并记 `[PROXY-GUARD]` 日志）；② 额度检查改**两级**：账号级（`proxy_usage_get_user`，用满即拒，读不到用户 fail-closed）+ 全站池（保留运营者保护）；③ 新增 `db.proxy_usage_get_user()` / `db.set_user_quota()`；④ 新增管理台端点 `/admin/api/users/quota` + `admin.html` 配额列（可点击编辑） | `server/server_app.py`、`server/db.py`、`server/frontend/admin.html` |
| 3 | **P0** | `/import-data` 破坏性覆盖：先 `rmtree(root)` 再逐个解压，中途失败留下空目录/半写状态且不可回滚；且"导入前自动备份失败"被 `except: pass` 静默吞掉 | ① 新增 `_swap_dir_into_place()`：解压到同盘临时目录后整体 `rename`，失败时目标未动（含回滚分支）；② **备份失败即中止导入**（返回明确文案）；③ 模块级 `import shutil` | `app/routes.py`（线上）、`app/routes_data.py`（开发版，含多包恢复同样原子化） |
| 4 | P1 | `/requests`、`/metrics` 无用户维度 → 任一账号可见全站用量与运营成本 | 两端点在服务器版返回 403（仅本地版可用） | `app/routes.py` |
| 5 | P1 | `_migrate_legacy_layout()` 在 **import 期**执行 → 服务器每次启动都对**共享** `user_data` 根做移动式迁移；`move` 失败静默吞错；零备份 | ① 改为显式 `run_legacy_migration()`（服务器版永不调用，本地版入口 `main()` 调用）；② 迁移前打 `user_data/legacy_pre_move.zip`，**备份失败即中止迁移**；③ `move` 失败逐条 warning + 汇总；④ 首启引导（建目录 + 拷贝默认文件）收敛为 `run_startup_init()`，**在迁移之后**调用（保住"迁移必须先于默认拷贝"的顺序铁律） | `app/modules/app_config.py`、`app/server.py` |
| 6 | P1 | （实施中自测发现）`_legacy_targets()` 调用 `mode_character_dir()` 有 **mkdir 副作用** → 无旧布局时也会凭空建 `user_data/story/` | 改为纯路径计算（不调用 `mode_*_dir`） | 同上 |
| 7 | — | （实施中发现并撤销）曾尝试"启动时固化 config providers 结构"以减少反复迁移备份，但 `save_config` 定义在赋值点之后 → `NameError`（已被 try/except 安全降级，无实际影响）。因属优化非安全项，**已撤销**，不留隐患 | — | — |

**未做（有意）**：未动 `api_client.py`/`multipart.py`（线上加固版，无需改）；未改任何权限、systemd、nginx；未清理 `user_data/story/character/` 遗留文件（休眠数据，清理需你确认）。

---

## 三、验证证据

### 3.1 本地验证（改后、部署前）

| 验证 | 结果 |
|---|---|
| 仓库全量回归（35 个 `tests/test_*.py`） | **35 通过 / 0 失败** |
| 新增 `test_p0_1_pipeline_isolation.py` | **10 / 10**（含两用户交叉读取断言：用户2 读到 0 条） |
| 新增 `test_p0_2_proxy_authz.py` | **26 / 26**（真实 SQLite 验证 + 授权门 8 种输入 + fail-closed） |
| 新增 `test_p0_2b_quota_admin.py` | **22 / 22**（`set_user_quota` 真实 UPDATE + 门联动 + 端点静态断言） |
| 新增 `test_p1_migration.py` | **21 / 21**（零副作用 / 顺序铁律 / 备份产生 / 失败可见 / 幂等） |
| 语法检查 | 12 个改动文件全部 `ast.parse` 通过 |

> 说明：批量连跑时 `test_fault_tolerance.py` 曾出现一次 exit=1（其内部 `PASS=26 FAIL=0`，含 0.50s 时间余量断言）；单独复跑 **3 次均 exit=0**，判定为负载抖动导致的 flaky，非本次改动引入。

### 3.2 部署过程

| 步骤 | 结果 |
|---|---|
| 备份（第 1 批） | `/opt/backups/code_pre_auditfix_20260910-213115`（6 文件 + md5 清单） |
| 备份（第 2 批） | `/opt/backups/code_pre_batch2_20260910-213449`（4 文件） |
| 传输 | 本地字节 → base64 → stdin → 远端 `base64 -d`（CRLF 原样保留） |
| 哈希校验 | 第 1 批 6/6、第 2 批 4/4，**本地↔线上逐字节一致** |
| 重启 | `systemctl restart firefly-server`，两次均 `active`，**无导入错误** |

### 3.3 部署后线上验证

| 项 | 结果 |
|---|---|
| 服务 | `firefly-server` **active**、`firefly-downloads` **active** |
| 端点 | `/health` 200 · `/version.json` 200 · `/config` 401 · `/pipeline` 401 · `/metrics` 401 · `/requests` 401 · `/admin`（本机无令牌）403 |
| P0-1 | `get_pipeline_log` 读全局列表计数 = **0**；`entry["_scope"]` 写入 = 2 处 |
| P0-2 | `PROXY-GUARD` = 1 · 账号级额度检查 = 1 · db 新函数 = 2 · 管理台端点 = 1 · UI 入口 = 2 |
| P0-3 | `_swap_dir_into_place` = 1 |
| P1 | 运维端点门控 = 2 处 · `run_legacy_migration` = 1 · import 期自动调用 = **0** |
| 共享根 | 非数字目录仍只有 `stickers`/`story`（mtime 均为 08-12，**无新增**）；`story/character` 文件数仍为 3；`legacy_pre_move.zip` **不存在**（零副作用成立） |
| DB 授权现状 | 总账号 5 · `quota>0` = **0** · admin = 1（即当前**托管仅管理员可用**） |

---

## 四、回滚方案（秒级）

```bash
# 单文件回滚（示例：orchestrator）
cp -p /opt/backups/code_pre_auditfix_20260910-213115/app/orchestrator.py /opt/firefly/app/orchestrator.py
# 全量回滚第 1 批
BK=/opt/backups/code_pre_auditfix_20260910-213115
cp -p $BK/app/routes.py /opt/firefly/app/ ; cp -p $BK/app/orchestrator.py /opt/firefly/app/
cp -p $BK/app/server.py /opt/firefly/app/ ; cp -p $BK/app/modules/app_config.py /opt/firefly/app/modules/
cp -p $BK/server/server_app.py /opt/firefly/server/ ; cp -p $BK/server/db.py /opt/firefly/server/
systemctl restart firefly-server
```
第 2 批同理用 `code_pre_batch2_20260910-213449`。**回滚不涉及用户数据、DB schema、systemd。**

---

## 五、运维后续（需要你决定）

1. **托管白名单**：当前 `quota>0` 账号数 = 0 → 托管仅 admin 可用。要给某账号开通：管理台（`ssh -L 8766:127.0.0.1:8766`）→ 用户表「托管额度」列点击 → 填次数/日。**这是有意的安全默认**（修复前是"人人可用"）。
2. **`/pipeline` 面板**：为杜绝跨界，**无 `_scope` 字段的历史条目一律不显示** → 管理员面板在重启后会暂时为空，发一条消息即恢复。
3. **遗留目录** `user_data/story/character/`（3 个文件，08-12~08-16，不被任何账号读取）：可清理，等你确认后再动。
4. **代码可追溯性（仍未解决）**：`server/` 不在 git、`/opt/firefly/.git` 不存在 → 建议把 `server/` 纳入版本控制；本次修复的线上版本已留 2 份时间戳备份，但**长期仍靠人工**。
5. **备份裁剪**：`backups/` 的 `auto-*`/`pre-restore-*` 在开发版仍无上限（线上 v0.8.1 无快照、仅有 7 天 cron），未在本次范围内。

---

## 六、第二批：开发版角色卡修复（本机，未部署到服务器）

线上 v0.8.1 不含角色包体系，故这批只改本仓库开发版（不影响线上）。

| 序 | 等级 | 问题 | 修复 | 涉及文件 |
|---|---|---|---|---|
| 8 | **P0** | **R-01 自建包在快照恢复中丢失**（沙箱已复现：`ok=False "快照里没有可恢复的数据"`） | 恢复改**两阶段**：先暂存并落地**包定义**（`character/`）→ `reload_presets()` → 再搬用户数据；白名单从"启动期 `cfg.MODES`"改为"包 id 合法 + 含 preset.json"；新增 `_pack_restorable()` 护栏（内置包不允许被快照覆盖）；顶层无法识别者记 `ignored_packs` 日志 | `app/routes_data.py` |
| 9 | **P0** | **F-1 前端无导出入口** → 自建包**没有任何可用备份路径**（快照曾丢包 + 无法导出 = 建包必丢） | 设置页新增「导出当前角色（zip）」按钮 + `exportData()`（走既有 `GET /export-data?mode=`，本地/服务器双基址），并在文案中点明它是唯一可离线留存的备份方式 | `app/static/index.html`、`app/static/js/chat.js`（三端已同步） |
| 10 | **P0** | R-02 恢复非原子（`rmtree` 后逐个解压，失败不可回滚） | 新增 `_swap_dir_into_place()`：解压到同盘临时目录后整体 `rename`，失败时目标未动（含回滚分支）；**备份失败即中止导入** | `app/routes_data.py` |
| 11 | **P0** | E-1 `backup=False` 无条件 → "唯一保险"（pre-restore 快照）可能不存在 | 改为 `backup=not pre_ok`：pre-restore 快照成功才跳过逐包备份；失败则回落逐包备份；响应新增 `backup_ok` | `app/routes_snapshot.py` |
| 12 | P1 | R-04 bundled 更新被用户副本**永久遮蔽**（实测） | ① **不再预拷贝**设定到用户区（读取链路本就 user→bundled 回落，用户编辑时自然写出副本）；② 启动时清理"与 bundled 逐字相同"的历史副本，**内容不同一律保留**（不误删用户内容） | `app/modules/app_config.py` |
| 13 | P1 | R-05 无 schema 版本号（`bans` 是死字段） | 新增只读 `schema`（缺失=1、过高仅告警、脏值回退）+ `PRESET_SCHEMA` 常量；**两处写盘点同步写入**（`pack_forge.forge_finish`、`routes_pack.create_pack`），避免重蹈 `bans` | `app/modules/app_config.py`、`app/modules/pack_forge.py`、`app/routes_pack.py` |
| 14 | P1 | R-06 知识库缓存无用户维度、无失效入口 | 缓存 key 加 `user_scope_key()` + 新增 `clear_knowledge_cache()`；`llm_base.clear_cache()` 连带清理（编辑设定/导入/恢复后立即生效） | `app/modules/llm_retriever.py`、`app/modules/llm_base.py` |
| 15 | P1 | **F-2 自建包「恢复默认」= 删唯一副本**（按钮文案误导、不可逆） | 新增 `_no_bundled_fallback()`：自建包（无 bundled 对应物）拒绝删除并给出明确文案；内置包行为不变 | `app/routes_pack.py` |
| 16 | P1 | R-07 未知 mode **静默**回退默认包（PC 建包→服务器模式会让"角色变了"且同步会串数据） | 新增 `_body_mode_ex()/_query_mode_ex()` 返回 `(mode, fell_back)`，保留旧函数兼容；`/chat` 响应新增 `mode_fallback` + `mode_used` | `app/routes_common.py`、`app/routes.py` |
| 17 | P1 | R-03 仓库内安卓后端副本漂移且**三门禁都不校验** | 新增 `tools/check_backend_sync.py`（文件集合 + md5 比对，`--sync` 可修）；已执行 `--sync` 使仓库副本与源一致 | `tools/check_backend_sync.py` |

### 6.1 实施中自测发现并修掉的 4 个自身问题

| # | 问题 | 处置 |
|---|---|---|
| a | `_legacy_targets()` 调用 `mode_character_dir()` 有 mkdir 副作用 → 无旧布局时也凭空建 `user_data/story/`，破坏"零副作用"承诺 | 改为纯路径计算 |
| b | "启动时固化 config providers 结构"中 `save_config` 定义在赋值点之后 → `NameError`（有 try/except 兜底，无实际影响）；因属优化非安全项 | **整段撤销**，不留隐患 |
| c | `_DEFAULTS` 是模块级字典、绑定 import 时的 `USER_DIR` → 运行时替换 `USER_DIR`（测试/多根部署）会指向旧路径 | 新增 `_defaults_map()` 按当前路径重算 |
| d | "先清理、后拷贝"顺序导致每次启动"删了又拷回"，日志误报清理数 | 改为"不再预拷贝"，清理只针对内容与 bundled 相同的历史副本 |

### 6.2 第二批验证

| 验证 | 结果 |
|---|---|
| 仓库全量回归（35 个脚本） | **35 / 0** |
| `test_r01_pack_restore.py` | **24 / 0**（含原缺陷点由 `ok=False` 变 `ok=True`、内置包护栏、zip slip、幂等） |
| `test_r04_r05_r06_f2.py` | **24 / 0**（R-04 三种情形：同内容清理 / 不同内容保留 / 删副本后更新送达） |
| `test_r07_mode_fallback.py` | **13 / 0** |
| 三门禁 | 版本 `PASS 可发布` · 前端 `一致` · 后端镜像 `一致 ✓` |

> 测试过程中出现过 5 次"失败"，逐一查明**全部是测试自身缺陷**（前置状态被前一小节污染、断言字符串与标记同名、`fresh()` 清目录时机、构造的"陈旧副本"其实与 bundled 不同），代码逻辑正确。已全部修正并复跑通过；其中"构造的陈旧副本与 bundled 不同"一条促使我把 R-04 测试重写为三种真实情形的完整语义验证。

---

## 七、本次未做 / 建议后续

| 项 | 说明 |
|---|---|
| 前端对 `mode_fallback` 的提示 UI | 后端已回传标记，前端尚未因此弹提示（可后续在 `chat.js` 读 `data.mode_fallback` 显示"该角色在服务器版不可用，已按默认角色回复"） |
| `R-13` 建卡向导草案落盘 | 仅内存 + 30 分钟 TTL，未改 |
| `R-09` 孤儿包目录可清理 | 未加删除入口（线上 `user_data/story/character/` 遗留 3 文件仍在，等你确认后再动） |
| `R-10` `knowledge_dirs` 路径校验 | 未加（越界路径风险有限，属加固项） |
| `F-5` 自建包无头像时沿用上一个包的图 | 未改（属 UI 润色） |
| 部署第二批到生产 | **不需要**：线上 v0.8.1 无角色包体系、无快照端点，这批修复只在开发版生效 |
