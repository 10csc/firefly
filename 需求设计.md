# 需求设计说明 V4

本文档为需求总览。各模块详细规格见 `docs/modules/` 下的独立文档。

当前使用 DeepSeek V4 Flash 作为 LLM API。

---

## 一、当前进度

### 已完成

| 模块 | 位置 | 说明 |
|------|------|------|
| 规划判断器 | [app/modules/planning_judge.py](app/modules/planning_judge.py) | 纯分类器，5 步判定，输出 JudgeResult（含 execution_plan） |
| 回复生成 | [app/server.py](app/server.py) `call_reply()` | 基础角色扮演，临时路由（后续替换为统一回复模块） |
| 前端 | [app/static/](app/static/) | 星铁风格聊天界面，动态 API Key |

### 进行中

| 模块 | 状态 | 说明 |
|------|------|------|
| 上下文管理器 | 设计中 | 对话历史 + token 估算 + 精力计算 |

### 待实现

| 模块 | 说明 |
|------|------|
| 状态更新器 #1 | 输入影响（判断器输出 + 用户原文 → 5 个内部状态更新） |
| 状态更新器 #2 | 行动影响（回复后状态更新） |
| 规划器 | 接收更新器合并输出（JudgeResult + 新 state），执行 knowledge/memory/tools |
| 回复模块 | 统一入口，接收上下文 + state 生成回复 |
| 记忆系统 | 短期/长期/核心记忆 |
| 工具系统 | time、搜索等 |

---

## 二、核心流程（V4）

```
用户输入
    │
    ▼
规划判断器 (Planning Judge)
  ─ 纯分类器，输出 JudgeResult（stopped_at, stop_reason, execution_plan）
    │
    ▼
状态更新器 #1 (State Updater)
  ─ 输入：user_input + JudgeResult + 当前 state + energy(从上下文管理器)
  ─ 输出：更新后的 mood/relationship/tension/initiative
  ─ 注意：更新器将 JudgeResult 和 新 state 合并输出，传给下游
    │
    ▼ JudgeResult + updated State
    │
    ├─ 中断路径 (stopped_at≤1, direct)
    │     └─ 回复模块（轻量上下文）
    │
    └─ 正常路径 (stopped_at≥2)
          └─ 规划器 → 回复模块（完整上下文）
              │
              ▼
          状态更新器 #2 (行动影响)
              │
              ▼
          返回用户
```

### 状态维度

**内部状态（状态更新器维护）**：

| 属性 | 范围 | 说明 |
|------|------|------|
| mood | 标签 | LLM 选择。禁止：愤怒、哭泣 |
| energy | 0-300 | 由上下文管理器计算（token 占用率），状态更新器只读 |
| relationship | 65-100 | 好感度，单次变化 0.1~1.0，长期属性 |
| tension | 0-100 | 紧张度，不自然回落（仅休息时回落） |
| initiative | 0-100 | 主动性，高→主动打招呼、回复更长 |

**外部场景（系统维护，状态更新器只读）**：

| 属性 | 说明 |
|------|------|
| time | 当前时间/星期/时段 |
| event | idle/task/combat/story |

### 模块间数据流

```
上下文管理器 → energy → 状态更新器
规划判断器  → JudgeResult → 状态更新器
状态更新器  → JudgeResult + new State → 规划器
规划器      → AssembledContext → 回复模块
回复模块    → reply_text → 状态更新器 #2
```

---

## 三、场景真实化（讨论中）

### 3.1 对话上下文扩展

| 维度 | 字段 | 来源 |
|------|------|------|
| 时间 | 时分、星期、时段标签 | 设备本地时间 |
| 地点 | 位置信息（预留） | IP 或传感器 |
| 角色状态 | mood/energy/relationship/tension/initiative | 状态更新器 |
| 对话历史摘要 | 长对话压缩摘要 | 后续上下文压缩模块 |

### 3.2 状态驱动

- 状态更新器 #1：用户输入 → 流萤状态变化
- 状态更新器 #2：流萤回复后 → 自身行动影响状态
- 不中断对话通知状态变化（静默），后续可考虑可视化

---

## 四、远期规划

- 规划器：执行 execution_plan，加载知识/查询记忆/调用工具/组装上下文
- 记忆系统：仿人类记忆（短期/长期/核心）
- 工具集成：表情包、搜索，模块化热插拔
- 上下文伪装：长对话 → 困意 → 强制休息（后台压缩）
- 多对象适配：对银狼/刃/卡芙卡的不同说话风格

---

## 文档索引

| 文档 | 路径 |
|------|------|
| 项目设计原则 | [CLAUDE.md](CLAUDE.md) |
| 错误总结 | [docs/错误总结.md](docs/错误总结.md) |
| 流萤说话风格 | [docs/流萤说话风格对照.md](docs/流萤说话风格对照.md) |
| 训练对话样本 | [datebase/训练数据_流萤角色对话样本集.md](datebase/训练数据_流萤角色对话样本集.md) |
| 规划判断器规格 | [docs/modules/规划判断器/规格.md](docs/modules/规划判断器/规格.md) |

---

## 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| V1 | — | 初始判定流水线框架 |
| V2 | 2026-06-26 | 话题过滤、上下文分层、工具模块化、违规分级、好感度；文档拆分 |
| V3 | 2026-06-27 | MVP 收尾，标注已完成项，新增场景真实化讨论框架 |
| V4 | 2026-06-28 | MVP 模块化：规划判断器独立、上下文管理器设计、状态更新器/规划器/回复模块设计 |
