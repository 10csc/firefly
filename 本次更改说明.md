我是一个开发者，正在维护一个服务器项目。项目结构如下：
- 服务器版本的代码有部分存储在本地
- 登录服务器所需的密钥也在本地存储（非SSH密钥，请注意识别）
- 每台服务器的各项信息（IP、端口、账号等）都存放在对应的文件夹中

当前需要完成两项任务：优化前端下载页面 +写一个管理员页面。

说明：
前端下载页动态元素：飞行+发光点（萤火虫）
背景：上半夜空（繁星），页面下半是晚霞偏向粉紫的风格（中间渐变过渡）。此描述只是大致，风格实际情况以流萤的眼睛（"C:\Users\FANGL\Desktop\firefly\tupian\oson.png"图中人物的眼睛）为准。

以此为中心，自行设计其他元素的优化方案

管理员页面仅允许服务器管理员身份登录（访问路径限定在阿里云服务器给的管理员特定端口号上），我希望本机能够直接访问管理员相关页面（能做到吗？不能就用传统的方式（不能的话端口就放现有的公开端口），将已有的qq邮箱为1280728905qq.com的账号设置为管理员身份）。
管理员页面能够看见用户相关数据（需要你自己设计），包括用户账号，最近服务器收到的请求等。
