<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 数据导出：下载 zip 到系统下载目录（API 28 及以下需要写外部存储；29+ 走 scoped storage 免权限） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!-- 图片发送：Android 13+ 用系统照片选择器（PickVisualMedia 免权限）；此权限为 Android 12-
         旧版 GET_CONTENT 兜底 + 部分厂商相册偏好所需（仅申请，12- 才请求） -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- 忽略电池优化：后台回复流程防进程冻结（OPPO/ColorOS 会冻结后台进程，即使前台服务） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 安全审查 2026-08-25：allowBackup 改 false——user_data 含 API Key（config.json）与
         登录凭证（auth.json），自动云备份会经 adb/厂商通道把密钥带出设备；项目自带
         导出/同步链路，无需系统级备份（教训：禁止在 application 标签属性间插注释） -->
    <application
        android:name="com.chaquo.python.android.PyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Firefly"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 诊断包分享：ACTION_SEND 带附件要用 content:// URI（Android 7+ 禁 file://），
             所以必须有 FileProvider；白名单只放 cacheDir/diagnostics/（见 res/xml/file_paths.xml） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.firefly.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 后台保活：对话流程期间进程不被系统回收 -->
        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="keep_chat_service_alive_for_long_replies" />
        </service>
    </application>
</manifest>
