# Firefly Agent Project

这是「Firefly（萤火虫）」角色扮演独立 Agent 项目。目标是构建一个独立运行的角色扮演系统（不依附于 Claude Code），使用 DeepSeek API 作为语言模型后端。

## 项目设计原则（硬性约束）

### 执行成本铁律（硬约束：绝对禁止子代理）

**禁止使用任何形式的子代理 / 多智能体编排**：`subagent`、`subagent_fork`、`workflow`、`ralph` 一律不许调用，
也不许用任何其他方式派生下级执行体。调研、检索、审计、并行任务——**全部由主会话直接做**。

- **事故记录（2026-09-11）**：一次语音模型调研，主会话派了 2 个子代理；子代理**自行层层派生到 3 层、共 24 个**。
  **不到 5 分钟花掉 18 元**，当日 token 1.317 亿（未命中缓存输入 983 万、输出 152 万）。
  根因是子代理会不受控地自我繁殖，成本无法预估。
- **替代做法**：
  - 联网调研 → 直接用 `web_search` / `web_fetch`，主会话自己读、自己筛
  - 代码审计 → `grep` / `read` / `glob` 直接查
  - 长任务 → 后台 job（`run_in_background: true`）+ `job_output` 收结果，**不要**用子代理
- **例外：无。** 即使某任务看起来"适合并行"，也必须主会话顺序做完。

### 模块设计铁律
每个模块必须遵循固定流程：**接收输入 → 审查约束（输入合法性）→ 模块处理 → 验证结果（输出合法性）→ 最终输出**。
- 审查阶段：校验输入格式、类型、边界值，不合法直接拒绝并记录
- 验证阶段：校验输出是否符合预期格式和约束，失败时记录并返回错误标记
- 两个阶段都需要做好监控（日志/计数器），确保能快速定位问题模块

### 设计方法铁律（每次设计前必须执行，禁止跳步）

任何系统行为（情绪、对话、状态变化、数值规则）的设计，遵循**三层思考法**：

**第一层：真实世界是什么样的？**
先抛开代码，思考真实人类在这个情境下会怎样——什么反应、持续多久、为什么。
如果自己不确定，先查、先问、先讨论。禁止用"我觉得"代替事实。

**第二层：流萤是什么样的？**
在真实世界基础上，叠加上流萤的独特性——
- 温柔克制、安静真诚、不浮夸
- 身患失熵症，理解死亡但不绝望，珍惜当下
- 渴望被当作"人"而非兵器
- 对开拓者好感高，但表达方式克制
哪些反应和普通人一致？哪些因为她的性格/经历而不同？**为什么不同？**

**第三层：怎么用规则表达？**
前两层想清楚了，再用代码/数值/提示词去实现。
规则是工具，不是目的。如果一个规则在技术上完美但感受上不对——改规则，别改感受。

**禁止行为**：直接从规则倒推设计（"这个规则看起来合理"）、不思考就套用、把教条当真理。

### 模型与思考模式选择

项目使用 DeepSeek V4 系列（Flash / Pro），均支持三档思考模式：

| 模式 | 关键参数 | 特点 | 适用 |
|------|------|------|------|
| **Non-think** | `extra_body={"thinking":{"type":"disabled"}}` | 无推理，最快最省；**temperature 等采样参数只在此模式生效** | 简单分类（organizer 表情包选择）、数值判定 |
| **Think High** | `extra_body={"thinking":{"type":"enabled"},"reasoning_effort":"high"}` | 逻辑推理，兼顾速度与准确性 | **默认**——分析器、回复器、记忆整理 |
| **Think Max** | `extra_body={"thinking":{"type":"enabled"},"reasoning_effort":"max"}` | 极限推理，token ~2x，需 384K+ 上下文 | 复杂 Agent 场景——多工具链调用、大量提示词堆叠、高风险决策 |

**原则**：
- **thinking 默认 enabled**——不传参数就是思考模式。必须显式 `{"thinking":{"type":"disabled"}}` 才是 Non-think（官方文档 guides/thinking_mode）。
- **思考模式下 temperature/top_p/presence_penalty/frequency_penalty 静默无效**（官方明确：不报错但无效果）。调温度必须先关思考。effort 三档 low/high/max 直通（2026-08-13 起 low 是真实档位；`none` = 显式关闭思考）。
- 默认用 Flash + Think High。当前简单测试中 Think High 比 Non-think 还省 token（思考让输出更精炼）。
- Think Max 仅在提示词/工具大量堆叠后 Think High 不够用时启用。当前简单场景 Max 可能过度思考破坏结构化输出，不代表能力弱——是场景不够复杂。
- Pro 仅在知识密集型任务（搜索、问答）或 Flash 明确不够用时启用。成本差 ~12 倍。
- 每次模型选择需记录：任务类型、为什么选这个模式、实测 token 消耗。禁止盲选。

### 依赖管理
- 模块必须健壮、内化。对任何外部代码库的依赖保持高度敏感
- 优先将外部逻辑内化到模块中（自己实现），而非简单调用
- 实在需要外部依赖时，必须标注：依赖名称、版本号、依赖文件在库中的相对路径
- 禁止"为了一行代码引入一个库"

### 信息获取
- 深度搜索和研究使用 Certus CLI（本地工具，路径见个人环境配置）弥补信息差距
- Certus 可进行联网深度搜索，用于获取前沿信息、官方文档、社区方案

### 文档管理
- 文档采用树状分文件结构，禁止将所有内容堆砌到单一文档
- 每个模块独立编写自己的规格文档
- 分文件阈值：单文档超过模型有效注意长度（~200 行）时必须拆分
- 模块文档按模块目录存放，需求文档按主题分文件存放
- 文档根索引用 README.md 或索引文件管理

## 已知错误模式（每次启动必读，避免再犯）

见 [docs/错误总结.md](docs/错误总结.md)

11 条已犯错误摘要：
1. 跳步——没做需求就动手
2. 方向误判——把独立 Agent 做成 Claude Code Skill
3. 文档堆砌——所有内容塞一个文件
4. 约束层次混乱——流程步骤当代码模块设计
5. 过早过度设计——没讨论先写死细节
6. 替用户做决定——未经确认就执行
7. **子代理烧钱——用多智能体编排做本可主会话直接做的事（2026-09-11，5 分钟 18 元）**
8. **陈腐缓存 + 先怀疑素材——语音参考排查走 7 轮弯路（2026-09-12）。先控制变量再下结论；长时间任务产物复用前必须校验完整性**
9. **把资产放进 Gradle `Sync` 任务的输出目录——构建成功但文件被静默删掉（2026-09-18）。想进 APK 的文件必须走 Sync 的输入**
10. **两个口径不一致却当成一个——记忆整理游标用了被 40 轮截断的内存窗口，长对话重启后记忆永不更新且零报错（2026-09-18）。要写盘/当权威的口径一律从盘上取**
11. **告警去重键选了"每次都变"的字段——标记文件每分钟重写时间戳，一次 SSH 封禁被当成 4-5 个事件反复弹窗（2026-09-18）。去重键要用事件本身的特征，时间戳只能做冷却窗口**

## 开工必读清单（每次动手前，顺序执行）

1. 本文件（设计原则与模型选择，**含执行成本铁律：禁止子代理**）
2. [docs/未完成事项清单.md](docs/未完成事项清单.md)（**唯一待办真相源**：还剩什么没做；
   明文传输/部署债/安装包重建这三类问题已反复被审出多次，做完一条删一条，禁止只写进报告不入清单）
3. [docs/错误总结.md](docs/错误总结.md)（11 条已犯错误）
4. [docs/开发规范.md](docs/开发规范.md)（Flash 执行规范：核心流程冻结 + 一切扩展走统一工具协议 + 工具结果进消息历史）
5. 涉及本地运行/测试/构建 PC/APK/安装器时，先读 [docs/构建规范.md](docs/构建规范.md)（依赖位置总表，避免重新探测环境）；
   发版必须过 `tools/check_apk_manifest.py`（验 `allowBackup=false`、包内含 `sync.js`）
6. 涉及服务器操作时，先读 [docs/服务器管理规范.md](docs/服务器管理规范.md)；
   **注意本地工作区常有大量未提交改动——严禁全量 `scp` 同步，只能单文件挑传**
7. 涉及提交/推送/发布/部署/归档时，先读 [docs/部署与发布约定.md](docs/部署与发布约定.md)
8. 发版或**改动前端/后端逻辑**（可能要走热更）时，先读 [docs/热更新规范.md](docs/热更新规范.md) +
   [实现契约](docs/热更新/02_实现契约.md)。三条最容易踩的：
   ① **门禁是五道**（新增 `tools/check_hotupdate.py`）；
   ② 热更只用于**修 bug**，不许发新功能；
   ③ **底座内容变了必须升版本号**，否则旧补丁会与新整包混在一起（§5.4 反向铁律）。
9. 改动**公告/更新说明**（服务端下发内容），或改动 `app/static/` 下任何文件时，先读
   [docs/公告通道规范.md](docs/公告通道规范.md)。三条最容易踩的：
   ① 公告**只接受结构化块**（p/h/li/tip/img），端侧一律 `textContent` 渲染 —— 别引入任何 HTML；
   ② 内容源在 `notice_src/`、运行期缓存在 `notice/`（**两个目录刻意不同名**，别合并）；
   ③ 接口是"先回缓存、后台刷新"，**消费方必须处理 `refreshing` 字段**（不跟第二次 = 功能像没做）。
   改完 `app/static/` 必跑 `python tools/build_frontend_bundle.py` + `sync_frontends.py`，
   并在发版前确认 `tests/test_frontend_boot.py` 绿（它把**产物 bundle** 真跑一遍，
   抓的是"源码全对、拼起来就崩"的 TDZ 类问题，见 `docs/错误总结.md` #15）。

## 当前阶段

**双线版本**：
- **本地版**（`app/`）：PC + 安卓，用户设备独立运行，MVP 已跑通（LLM 子代理检索 → 分析器 → 回复器 → 组织器 + 聊天前端）
- **服务器版**（`server/`）：云服务器多用户部署，用户自带 API Key（存浏览器 localStorage，服务器不落盘），数据按账号（user_id）隔离——见 [docs/部署说明.md](docs/部署说明.md)

下一步：本地版**场景真实化**；服务器版部署验证与推广。
详见 [docs/需求设计.md](docs/需求设计.md) V5。原则：先讨论再动手，不在没有充分讨论的情况下做架构决策。

## 协作记忆（跨会话，自动生效）

用户习惯与长期方向见 [docs/协作记忆.md](docs/协作记忆.md)。规则：每个会话结束前主动更新该文件（追加用户新习惯/方向变化，删除过时条目）；开工前先读一次。
模型分工：默认 flash 执行；规划、架构决策、系统级错误排查在回复中标记「建议切 pro」，由用户切换后同会话继续。
